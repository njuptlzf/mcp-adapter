# Phase 1: Universal Interfaces - Plan

**Created:** 2026-06-10
**Phase:** 01-universal-adapter
**Wave:** 1

<overview>
Create generic interfaces and Pi adapter implementation. No core MCP logic changes in this phase - purely additive.
</overview>

<tasks>
## Task List

### Task 1: Create interfaces/agent-api.ts
**File:** `interfaces/agent-api.ts`
- Define `AgentAPI` interface with 8 methods (registerTool, registerCommand, registerFlag, on, getAllTools, getFlag, sendMessage, exec)
- Define `AgentContext` interface with cwd, hasUI, ui?, model?, modelRegistry?, signal?, reload?
- Define `UISystem` interface with required notify, optional setStatus/form/custom/theme
- Define `ToolRegistration`, `CommandConfig`, `FlagConfig`, `FormConfig`, `FormResult`, `FormField` types
- Export all interfaces
**Verification:** TypeScript compiles without errors, all types properly exported

### Task 2: Create adapters/pi-adapter.ts
**File:** `adapters/pi-adapter.ts`
- Create `PiAdapter` class implementing AgentAPI
- Implement registerTool via pi.registerTool with type cast
- Implement registerCommand/registerFlag/on/getAllTools/getFlag/sendMessage/exec
- Create `adaptPiContext` function converting ExtensionContext to AgentContext
- Create `adaptPiUI` helper for ExtensionUIContext to UISystem conversion
- Handle optional UI methods (form, custom, theme)
**Verification:** All methods compile, unit tests pass

### Task 3: Create MAPPING.md
**File:** `MAPPING.md`
- Document all 8 API method mappings (AgentAPI → Pi)
- Document all UI method mappings (UISystem → Pi)
- Document type mappings (AgentAPI ↔ ExtensionAPI)
- Include file references and line numbers
- Add upstream update checklist
**Verification:** All current usages documented, clear for future maintainers

### Task 4: Update package.json
**File:** `package.json`
- Move `@earendil-works/pi-coding-agent` to peerDependencies with optional: true
- Move `@earendil-works/pi-ai`, `@earendil-works/pi-tui` to optionalDependencies
- Add note about Pi being optional peer dependency
**Verification:** npm install succeeds, pnpm install succeeds

### Task 5: Update index.ts exports
**File:** `index.ts`
- Add imports for PiAdapter and type exports
- Export `piMcpAdapter` as alias for backward compatibility
- Export `PiAdapter`, `adaptPiContext`
- Export types: `AgentAPI`, `AgentContext`, `UISystem`
- Do NOT export `createMcpAdapter` (incomplete)
**Verification:** All exports work, backward compatible

### Task 6: Add unit tests for PiAdapter
**File:** `__tests__/pi-adapter.test.ts`
- Test registerTool wraps pi.registerTool correctly
- Test getAllTools returns pi.getAllTools result
- Test adaptPiContext converts context properly
- Test adaptPiUI handles optional methods
- Test exec returns Promise correctly
**Verification:** All tests pass (vitest run)

### Task 7: Add integration test
**File:** `__tests__/integration.test.ts`
- Test mcpAdapter still works with original Pi API
- Verify exported types are correct
- Test adapter can be instantiated separately
**Verification:** Integration tests pass
</tasks>

<verification>
## Success Criteria

1. TypeScript compiles: `npx tsc --noEmit` succeeds
2. Tests pass: `npm test` with new tests passing
3. Backward compatibility: Existing Pi usage unchanged
4. Interfaces complete: All 8 AgentAPI methods defined
5. Documentation complete: MAPPING.md covers all current usages
6. Import paths: All type imports resolve correctly

## Test Commands
```bash
npx tsc --noEmit
npm run test
```
</verification>