---
phase: 04-testing-verification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - __tests__/mock-adapter.test.ts
  - __tests__/adapter-contract.test.ts
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Non-Pi agent adapter can be created following the same contract as PiAdapter"
    - "All AgentAPI methods work correctly with mock adapter"
    - "All UISystem methods work correctly with mock adapter"
    - "Adapter pattern is verified for multiple agent scenarios"
  artifacts:
    - path: "__tests__/mock-adapter.test.ts"
      provides: "Mock adapter for non-Pi agent testing"
      min_lines: 80
    - path: "__tests__/adapter-contract.test.ts"
      provides: "Contract verification tests"
      min_lines: 60
  key_links:
    - from: "__tests__/mock-adapter.test.ts"
      to: "interfaces/agent-api.ts"
      via: "MockAdapter implements AgentAPI"
    - from: "__tests__/adapter-contract.test.ts"
      to: "__tests__/mock-adapter.test.ts"
      via: "Imports MockAgentAPI for testing"
---

<objective>
Create mock adapters and contract tests to demonstrate the universal adapter pattern works across different agent implementations.

Purpose: Per REQ-07, Phase 4 needs tests for multiple agent scenarios. Since Pi-specific tests already exist, this plan adds abstract contract verification using a mock adapter.

Output: Test files validating AgentAPI/UISystem contract compliance
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@interfaces/agent-api.ts
@adapters/pi-adapter.ts
@__tests__/pi-adapter.test.ts
@__tests__/integration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MockAdapter implementing AgentAPI</name>
  <files>__tests__/mock-adapter.test.ts</files>
  <behavior>
    - Test 1: MockAdapter.registerTool stores tool in internal array
    - Test 2: MockAdapter.registerCommand stores command by name
    - Test 3: MockAdapter.registerFlag stores flag with config
    - Test 4: MockAdapter.on registers event handler
    - Test 5: MockAdapter.getAllTools returns all registered tools
    - Test 6: MockAdapter.getFlag returns stored flag value or undefined
    - Test 7: MockAdapter.sendMessage forwards message + options
    - Test 8: MockAdapter.exec runs command and returns mock result
    - Test 9: MockAdapter implements AgentContext for testing
    - Test 10: MockAdapter implements UISystem with notify/setStatus/form/custom
  </behavior>
  <action>Create a MockAgentAPI class that implements AgentAPI interface with a MockAgentContext. Add tests verifying all methods work correctly. Use vi.fn() mocks for all function-type fields.</action>
  <verify>
    <automated>npx vitest run __tests__/mock-adapter.test.ts</automated>
  </verify>
  <done>All 10 tests pass demonstrating mock adapter implements AgentAPI contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create adapter contract verification tests</name>
  <files>__tests__/adapter-contract.test.ts</files>
  <behavior>
    - Test 1: AgentAPI contract - all required methods exist on any AgentAPI implementation
    - Test 2: AgentContext contract - cwd and hasUI are required, ui is optional
    - Test 3: UISystem contract - notify is required, all others optional
    - Test 4: Adapter pattern - MockAdapter can be used where PiAdapter is used
    - Test 5: initializeMcp works with any AgentAPI implementation (using mock)
  </behavior>
  <action>Create contract tests that verify any AgentAPI implementation must have all required methods. Import and use MockAgentAPI to test that init.ts works with any compliant adapter.</action>
  <verify>
    <automated>npx vitest run __tests__/adapter-contract.test.ts</automated>
  </verify>
  <done>5 contract tests pass, demonstrating universal adapter pattern works for any AgentAPI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → adapter | Test code calls adapter methods; untrusted input would be mock data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Injection | MockAdapter | accept | Test mocks use controlled vi.fn() stubs; no external input processing |
| T-04-02 | Tampering | Test data | accept | Test artifacts are dev-only; not shipped in package |
</threat_model>

<verification>
- All mock-adapter tests pass
- All adapter-contract tests pass
- TypeScript compilation succeeds
- Total test suite maintains baseline (350+ passing)
</verification>

<success_criteria>
- MockAgentAPI implements AgentAPI interface fully
- Contract tests verify universal adapter pattern
- No new TypeScript errors introduced
- All tests pass (existing 350 + new 15+)
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-verification/04-01-SUMMARY.md`
</output>