---
phase: 04-testing-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Test coverage can be generated with vitest --coverage"
    - "Coverage configuration is properly set up"
    - "Coverage report shows adapter coverage metrics"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Coverage reporting configuration"
      min_lines: 15
  key_links:
    - from: "vitest.config.ts"
      to: "vitest execution"
      via: "coverage reporter configuration"
---

<objective>
Configure vitest for test coverage reporting and generate initial coverage metrics.

Purpose: Enable test coverage reporting to validate the universal adapter has adequate test coverage per REQ-07.

Output: Configured vitest.config.ts with coverage settings, coverage report generated
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@vitest.config.ts
@__tests__/pi-adapter.test.ts
@__tests__/integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install coverage dependency and configure vitest</name>
  <files>vitest.config.ts</files>
  <action>Add `@vitest/coverage-v8` to devDependencies and configure vitest.config.ts to enable coverage reporting. Set coverage thresholds for interfaces/ and adapters/ directories at 80%. Include reporters: text, html, json.</action>
  <verify>
    <automated>npm install @vitest/coverage-v8 --save-dev && npx vitest run --coverage 2>&1 | head -50</automated>
  </verify>
  <done>Coverage dependency installed, vitest --coverage runs successfully</done>
</task>

<task type="auto">
  <name>Task 2: Run coverage report and verify adapter coverage</name>
  <files></files>
  <action>Run coverage report targeting interfaces/ and adapters/ directories. Verify coverage percentages meet thresholds for the universal adapter project.</action>
  <verify>
    <automated>npx vitest run --coverage --reporter=text 2>&1 | grep -E "(interfaces/|adapters/|Total)"</automated>
  </verify>
  <done>Coverage report shows interfaces/ and adapters/ coverage at 80%+ threshold</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | No security boundaries; configuration-only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | — | Coverage config | accept | Low-risk dev tooling configuration |
</threat_model>

<verification>
- npm install succeeds for @vitest/coverage-v8
- vitest --coverage runs without errors
- Coverage threshold for interfaces/ meets 80%
- Coverage threshold for adapters/ meets 80%
</verification>

<success_criteria>
- @vitest/coverage-v8 installed as devDependency
- vitest.config.ts has coverage configuration with reporters
- Coverage report generates successfully
- interfaces/ and adapters/ coverage ≥ 80%
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-verification/04-02-SUMMARY.md`
</output>