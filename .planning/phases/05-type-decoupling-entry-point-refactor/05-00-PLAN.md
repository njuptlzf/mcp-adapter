---
phase: 05-type-decoupling-entry-point-refactor
plan: 00
type: execute
wave: 0
depends_on: []
files_created:
  - __tests__/entry.test.ts
  - types/pi-coding-agent.d.ts
  - types/pi-ai.d.ts
  - types/pi-tui.d.ts
files_modified:
  - tsconfig.json
  - .planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md
autonomous: true
requirements:
  - WAVE0-01
must_haves:
  truths:
    - All Phase 5 test files required by downstream verify commands exist before Wave 1 runs
    - TypeScript declaration stubs exist for optional Pi peer packages so the project compiles without them installed
    - tsconfig.json includes the types/ directory
    - 05-VALIDATION.md frontmatter marks wave_0_complete and nyquist_compliant true
  artifacts:
    - path: __tests__/entry.test.ts
      provides: Wave 0 stub for ENTRY-01 entry-point tests
      contains:
        - describe
        - it
    - path: types/pi-coding-agent.d.ts
      provides: TypeScript stubs for @earendil-works/pi-coding-agent
    - path: types/pi-ai.d.ts
      provides: TypeScript stubs for @earendil-works/pi-ai
    - path: types/pi-tui.d.ts
      provides: TypeScript stubs for @earendil-works/pi-tui
---

<objective>
Create Wave 0 test stubs and optional Pi peer-package TypeScript declaration stubs so that every file referenced by downstream Phase 5 verify commands exists and the project type-checks before Wave 1 begins. Update the phase validation file to mark Wave 0 complete.

Purpose: Satisfy Nyquist compliance and prevent downstream verify commands from failing on missing files or unresolved Pi peer types.
Output: Required test stub, Pi peer declaration stubs, updated tsconfig.json, and updated 05-VALIDATION.md frontmatter.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md
@tsconfig.json
@__tests__/index-lifecycle.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript declaration stubs for optional Pi peer packages</name>
  <files>types/pi-coding-agent.d.ts, types/pi-ai.d.ts, types/pi-tui.d.ts, tsconfig.json</files>
  <read_first>
    @tsconfig.json
    @adapters/pi-adapter.ts lines 14-27
    @sampling-handler.ts lines 1-12
    @tool-result-renderer.ts lines 1-10
  </read_first>
  <action>Create minimal ambient TypeScript declaration stubs under `types/` for the optional peer packages `@earendil-works/pi-coding-agent`, `@earendil-works/pi-ai`, and `@earendil-works/pi-tui`. Export the types/functions/classes currently referenced by the codebase (e.g., `ExtensionAPI`, `ExtensionContext`, `ExtensionUIContext`, `ToolInfo`, `ModelRegistry`, `AgentToolResult`, `AgentToolUpdateCallback`, `ToolRenderResultOptions`, `complete`, `Api`, `AssistantMessage`, `Message`, `Model`, `TextContent`, `ImageContent`, `Text`, `matchesKey`, `truncateToWidth`, `visibleWidth`) as `any`/variadic constructors so the project type-checks when the real packages are not installed. Update `tsconfig.json` to include `"types/**/*.ts"` in the `include` array.</action>
  <acceptance_criteria>
    - `types/pi-coding-agent.d.ts`, `types/pi-ai.d.ts`, and `types/pi-tui.d.ts` exist.
    - `grep -n "declare module" types/pi-coding-agent.d.ts` shows the module declaration.
    - `grep -n "declare module" types/pi-ai.d.ts` shows the module declaration.
    - `grep -n "declare module" types/pi-tui.d.ts` shows the module declaration.
    - `tsconfig.json` includes `"types/**/*.ts"` in the `include` array.
    - `npx tsc --noEmit` passes after the stubs are created.
  </acceptance_criteria>
  <verify>
    <automated>test -f types/pi-coding-agent.d.ts &amp;&amp; test -f types/pi-ai.d.ts &amp;&amp; test -f types/pi-tui.d.ts &amp;&amp; grep -n "declare module" types/pi-coding-agent.d.ts &amp;&amp; grep -n "declare module" types/pi-ai.d.ts &amp;&amp; grep -n "declare module" types/pi-tui.d.ts &amp;&amp; grep -n '"types/\*\*/\*.ts"' tsconfig.json &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript stubs allow the project to compile without the optional Pi peer packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure required Phase 5 test stub exists</name>
  <files>__tests__/entry.test.ts</files>
  <read_first>
    @.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md
    @__tests__/index-lifecycle.test.ts
  </read_first>
  <action>If `__tests__/entry.test.ts` does not exist, create a minimal Wave 0 stub using vitest's `describe`/`it` pattern. The other downstream test files (`sampling-handler`, `tool-result-renderer`, `elicitation-handler`, `server-manager-sampling`, `pi-adapter`) already exist in the repository and will be updated by the relevant Wave 1–3 plans. This Wave 0 task only verifies that `__tests__/entry.test.ts` exists.</action>
  <acceptance_criteria>
    - `test -f __tests__/entry.test.ts` passes.
    - `grep -n "describe" __tests__/entry.test.ts` shows at least one test suite.
  </acceptance_criteria>
  <verify>
    <automated>test -f __tests__/entry.test.ts &amp;&amp; grep -n "describe" __tests__/entry.test.ts</automated>
  </verify>
  <done>`__tests__/entry.test.ts` exists as a Wave 0 stub.</done>
</task>

<task type="auto">
  <name>Task 3: Update 05-VALIDATION.md Wave 0 status</name>
  <files>.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md</files>
  <read_first>
    @.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md
  </read_first>
  <action>Update the frontmatter of `05-VALIDATION.md` to `wave_0_complete: true` and `nyquist_compliant: true`. Also mark the Wave 0 Requirements checkboxes as complete.</action>
  <acceptance_criteria>
    - `grep "wave_0_complete: true" .planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md` succeeds.
    - `grep "nyquist_compliant: true" .planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep "wave_0_complete: true" .planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md &amp;&amp; grep "nyquist_compliant: true" .planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md</automated>
  </verify>
  <done>05-VALIDATION.md reflects Wave 0 completion and Nyquist compliance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundaries are introduced by creating test or declaration stubs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-SC | Tampering | npm installs | accept | No new packages are installed in this plan. |
</threat_model>

<verification>
- `types/pi-coding-agent.d.ts`, `types/pi-ai.d.ts`, and `types/pi-tui.d.ts` exist and declare the required modules.
- `tsconfig.json` includes `types/**/*.ts`.
- `npx tsc --noEmit` passes.
- `__tests__/entry.test.ts` exists.
- `05-VALIDATION.md` frontmatter shows `wave_0_complete: true` and `nyquist_compliant: true`.
</verification>

<must_haves>
## Must-Haves

### Observable Truths
- `__tests__/entry.test.ts` exists as a Wave 0 stub.
- TypeScript declaration stubs exist for optional Pi peer packages.
- `tsconfig.json` includes the `types/` directory.
- `05-VALIDATION.md` marks Wave 0 complete and Nyquist compliant.

### Required Artifacts
- `__tests__/entry.test.ts` — Wave 0 stub.
- `types/pi-coding-agent.d.ts`, `types/pi-ai.d.ts`, `types/pi-tui.d.ts` — declaration stubs.
- `tsconfig.json` — updated include array.
- `05-VALIDATION.md` — updated frontmatter.
</must_haves>

<success_criteria>
- `__tests__/entry.test.ts` exists.
- TypeScript stubs allow `npx tsc --noEmit` to pass without the optional Pi peer packages.
- `tsconfig.json` includes `types/**/*.ts`.
- `05-VALIDATION.md` frontmatter is updated to `wave_0_complete: true` and `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/05-type-decoupling-entry-point-refactor/05-00-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `__tests__/entry.test.ts`: Wave 0 stub for entry-point tests.
- `types/pi-coding-agent.d.ts`, `types/pi-ai.d.ts`, `types/pi-tui.d.ts`: declaration stubs for optional Pi peer packages.
- `tsconfig.json`: updated to include the new declaration stubs.
- Updated `05-VALIDATION.md` frontmatter.
