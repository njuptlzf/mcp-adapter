---
phase: 05-type-decoupling-entry-point-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-dir.ts
  - __tests__/agent-paths-integration.test.ts
autonomous: true
requirements:
  - DECOUPLE-07
must_haves:
  truths:
    - agent-dir.ts reads MCP_AGENT_DIR first
    - PI_CODING_AGENT_DIR remains as backward-compatible fallback
    - Existing path normalization and default Pi directory behavior preserved
    - agent-paths integration tests still pass
  artifacts:
    - path: agent-dir.ts
      provides: Agent directory resolution with MCP_AGENT_DIR / PI_CODING_AGENT_DIR fallback
      exports:
        - getAgentDir
        - getAgentPath
---

<objective>
Replace the hardcoded `PI_CODING_AGENT_DIR` env variable in agent-dir.ts with a primary `MCP_AGENT_DIR` lookup while preserving `PI_CODING_AGENT_DIR` as a backward-compatible fallback. Path normalization and the default Pi directory remain unchanged.

Purpose: Make the default agent-path resolver usable by non-Pi agents without breaking existing Pi users who rely on `PI_CODING_AGENT_DIR`.
Output: Updated agent-dir.ts that honors the new env variable first and falls back to the old one.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md

Per AGENTS.md, run `gitnexus_impact({target: "getAgentDir", direction: "upstream"})` before editing it, and run `gitnexus_detect_changes()` before committing. Warn the user if impact analysis returns HIGH or CRITICAL risk.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-type-decoupling-entry-point-refactor/05-CONTEXT.md
@.planning/phases/05-type-decoupling-entry-point-refactor/05-RESEARCH.md
@.planning/phases/05-type-decoupling-entry-point-refactor/05-PATTERNS.md
@.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md
@agent-dir.ts
@interfaces/agent-paths.ts
@__tests__/agent-paths-integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP_AGENT_DIR support with PI_CODING_AGENT_DIR fallback in agent-dir.ts</name>
  <files>agent-dir.ts</files>
  <read_first>
    @agent-dir.ts
    @interfaces/agent-paths.ts
    @.planning/phases/05-type-decoupling-entry-point-refactor/05-PATTERNS.md lines 691-723
  </read_first>
  <action>Per DECOUPLE-07, update `getAgentDir` so the primary env variable is `MCP_AGENT_DIR` and `PI_CODING_AGENT_DIR` is the backward-compatible fallback. Preserve existing path normalization (`~`, `~/...`, absolute path) and the default `join(homedir(), ".pi", "agent")` behavior. Add a brief comment explaining the fallback. Do not change `getAgentPath` or the `AgentPathResolver` interface.</action>
  <acceptance_criteria>
    - `grep -n "MCP_AGENT_DIR" agent-dir.ts` shows the primary read.
    - `grep -n "PI_CODING_AGENT_DIR" agent-dir.ts` shows the fallback read.
    - `grep -n 'join(homedir(), ".pi", "agent")' agent-dir.ts` confirms the default is unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "MCP_AGENT_DIR" agent-dir.ts &amp;&amp; grep -n "PI_CODING_AGENT_DIR" agent-dir.ts &amp;&amp; grep -n 'join(homedir(), ".pi", "agent")' agent-dir.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>agent-dir.ts honors MCP_AGENT_DIR with PI_CODING_AGENT_DIR fallback while keeping the default Pi path.</done>
</task>

<task type="auto">
  <name>Task 2: Verify agent-paths integration tests still pass</name>
  <files>agent-dir.ts, __tests__/agent-paths-integration.test.ts</files>
  <read_first>
    @__tests__/agent-paths-integration.test.ts
    @.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md lines 38-52
  </read_first>
  <action>Run the agent-paths integration test suite. If no test covers the new `MCP_AGENT_DIR` fallback behavior, add a minimal test case that sets `MCP_AGENT_DIR` and asserts the resolved path, and another that clears `MCP_AGENT_DIR` but sets `PI_CODING_AGENT_DIR` and asserts the fallback path. Reset env vars in `afterEach` to avoid cross-test contamination.</action>
  <acceptance_criteria>
    - `npx vitest run __tests__/agent-paths-integration.test.ts` passes.
    - If new tests were added, `grep -n "MCP_AGENT_DIR" __tests__/agent-paths-integration.test.ts` and `grep -n "PI_CODING_AGENT_DIR" __tests__/agent-paths-integration.test.ts` show the new assertions.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run __tests__/agent-paths-integration.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>agent-paths integration tests verify both the new env variable and the backward-compatible fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → file path | `agent-dir.ts` translates environment configuration into a directory path used for config/cache lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | agent-dir.ts env fallback | accept | Env variables are process-local; fallback order does not expose new data |
| T-05-02-02 | Tampering | Path traversal via env override | accept | Existing normalization only expands `~` and resolves relative paths; no new traversal introduced |
| T-05-02-SC | Tampering | npm installs | mitigate | No new packages; if any install is required, apply slopcheck and a blocking human checkpoint for [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx vitest run __tests__/agent-paths-integration.test.ts` passes.
- `grep -n "MCP_AGENT_DIR" agent-dir.ts` shows the primary read.
- `grep -n "PI_CODING_AGENT_DIR" agent-dir.ts` shows the fallback read.
</verification>

<must_haves>
## Must-Haves

### Observable Truths
- agent-dir.ts reads `MCP_AGENT_DIR` first.
- `PI_CODING_AGENT_DIR` remains as backward-compatible fallback.
- Existing path normalization and default Pi directory behavior are preserved.
- agent-paths integration tests still pass.

### Required Artifacts
- `agent-dir.ts` — `getAgentDir` with `MCP_AGENT_DIR` / `PI_CODING_AGENT_DIR` fallback.
</must_haves>

<success_criteria>
- `getAgentDir` reads `MCP_AGENT_DIR` first and falls back to `PI_CODING_AGENT_DIR`.
- The default `join(homedir(), ".pi", "agent")` behavior is unchanged.
- `npx vitest run __tests__/agent-paths-integration.test.ts` passes.
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/phases/05-type-decoupling-entry-point-refactor/05-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `agent-dir.ts`: `getAgentDir` with `MCP_AGENT_DIR` / `PI_CODING_AGENT_DIR` fallback.
