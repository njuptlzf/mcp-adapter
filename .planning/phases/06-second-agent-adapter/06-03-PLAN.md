---
phase: 06-second-agent-adapter
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - vitest.config.ts
files_created:
  - adapters/qoder-renderer.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "adapters/qoder-renderer.ts exports a pass-through RenderOutput type + qoderRenderWrapper helper (mirrors adapters/pi-renderer.ts shape)"
    - "qoderRenderWrapper returns a RenderOutput string directly (Qoder's tool renderer surface uses strings, not a Text class) — does NOT import @earendil-works/pi-tui"
    - "adapters/qoder-renderer.ts is the only file in adapters/qoder-* that imports @qoder-ai/qoder-agent-sdk for renderer concerns (if any); the placeholder file itself needs no SDK import"
    - "vitest.config.ts coverage thresholds include adapters/qoder-adapter.ts, adapters/qoder-sampling-provider.ts, adapters/qoder-renderer.ts, scripts/qoder-smoke.ts at 80% lines/functions/branches/statements"
    - "npx tsc --noEmit remains clean after the vitest.config.ts update"
  artifacts:
    - path: adapters/qoder-renderer.ts
      provides: RenderOutput type + qoderRenderWrapper pass-through helper
      exports:
        - RenderOutput
        - qoderRenderWrapper
      min_lines: 20
    - path: vitest.config.ts
      provides: Coverage thresholds registered for all qoder-* source files created in Plans 01–03
      contains:
        - "adapters/qoder-adapter.ts"
        - "adapters/qoder-sampling-provider.ts"
        - "adapters/qoder-renderer.ts"
  key_links:
    - from: adapters/qoder-renderer.ts
      to: "@qoder-ai/qoder-agent-sdk (optional, only if SDK needs a typed render envelope)"
      via: "type-only import if needed"
      pattern: "from \"@qoder-ai/qoder-agent-sdk\""
    - from: vitest.config.ts
      to: adapters/qoder-adapter.ts
      via: "coverage threshold entry"
      pattern: "adapters/qoder-adapter\\.ts"
---

<objective>
Create the `adapters/qoder-renderer.ts` placeholder per D-11 (file layout) and per RESEARCH.md §Standard Stack §Alternatives Considered (Qoder tool renderers are defined at the tool definition level via `tool()` — there is no separate renderer wrap, so this file is a thin pass-through that mirrors `adapters/pi-renderer.ts`'s shape but returns strings, not `Text`). Also update `vitest.config.ts` coverage thresholds so the new source files are tracked at 80%.

Purpose: D-11's file layout mandates this file exists in `adapters/`. Even if Qoder's tool renderers are defined inline via `tool()`, the file gives future agent authors a place to add Qoder-specific render wrappers without polluting `adapters/qoder-adapter.ts`. Plan 04's contract test relies on the renderer file being present so future `qoder-renderer.ts` extensions have a hook point. Coverage thresholds ensure the new files stay exercised.

Output:
- New `adapters/qoder-renderer.ts` (~25 lines) with `RenderOutput` type + `qoderRenderWrapper` helper
- Modified `vitest.config.ts` adding coverage thresholds for all qoder-* source files
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md

Per AGENTS.md, run `gitnexus_impact({target: "piRenderWrapper", direction: "upstream"})` before editing vitest.config.ts or creating qoder-renderer.ts (pi-renderer is the closest analog), and run `gitnexus_detect_changes()` before committing. Warn the user if any impact analysis returns HIGH or CRITICAL risk.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-second-agent-adapter/06-CONTEXT.md
@.planning/phases/06-second-agent-adapter/06-RESEARCH.md
@.planning/phases/06-second-agent-adapter/06-VALIDATION.md
@adapters/pi-renderer.ts (current full file — read for the analog shape)
@vitest.config.ts (current — read for existing threshold pattern)
@adapters/qoder-adapter.ts (created in 06-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create adapters/qoder-renderer.ts as a thin pass-through placeholder</name>
  <files>adapters/qoder-renderer.ts</files>
  <read_first>
    @adapters/pi-renderer.ts (current full file — read for the analog shape: `RenderOutput = string` + a wrapper that returns a `Text` instance for Pi)
    @adapters/qoder-adapter.ts (created in 06-01 — confirm QoderAdapter does NOT currently use a renderer wrapper; the renderer file is a placeholder)
    @.planning/phases/06-second-agent-adapter/06-RESEARCH.md lines 281-282 (Alternatives Considered: "qoder-renderer.ts file from D-11 is optional and most likely ends up being a thin pass-through")
  </read_first>
  <action>
Per D-11 and RESEARCH.md §Alternatives Considered, create `adapters/qoder-renderer.ts` (~25 lines). This is a placeholder pass-through file: Qoder's tool renderers are defined inline at the tool definition level via `@qoder-ai/qoder-agent-sdk`'s `tool()` builder, so there is no separate renderer wrap analogous to Pi's `piRenderWrapper`. The file exists so future Qoder-specific render extensions have a hook point and to mirror the file-layout discipline of `adapters/pi-renderer.ts`.

**File contents (per RESEARCH.md Pattern under "Alternatives Considered"):**

```ts
/**
 * Qoder-specific render helpers.
 *
 * Qoder's tool renderers are defined inline at the tool definition level
 * (via @qoder-ai/qoder-agent-sdk's tool() builder), so there is no
 * separate wrapper analogous to piRenderWrapper.
 *
 * This file exists per D-11 (file layout) and as a hook point for future
 * Qoder-specific render extensions. The current QoderAdapter does not use
 * a renderer wrapper.
 */

export type RenderOutput = string;

/**
 * Pass-through helper. Returns the raw string from the supplied renderer.
 *
 * Future implementations may wrap this in a Qoder-specific envelope if the
 * SDK adds a typed render output class.
 */
export function qoderRenderWrapper<T extends (...args: unknown[]) => RenderOutput>(
  fn: T,
): (...args: Parameters<T>) => RenderOutput {
  return (...args: Parameters<T>) => fn(...args);
}
```

**File boundary check:**
- Do NOT import `@earendil-works/pi-tui` (Pi TUI is a separate boundary; the renderer file is qoder-specific).
- Do NOT import `@qoder-ai/qoder-agent-sdk` (the placeholder does not need SDK types — it only returns strings).
- Do NOT import `@earendil-works/pi-*` for any reason (T-06-SC isolation).
- Do NOT add any runtime logic beyond the type + the pass-through helper.

**No test file needed** — the placeholder is too thin to warrant a test (matches the precedent of `adapters/pi-renderer.ts` having no dedicated test file). If a future change introduces real rendering logic, the test goes alongside that change.
  </action>
  <acceptance_criteria>
    - `adapters/qoder-renderer.ts` exists and exports `RenderOutput` + `qoderRenderWrapper`.
    - `grep -n "export type RenderOutput" adapters/qoder-renderer.ts` finds the type declaration.
    - `grep -n "export function qoderRenderWrapper" adapters/qoder-renderer.ts` finds the function declaration.
    - `grep -c "@earendil-works" adapters/qoder-renderer.ts` returns 0 (no Pi TUI imports).
    - `grep -c "@qoder-ai" adapters/qoder-renderer.ts` returns 0 (no SDK imports needed for pass-through).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>test -f adapters/qoder-renderer.ts &amp;&amp; grep -n "export type RenderOutput" adapters/qoder-renderer.ts &amp;&amp; grep -n "export function qoderRenderWrapper" adapters/qoder-renderer.ts &amp;&amp; test "$(grep -c '@earendil-works' adapters/qoder-renderer.ts || true)" -eq 0 &amp;&amp; test "$(grep -c '@qoder-ai' adapters/qoder-renderer.ts || true)" -eq 0 &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>adapters/qoder-renderer.ts exists as a thin pass-through placeholder mirroring pi-renderer.ts shape but for Qoder; no Pi imports; no SDK imports.</done>
</task>

<task type="auto">
  <name>Task 2: Register coverage thresholds in vitest.config.ts for all qoder-* source files</name>
  <files>vitest.config.ts</files>
  <read_first>
    @vitest.config.ts (current full file — read for the existing thresholds block format and the precedent from 05-05-PLAN.md Task 3)
    @adapters/qoder-adapter.ts (created in 06-01)
    @adapters/qoder-sampling-provider.ts (created in 06-02)
    @adapters/qoder-renderer.ts (just created in Task 1)
    @scripts/qoder-smoke.ts (created in 06-01)
  </read_first>
  <action>
Per VALIDATION.md Sampling Rate and the precedent set in `05-05-PLAN.md` Task 3, register coverage thresholds for all qoder-* source files created in Plans 01–03 at 80% lines/functions/branches/statements, matching the existing `adapters/pi-adapter.ts` threshold pattern.

**Steps:**

1. Read the current `vitest.config.ts` and locate the `thresholds` block (or `coverage.thresholds` sub-object).
2. Add entries for the four new source files:
   - `adapters/qoder-adapter.ts`
   - `adapters/qoder-sampling-provider.ts`
   - `adapters/qoder-renderer.ts`
   - `scripts/qoder-smoke.ts`
3. Match the existing threshold shape (likely `{ lines: 80, functions: 80, branches: 80, statements: 80 }` per file). If the file is a placeholder with effectively no logic (e.g., `adapters/qoder-renderer.ts`), a lower threshold (e.g., 60) may be acceptable; document the choice in a comment.
4. Do NOT modify existing threshold entries — only add new ones.
5. Do NOT change the test runner, reporter, or any other config setting.
6. After the edit, run `npx tsc --noEmit` to confirm TypeScript still compiles.
7. Run `npx vitest run __tests__/qoder-adapter.test.ts __tests__/qoder-sampling-provider.test.ts --coverage --coverage.include=adapters/qoder-*.ts,interfaces/agent-paths.ts,scripts/qoder-smoke.ts` to confirm coverage actually meets the registered thresholds. If a file is under threshold, document why in the SUMMARY and decide whether to add more tests (defer to Plan 04) or lower the threshold (with a justification comment).

**File boundary check:**
- `vitest.config.ts` is the project's coverage config — modifying it is expected.
- Do NOT touch any other `*.config.ts` files (no `vite.config.ts`, no `tsconfig.json`).
  </action>
  <acceptance_criteria>
    - `grep -n "adapters/qoder-adapter.ts" vitest.config.ts` finds the new threshold entry.
    - `grep -n "adapters/qoder-sampling-provider.ts" vitest.config.ts` finds the new threshold entry.
    - `grep -n "adapters/qoder-renderer.ts" vitest.config.ts` finds the new threshold entry.
    - `grep -n "scripts/qoder-smoke.ts" vitest.config.ts` finds the new threshold entry (or is intentionally absent with a comment justifying the omission — pass-through files may not need coverage).
    - `npx vitest run __tests__/qoder-adapter.test.ts __tests__/qoder-sampling-provider.test.ts --coverage` exits 0 (thresholds met OR coverage failure logs are empty).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "adapters/qoder-adapter.ts" vitest.config.ts &amp;&amp; grep -n "adapters/qoder-sampling-provider.ts" vitest.config.ts &amp;&amp; grep -n "adapters/qoder-renderer.ts" vitest.config.ts &amp;&amp; npx vitest run __tests__/qoder-adapter.test.ts __tests__/qoder-sampling-provider.test.ts --coverage &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>vitest.config.ts coverage thresholds include all qoder-* source files; coverage run passes; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QoderAdapter → qoder-renderer | Future render wrappers will cross this boundary; today it's a pass-through |
| vitest.config.ts → coverage thresholds | Misconfigured thresholds could silently let regressions slip through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-R-01 | Tampering | `adapters/qoder-renderer.ts` | accept | Placeholder file has no logic to subvert. If a future implementation introduces render logic, it inherits the same risk profile as `piRenderWrapper` (no new attack surface vs. letting tool result content flow through unchanged). |
| T-06-R-02 | Repudiation | `vitest.config.ts` thresholds | mitigate | Every threshold entry has a name attached to a source file — easy to audit. Comment on placeholder files (e.g., `adapters/qoder-renderer.ts`) justifying any below-80% threshold. |
| T-06-R-SC | Tampering | npm installs | mitigate | No new installs in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `grep -n "adapters/qoder-adapter.ts" vitest.config.ts` confirms threshold entry.
- `grep -n "adapters/qoder-sampling-provider.ts" vitest.config.ts` confirms threshold entry.
- `grep -n "adapters/qoder-renderer.ts" vitest.config.ts` confirms threshold entry.
- `test -f adapters/qoder-renderer.ts` confirms placeholder exists.
- `grep -c "@earendil-works" adapters/qoder-renderer.ts` returns 0.
</verification>

<must_haves>
## Must-Haves

### Observable Truths
- adapters/qoder-renderer.ts exports a pass-through RenderOutput type + qoderRenderWrapper helper mirroring pi-renderer.ts shape.
- The placeholder does NOT import @earendil-works/pi-tui (Pi TUI is a separate boundary).
- The placeholder does NOT import @qoder-ai/qoder-agent-sdk (no SDK types needed for pass-through).
- vitest.config.ts coverage thresholds include adapters/qoder-adapter.ts, adapters/qoder-sampling-provider.ts, adapters/qoder-renderer.ts at 80% (with comment for any lower threshold on placeholder files).
- npx tsc --noEmit passes after both edits.

### Required Artifacts
- `adapters/qoder-renderer.ts` — RenderOutput type + qoderRenderWrapper pass-through helper.
- `vitest.config.ts` — coverage thresholds registered for all qoder-* source files.
</must_haves>

<success_criteria>
- adapters/qoder-renderer.ts exists with the pass-through shape.
- vitest.config.ts includes threshold entries for the qoder-* source files.
- npx tsc --noEmit passes.
- No regression in the existing test suite (npx vitest run baseline).
- D-11 file-layout requirement satisfied (adapters/qoder-renderer.ts exists).
</success_criteria>

<output>
Create `.planning/phases/06-second-agent-adapter/06-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `adapters/qoder-renderer.ts`: pass-through RenderOutput type + qoderRenderWrapper helper.
- `vitest.config.ts`: coverage thresholds registered for adapters/qoder-adapter.ts, adapters/qoder-sampling-provider.ts, adapters/qoder-renderer.ts, scripts/qoder-smoke.ts.
