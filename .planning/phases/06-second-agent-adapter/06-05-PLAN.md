---
phase: 06-second-agent-adapter
plan: 05
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified: []
files_created:
  - tests/reports/qoder-adapter-test-report.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "tests/reports/qoder-adapter-test-report.md exists and contains a one-screen summary with the verdict from running the mcp-adapter-test skill (Section 4 + 5 + 6 parity)"
    - "The report identifies which Capability Gate path applies to Qoder (Path A / Path B / Path C) based on whether the adapter registers the `mcp` proxy tool (Path A) vs directTools (Path B) vs neither (Path C)"
    - "Section 4 MockAgent compatibility + Section 6 E2E (all 10 servers) pass when run via the universal createMcpAdapter(QoderAdapter) entry point"
    - "Section 5 token benchmark runs (proxy tool definition tokens ≤ 300, 10-server savings ≥ 95% — same targets as Phase 5 baseline)"
    - "Section 6 E2E smoke calls all 10 servers (calculator.add, string-utils.upper, datetime.now, unit-converter.length, json-tools.parse, markdown.word_count, file-stats.line_count, http-mock.get, kv-store.set, text-analyzer.sentiment) and reports per-server pass/fail"
    - "The report documents the limitations of Phase 6's adapter vs the mcp-adapter-test skill (e.g., the skill's Capability Gate currently assumes Pi's tool list format; Phase 7 will generalize it per TEST-01..05)"
    - "npx tsc --noEmit remains clean after any report file edits"
  artifacts:
    - path: tests/reports/qoder-adapter-test-report.md
      provides: Master report for Qoder adapter test run, documenting Section 4 + 5 + 6 parity vs Pi
      contains:
        - "Section 4"
        - "Section 6"
        - "Capability Gate"
        - "Qoder"
        - "Verdict"
  key_links:
    - from: tests/reports/qoder-adapter-test-report.md
      to: skills/mcp-adapter-test/SKILL.md
      via: "references the skill's phases + pass criteria"
      pattern: "skills/mcp-adapter-test"
    - from: tests/reports/qoder-adapter-test-report.md
      to: __tests__/qoder-adapter-integration.test.ts (created in 06-04)
      via: "links the integration test results"
      pattern: "qoder-adapter-integration"
---

<objective>
Run the `skills/mcp-adapter-test` skill end-to-end against the Qoder adapter per D-10 and produce a master report at `tests/reports/qoder-adapter-test-report.md`. The report must show Section 4 (MockAgent compatibility), Section 5 (token benchmark), and Section 6 (E2E 10 demo servers) all passing for the Qoder adapter — proving "for every agent" parity with Pi.

Purpose: D-10 mandates "full mcp-adapter-test plan Section 4 + 5 + 6 parity against 10 demo servers". Without this final plan, Phase 6 ships adapters that pass unit + integration tests but are not proven to give the same end-user experience as the Pi adapter. The mcp-adapter-test skill is the project's canonical verification recipe — running it for Qoder is the empirical proof of parity.

Output:
- New `tests/reports/qoder-adapter-test-report.md` (~200 lines) with the one-screen summary + per-section detail + Capability Gate verdict
- The `QODER_INTEGRATION=1` run of the Plan 04 integration test (which fires the full 10-server smoke)

**Note on Phase 7 scope:** The mcp-adapter-test skill's Capability Gate currently detects Pi-specific tool lists (TEST-01..05 in Phase 7 will generalize it). Phase 6's report documents the current behavior — Qoder's adapter registers the `mcp` proxy tool (Path A) but the skill's gate may not detect it correctly; we report whatever verdict the skill produces, then flag the gap for Phase 7.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md

Per AGENTS.md, run `gitnexus_impact({target: "createMcpAdapter", direction: "upstream"})` before producing the report (the report references the wiring that Plan 01-04 built), and run `gitnexus_detect_changes()` before committing. Warn the user if any impact analysis returns HIGH or CRITICAL risk.

The skill's Capability Gate is currently Pi-biased (TEST-01..05 in Phase 7 will fix it). The report should clearly state whether the Qoder run hit Path A (mcp proxy detected), Path B (directTools detected), or Path C (SDK_DIRECT fallback), and flag any anomalies for Phase 7.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-second-agent-adapter/06-CONTEXT.md
@.planning/phases/06-second-agent-adapter/06-RESEARCH.md
@.planning/phases/06-second-agent-adapter/06-VALIDATION.md
@.planning/phases/07-integration-test-rebuild/07-* (planned scope — context for what Phase 7 will fix in the skill)
@skills/mcp-adapter-test/SKILL.md
@.mcp.json
@__tests__/qoder-adapter-integration.test.ts (created in 06-04)
@__tests__/qoder-adapter.test.ts (created in 06-01)
@__tests__/qoder-sampling-provider.test.ts (created in 06-02)
@adapters/qoder-adapter.ts (created in 06-01)
@adapters/qoder-sampling-provider.ts (created in 06-02)
@adapters/qoder-renderer.ts (created in 06-03)
@interfaces/agent-paths.ts (modified in 06-01)
@scripts/qoder-smoke.ts (created in 06-01)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Run the mcp-adapter-test skill end-to-end with the Qoder adapter wired in</name>
  <files>tests/reports/qoder-adapter-test-report.md</files>
  <read_first>
    @skills/mcp-adapter-test/SKILL.md (current full file — read for the exact phase commands + pass criteria)
    @__tests__/qoder-adapter-integration.test.ts (created in 06-04 — reference for the QODER_INTEGRATION=1 invocation pattern)
    @.planning/phases/06-second-agent-adapter/06-VALIDATION.md (read for the per-task verification map)
    @.planning/phases/06-second-agent-adapter/06-RESEARCH.md lines 597-624 (Validation Architecture → Phase Requirements → Test Map)
  </read_first>
  <action>
Per D-10 and VALIDATION.md "Phase gate", run the mcp-adapter-test skill end-to-end with the Qoder adapter wired in. The skill is described in `skills/mcp-adapter-test/SKILL.md` — follow its phases sequentially. The agent runs these commands (this is the manual-verify gate because it touches the agent's runtime environment).

**Steps the agent runs:**

1. **Section 4 — MockAgent compatibility**:
   ```bash
   npx vitest run tests/compatibility/ --reporter=verbose
   ```
   Pass criteria: 44/44. Record per-server results.

2. **Section 5 — Token benchmark**:
   ```bash
   npx tsx tests/token-benchmark/run-baseline.ts
   npx tsx tests/token-benchmark/run-adapter.ts
   npx tsx tests/token-benchmark/report.ts
   ```
   Pass criteria: Proxy tool definition tokens ≤ 300; 10-server combined savings ≥ 95%.

3. **Section 5B — Conversation simulation** (if implemented):
   ```bash
   npx tsx tests/token-benchmark/run-conversation-sim.ts
   ```
   Pass criteria: 4-server conversation total savings ≥ 65%; search overhead ≤ 300 tokens.

4. **Section 6 — E2E validation against 10 demo servers**:
   - Proxy mode unit tests: `npx vitest run __tests__/proxy-modes-discovery.test.ts __tests__/proxy-modes-auto-auth.test.ts __tests__/proxy-modes-ui-messages.test.ts --reporter=verbose`
   - directTools unit + integration tests: `npx vitest run __tests__/direct-tools.test.ts __tests__/direct-tools-auto-auth.test.ts; npx vitest run tests/smoke/e2e-direct-tools.test.ts --reporter=verbose`
   - Capability Gate: detect whether Qoder registers `mcp` (Path A) or directTools (Path B). Run a tiny inspection script:
     ```bash
     npx tsx scripts/qoder-smoke.ts  # also from Plan 01
     npx vitest run __tests__/qoder-adapter-integration.test.ts  # confirms `mcp` proxy tool is registered
     ```
   - E2E tests (all paths): `npx vitest run tests/smoke/e2e-all-servers.test.ts --reporter=verbose`
   - For Qoder, set `QODER_INTEGRATION=1` to enable the full 10-server smoke from Plan 04's integration test:
     ```bash
     QODER_INTEGRATION=1 npx vitest run __tests__/qoder-adapter-integration.test.ts
     ```

5. **Produce master report** at `tests/reports/qoder-adapter-test-report.md`. The report must include:
   - One-screen summary (Section 4 / Section 5 / Section 5B / Proxy / DirectTools / Section 6 / Capability Gate / Verdict).
   - Per-section detail (counts, duration, failures).
   - Pass criteria checklist (target vs actual).
   - Bugs found / notes — including any Phase 7 follow-ups (e.g., Capability Gate generalization).
   - The report should clearly distinguish Pi results (from `tests/reports/mcp-adapter-test-report.md` if it exists from Phase 4-5) vs Qoder results (from this run).

6. **Commit the report** with a clear commit message: `docs(06): add Qoder adapter test report`.

**Why this is a human-verify checkpoint:** The skill touches the agent's runtime environment (it's the project's canonical verification recipe). Some steps may have unexpected failures (e.g., a demo server's tsx invocation timing out on a specific machine). The human verifies the report is correct and decides whether to fix-and-retry or accept the partial result.

**Phase 7 follow-ups to flag:**
- TEST-01..05: generalize Capability Gate to detect any `AgentAPI` registration (not just Pi's tool list format).
- DOC-01..03: update README to position Qoder as a first-class supported agent.
- The report should list these as "Phase 7 follow-ups" rather than "Phase 6 bugs" — the Phase 6 scope is "prove parity"; Phase 7 is "make the verification skill agent-aware".

Do NOT modify any source files in this plan — the report is a deliverable, not a code change. If a Section 4 / 5 / 6 test fails, file the failure in the report and let the user decide whether to fix in this plan or defer.
  </action>
  <acceptance_criteria>
    - `tests/reports/qoder-adapter-test-report.md` exists.
    - The report contains a "One-Screen Summary" section with the verdict line (`🟢 ALL PASS` or `🟡 PARTIAL` with documented failures).
    - The report contains per-section results (Section 4, Section 5, Section 5B, Proxy unit tests, directTools tests, Section 6 E2E, Capability Gate).
    - The report identifies the Capability Gate path (Path A / B / C) and explains how Qoder's adapter maps to it.
    - The report lists Phase 7 follow-ups (TEST-01..05, DOC-01..03) as "out of scope for Phase 6".
    - `npx tsc --noEmit` passes (no source changes in this plan).
    - Human has reviewed the report and typed "approved" (or flagged specific failures to retry).
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/reports/qoder-adapter-test-report.md &amp;&amp; grep -n "Verdict" tests/reports/qoder-adapter-test-report.md &amp;&amp; grep -n "Capability Gate" tests/reports/qoder-adapter-test-report.md &amp;&amp; grep -n "Section 6" tests/reports/qoder-adapter-test-report.md &amp;&amp; grep -n "Phase 7" tests/reports/qoder-adapter-test-report.md &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>tests/reports/qoder-adapter-test-report.md contains a one-screen summary + per-section detail + Capability Gate verdict + Phase 7 follow-ups; Section 4 + 5 + 6 parity verified for Qoder; D-10 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-adapter-test skill → 10 demo MCP servers | Skill spawns 10 stdio transports; same trust as Phase 5 |
| Skill → .mcp.json | Skill reads project config; trusted |
| Report → public repo | Report may surface internal findings; review before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-VT-01 | Tampering | report content | mitigate | The report is a markdown file in `tests/reports/` (gitignored pattern matches `tests/reports/*` if the gitignore is configured) or git-tracked if not. Either way, the human-review checkpoint prevents inaccurate claims from landing in main. |
| T-06-VT-02 | Information Disclosure | demo server output in report | mitigate | Report only includes pass/fail counts and per-server tool discovery results, not full stdout/stderr dumps. If a specific failure needs verbose output, link to the test artifact (e.g., `tests/reports/qoder-e2e-calculator.log`) rather than embedding. |
| T-06-VT-03 | Denial of Service | Section 6 E2E run time | accept | The full E2E run (10 servers) takes ~30-60s; gated behind `QODER_INTEGRATION=1` if needed. |
| T-06-VT-SC | Tampering | npm installs | mitigate | No new installs in this plan. |
</threat_model>

<verification>
- `tests/reports/qoder-adapter-test-report.md` exists and is non-empty.
- Report contains "Verdict" line.
- Report contains "Capability Gate" section.
- Report contains "Section 6" results.
- `npx tsc --noEmit` passes (no source changes).
- Human has reviewed the report and approved.
</verification>

<must_haves>
## Must-Haves

### Observable Truths
- tests/reports/qoder-adapter-test-report.md exists.
- The report contains a one-screen summary with the verdict from running the mcp-adapter-test skill.
- Section 4 + Section 5 + Section 6 results are all documented (parity vs Pi baseline).
- The Capability Gate verdict identifies the path Qoder's adapter takes.
- Phase 7 follow-ups are listed as out-of-scope for Phase 6.
- Human review of the report is recorded (approved comment in the checkpoint task).

### Required Artifacts
- `tests/reports/qoder-adapter-test-report.md` — master report documenting Section 4 + 5 + 6 parity for Qoder.
</must_haves>

<success_criteria>
- tests/reports/qoder-adapter-test-report.md exists with the one-screen summary + per-section detail.
- Section 4 MockAgent + Section 6 E2E results documented (10 servers).
- Capability Gate verdict identified.
- Phase 7 follow-ups flagged.
- Human has approved the report.
- npx tsc --noEmit passes.
- D-10 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-second-agent-adapter/06-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `tests/reports/qoder-adapter-test-report.md`: master report documenting Section 4 + 5 + 6 parity for Qoder, Capability Gate verdict, and Phase 7 follow-ups.
