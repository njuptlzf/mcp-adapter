---
phase: 08-upstream-merge-conflict-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UPSTREAM-CHANGES.md
  - .planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md
autonomous: true
requirements:
  - UPSTREAM-01
  - UPSTREAM-04
user_setup: []

must_haves:
  truths:
    - "`UPSTREAM-CHANGES.md` exists at the repo root with a 5-column diverged-file manifest (Path / Status / Category / Default Resolution / Rationale)"
    - "Manifest covers all files diverged between this fork and `upstream/main` (filtered to `.ts`/`.md`/`.json`; vendor + generated artefacts excluded)"
    - "Each manifest row references the relevant D-XX / DECOUPLE-XX decision so an agent can locate the rationale without re-running grep"
    - "Three known Pi-coupling residual rows (`mcp-panel.ts`, `mcp-setup-panel.ts`, `index.ts`) are explicitly tagged as 'follow-up needed' or 'backward-compat (D-04/ENTRY-02/05-05)'"
    - "Manifest mentions D-21 in the `skills/*` category rows to anchor Phase 7's per-agent references compatibility (UPSTREAM-04)"
    - "Static alignment check: `grep -cE '^\\| \\`' UPSTREAM-CHANGES.md` ≥ 60 AND differs from `git diff upstream/main --name-status | wc -l` by ≤ 10 (vendor filter)"
  artifacts:
    - path: "UPSTREAM-CHANGES.md"
      provides: "5-column fork-vs-upstream divergence manifest"
      contains: "## Divergence manifest"
      min_lines: 60
    - path: "UPSTREAM-CHANGES.md"
      provides: "Decision Anchors section linking D-07/D-21/Phase 5 DECOUPLE-XX"
      contains: "## Decision anchors"
    - path: "UPSTREAM-CHANGES.md"
      provides: "skills/mcp-adapter-test/SKILL.md row citing D-21"
      contains: "D-21"
    - path: ".planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md"
      provides: "Executor summary capturing commands run + divergence count"
      contains: "## Summary"
  key_links:
    - from: "UPSTREAM-CHANGES.md"
      to: ".planning/STATE.md"
      via: "D-XX citation in Rationale column"
      pattern: "D-(07|21|04|01|14|15|17|18|19|20)"
    - from: "UPSTREAM-CHANGES.md"
      to: ".planning/phases/05-type-decoupling-entry-point-refactor/*-SUMMARY.md"
      via: "DECOUPLE-XX citation"
      pattern: "DECOUPLE-(01|02|03|04|05|06|07)"
    - from: "08-01 executor verification"
      to: "git diff upstream/main --name-status"
      via: "static alignment cross-check"
      pattern: "git diff upstream/main --name-status"

---

<objective>
Author the `UPSTREAM-CHANGES.md` manifest at the repository root that captures every file diverged between this fork (`njuptlzf/mcp-adapter`) and the upstream `nicobailon/pi-mcp-adapter`. Each row carries a per-file rationale so that the Phase 8.02 skill (and any future agent invoking it) can make a fast path decision without re-running grep or re-deriving category semantics.

Purpose: Satisfies UPSTREAM-01 (manifest exists with per-file rationale) and UPSTREAM-04 (manifest references the adapter/wrapper patterns established in Phase 5-7, anchoring D-21's merge-friendly structure). This is the data foundation the SKILL.md decision tree reads from in Plan 08-02.

Output:
1. `UPSTREAM-CHANGES.md` — a ~150-200 line document with: Decision Anchors section, the 5-column divergence manifest (~60+ rows), and a Generation notes footer.
2. `.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md` — executor summary recording `git fetch` outcome, raw divergence count, and the static alignment command output.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-VALIDATION.md
@.planning/phases/07-integration-test-rebuild/07-LEARNINGS.md

# Project root for initial-fill command references
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initial-fill raw divergence + categorised TSV</name>
  <files>UPSTREAM-CHANGES.md (raw data source only; manifest written in Task 2)</files>
  <read_first>
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` (Dimension 1: `Initial-fill 命令 (verbatim 可拷贝)` block + Dimension 8: 实测 207 个 diverged 文件统计)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"UPSTREAM-01-C (生成机制)" + §"UPSTREAM-01-D (维护机制)"
    - `.planning/STATE.md` §"Decisions & Preferences" (D-07 AGENT_ADAPTERS / D-21 SKILL.md parametric / D-04 ENTRY-02 backward compat)
  </read_first>
  <action>
    Run the initial-fill pipeline (per RESEARCH Dimension 1, with the GnuTLS workaround per Dimension 8 §"Network 依赖") to materialise a categorised TSV at `/tmp/upstream-divergence.tsv`. The pipeline is:

    Step 1 — assert `upstream` remote exists:
    - `git remote get-url upstream` must succeed; if it fails, the executor MUST abort with the literal error message "ERROR: upstream remote not configured. Run: git remote add upstream https://github.com/nicobailon/pi-mcp-adapter.git".

    Step 2 — fetch upstream tags + commits using the GnuTLS-safe incantation:
    - `GIT_SSL_NO_VERIFY=1 git -c http.sslVerify=false fetch upstream --tags --unshallow 2>&1 | tail -20`
    - If the fetch still fails (offline / no credentials), the executor MUST record the error verbatim in the 08-01-SUMMARY.md "Issues Encountered" section, then fall back to `git rev-parse upstream/main`; if `upstream/main` is missing locally either, abort with "ERROR: cannot run initial-fill without upstream/main ref — re-run with network or set GIT_SSL_NO_VERIFY=1".

    Step 3 — write the raw `git diff upstream/main --name-status -- '*.ts' '*.md' '*.json'` output to `/tmp/upstream-divergence-raw.txt` and capture `wc -l` (this is the "raw divergence count" reported in the SUMMARY).

    Step 4 — feed the raw output through the awk classifier from RESEARCH Dimension 1 (the awk script that maps `A/M/D` to `Status`, classifies `Category` via 10 path regexes matching the 12-category schema, and emits a tab-separated `Status\tCategory\tPath` stream). Write the result to `/tmp/upstream-divergence.tsv` after sorting by `Category` then `Path` (DISCRETION-B). This is the data Task 2 turns into the 5-column manifest table.

    Step 5 — apply the vendor filter inline in awk (drop paths matching `node_modules/`, `dist/`, `coverage/`, `tests/reports/`, `app-bridge.bundle.js`, `examples/*/dist/`) before writing the TSV; record the pre-filter and post-filter counts in the SUMMARY. Use the verbatim 10-regex classifier from RESEARCH Dimension 1 (the regexes for `.planning/`, `skills/`, `adapters/`, `interfaces/`, `__tests__|tests|examples/`, `^(README|MAPPING|CHANGELOG|OAUTH)\.md$`, `^(package\.json|...)`, `^(AGENTS|CLAUDE)\.md$`, `\.claude/`, top-level `*.ts`, `types/`, fallback `other`).

    The executor MUST NOT inline-edit or hand-categorise the TSV in this task — Task 2 reads `/tmp/upstream-divergence.tsv` and authors the markdown.
  </action>
  <verify>
    <automated>test -s /tmp/upstream-divergence.tsv && awk -F'\t' '{print $2}' /tmp/upstream-divergence.tsv | sort -u | wc -l</automated>
    <automated_returns>≥ 6 (must contain at least the 6 main categories: adapter, interface, source, planning, skill, test, docs, config, agents_meta)</automated_returns>
  </verify>
  <acceptance_criteria>
    - `git remote get-url upstream` exits 0 (or the executor aborted with the documented error and wrote the abort reason to the SUMMARY)
    - `tail -20` of the fetch output is captured in 08-01-SUMMARY.md §"Commands run"
    - `/tmp/upstream-divergence-raw.txt` exists and `wc -l /tmp/upstream-divergence-raw.txt` matches the value logged in SUMMARY §"Raw divergence"
    - `/tmp/upstream-divergence.tsv` exists, has a tab-separated 3-column header (or awk-classified rows), and `sort -u` of column 2 yields ≥ 6 distinct categories
    - Pre-filter count > post-filter count (proves the vendor filter ran); both numbers recorded in SUMMARY
  </acceptance_criteria>
  <done>
    `/tmp/upstream-divergence.tsv` is populated, sorted, and contains every diverged `.ts`/`.md`/`.json` file (vendor + generated artefacts filtered) classified into the 12-category schema. The 08-01-SUMMARY.md §"Commands run" section records the fetch command, the raw diff line count, the post-filter row count, and any GnuTLS / network fallback used.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author UPSTREAM-CHANGES.md manifest (5-column table + Decision Anchors)</name>
  <files>UPSTREAM-CHANGES.md</files>
  <read_first>
    - `/tmp/upstream-divergence.tsv` (output of Task 1)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"UPSTREAM-01-B (5 列表格 schema)" + §"UPSTREAM-01-C" + §"UPSTREAM-01-D" + §"the agent's Discretion" §"DISCRETION-B"
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` Dimension 1 §"5 列表格 schema 实现建议" + §"Manifest 真实规模" + §"链接到 Phase 4-7 已实施的拆分 commit" + Dimension 4 §"哪些 core MCP 文件最容易引入 Pi-coupling" + Dimension 7 §"3 个真实 Pi-coupling 残留点"
    - `.planning/STATE.md` §"Decisions & Preferences" (full D-01..D-21 list)
    - `.planning/phases/07-integration-test-rebuild/07-LEARNINGS.md` §"D-21" (per-agent references compatibility)
  </read_first>
  <action>
    Create `UPSTREAM-CHANGES.md` at the repository root with the following structure (in this exact order):

    **Header section** (verbatim):
    - H1 title: `# Upstream Changes Manifest`
    - H2 subtitle: `Tracks every file diverged from upstream (nicobailon/pi-mcp-adapter) to njuptlzf/mcp-adapter. Read this file first when running \`skills/upstream-merge/SKILL.md\`.`
    - Metadata block (3 lines, exactly): `Generated at: 2026-06-17 for upstream/main @ <upstream-sha-short>` (executor fills in the short SHA from `git rev-parse --short upstream/main`); `Source of truth: \`git diff upstream/main --name-status -- '*.ts' '*.md' '*.json'\``; `Refresh policy: regenerate via PR when upstream ships a major release (per UPSTREAM-01-D, no CI hook)`.

    **Section `## Decision anchors`** — 6-9 bulleted lines that link this fork's per-file decisions to prior phases:
    - D-07 → `AGENT_ADAPTERS` static registry in `interfaces/agent-api.ts` (Phase 7) — source of truth for which adapters we own
    - D-21 → Phase 7 `skills/mcp-adapter-test/SKILL.md` parametric + per-agent references (main file untouched on adapter additions)
    - D-04 / ENTRY-02 / 05-05 → `index.ts` keeps `ExtensionAPI` import for Pi backward-compat wrapper (legal Pi-coupling, never re-introduce upstream diff into `mcpAdapter(pi)` signature)
    - DECOUPLE-01 (Phase 5-01) → `proxy-modes.ts` + `direct-tools.ts` already use generic `McpToolResult`
    - DECOUPLE-02 (Phase 5-04) → `sampling-handler.ts` + `elicitation-handler.ts` already use generic `UISystem`
    - DECOUPLE-06 (Phase 5-04) → `tool-result-renderer.ts` already uses generic `RenderOutput`; **DECOUPLE-06 does NOT cover `mcp-panel.ts` / `mcp-setup-panel.ts`** — those still import `@earendil-works/pi-tui` and are flagged as follow-up needed in the manifest body
    - D-01..D-03 → Capability Gate reads `interfaces/agent-api.ts` `envHints` (includes `PI_CODING_AGENT_DIR`) — legal Pi reference for detection, not for new logic
    - D-17 (Phase 7) → Matrix Reporter is fork-only artefact; never sync from upstream

    **Section `## Divergence manifest`** — the 5-column table per UPSTREAM-01-B. Author the table by reading `/tmp/upstream-divergence.tsv` and translating each row into a markdown row. Column semantics (exact):
    - `Path`: repo-relative path with backticks (e.g. `` `adapters/pi-adapter.ts` ``)
    - `Status`: one of `new` / `modified` / `deleted` (map from `A` / `M` / `D` in the raw diff)
    - `Category`: one of `adapter` / `interface` / `skill` / `test` / `docs` / `config` / `source` / `planning` / `agents_meta` / `other`
    - `Default Resolution`: one of `ours` / `theirs` / `assess` / `manual` per CONTEXT UPSTREAM-03-A 12-category table (or `assess` for source files per Dimension 4)
    - `Rationale`: 1-2 lines linking to D-XX / DECOUPLE-XX / Phase-X plan or noting the legacy/TODO reason

    Apply the 12-category default-resolution table (verbatim from CONTEXT §"UPSTREAM-03-A") when filling the `Default Resolution` column. For each row, the Rationale must:
    - Cite D-07 for any `adapters/pi-adapter.ts` / `adapters/qoder-adapter.ts` row
    - Cite D-21 in the `skills/mcp-adapter-test/SKILL.md` row's Rationale (1-2 lines: "per-agent reference 复制 `_template.md`;主文件不动 = UPSTREAM-04 compatible")
    - Cite D-04/ENTRY-02/05-05 for `index.ts` row with Rationale "backward-compat wrapper — accept theirs only if `mcpAdapter(pi)` signature unchanged"
    - Cite DECOUPLE-06 in `tool-result-renderer.ts` row's Rationale
    - Tag `mcp-panel.ts` and `mcp-setup-panel.ts` with Rationale "follow-up needed — DECOUPLE-06 did NOT cover this file; still imports @earendil-works/pi-tui"
    - Tag `panel-keys.ts` (Status=deleted) with Rationale "deleted in upstream, kept in fork (deprecated; do NOT `git rm` — see UPSTREAM-03 4-step rule for deleted files)"
    - Tag `interfaces/agent-api.ts` `envHints` (Status=modified) with Rationale "Capability Gate detection (D-01..D-03) — legal Pi-coupling, not follow-up"
    - Sort rows: by `Category` (alphabetical) then `Path` (alphabetical) per DISCRETION-B. The final table must have ≥ 60 data rows.

    **Section `## Special-cases footnotes`** — 4 bulleted lines anchored to the 3 known Pi-coupling residuals (RESEARCH Dimension 7):
    - `mcp-panel.ts` + `mcp-setup-panel.ts` (both Status=modified): "DECOUPLE-06 only covered `tool-result-renderer.ts`; these panel files still import `@earendil-works/pi-tui` (`matchesKey`, `truncateToWidth`, `visibleWidth`). Future Phase 9 follow-up should decouple them behind the generic `RenderOutput` interface."
    - `index.ts` (Status=modified): "Legal Pi-coupling — D-04/ENTRY-02/05-05 backward-compat wrapper. Any upstream diff that re-shapes the public `mcpAdapter(pi: ExtensionAPI)` signature requires manual review."
    - `interfaces/agent-api.ts:182-206` (Status=modified): "Legal Pi-coupling — JSDoc and `envHints` mention `ExtensionAPI` / `PI_CODING_AGENT_DIR` for Capability Gate (D-01..D-03). Do NOT treat as follow-up."

    **Section `## Generation notes`** — 2 lines:
    - "Generated: 2026-06-17 (one-shot baseline; future refresh via PR re-running `git diff upstream/main --name-status`)."
    - "Total diverged files: <raw count from Task 1>; manifest rows: <count> (post-vendor-filter)."

    The executor MUST use the Write tool to create the file. The manifest is a single Write call (the executor should chunk only if the tool's truncation fallback is triggered).
  </action>
  <verify>
    <automated>test -f UPSTREAM-CHANGES.md && [ "$(grep -cE '^\| \`' UPSTREAM-CHANGES.md)" -ge 60 ] && [ "$(grep -c 'D-21' UPSTREAM-CHANGES.md)" -ge 1 ] && [ "$(grep -cE 'DECOUPLE-(01|02|06)' UPSTREAM-CHANGES.md)" -ge 3 ]</automated>
    <automated_returns>all four conditions true (file exists, ≥60 table rows, D-21 cited ≥1, DECOUPLE cited ≥3)</automated_returns>
  </verify>
  <acceptance_criteria>
    - `test -f UPSTREAM-CHANGES.md` exits 0
    - `grep -cE '^\| \`' UPSTREAM-CHANGES.md` ≥ 60 (5-column table rows)
    - `grep -c 'D-21' UPSTREAM-CHANGES.md` ≥ 1 (UPSTREAM-04 anchor present)
    - `grep -cE 'DECOUPLE-(01|02|06)' UPSTREAM-CHANGES.md` ≥ 3 (Phase 5 decoupling refs present)
    - `grep -c '^## ' UPSTREAM-CHANGES.md` ≥ 5 (Decision anchors / Divergence manifest / Special-cases footnotes / Generation notes + the 2 preamble headings)
    - `mcp-panel.ts` and `mcp-setup-panel.ts` rows both contain "follow-up needed" Rationale
    - `panel-keys.ts` row exists with "deprecated" or "do NOT `git rm`" Rationale
    - `index.ts` row Rationale contains "backward-compat" or "D-04"
    - `skills/mcp-adapter-test/SKILL.md` row exists with D-21 Rationale
  </acceptance_criteria>
  <done>
    `UPSTREAM-CHANGES.md` exists at repo root with ≥ 60 sorted 5-column rows, 4 anchor sections (Decision anchors / Divergence manifest / Special-cases footnotes / Generation notes), all required D-21 + DECOUPLE citations, and the 3 known Pi-coupling residuals explicitly tagged. The file is ≤ 250 lines total (readable as one screenful when `cat | less`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Static alignment check + commit</name>
  <files>.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md (created)</files>
  <read_first>
    - `UPSTREAM-CHANGES.md` (output of Task 2)
    - `/tmp/upstream-divergence-raw.txt` + `/tmp/upstream-divergence.tsv` (Task 1 outputs)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-VALIDATION.md` §"Cross-check Validation" + §"Phase Requirements → Validation Method (1:1)"
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` Dimension 8 §"Initial-fill 工作量预估"
  </read_first>
  <action>
    Perform a static alignment check between the manifest and the live `git diff upstream/main --name-status` output, then commit.

    **Step 1 — Static alignment** (per VALIDATION.md §"Cross-check Validation" + RESEARCH Dimension 8):
    - Run: `RAW=$(wc -l < /tmp/upstream-divergence-raw.txt)`; `MANIFEST=$(grep -cE '^\| \`' UPSTREAM-CHANGES.md)`; `DIFF=$((RAW - MANIFEST))`
    - Acceptable if `|DIFF| ≤ 10` (vendor/生成文件过滤). If `|DIFF| > 10`, the executor MUST add a 5th section `## Alignment exceptions` to `UPSTREAM-CHANGES.md` listing each missing/extra path and the rationale (e.g. "path matches `node_modules/` filter; excluded").
    - Run: `test -f UPSTREAM-CHANGES.md && grep -c 'D-21' UPSTREAM-CHANGES.md` to confirm UPSTREAM-04 anchor; if 0, append a 1-line `skills/mcp-adapter-test/SKILL.md` row to the manifest immediately.

    **Step 2 — Refresh validation commands** (per VALIDATION.md §"Phase Requirements → Validation Method"):
    - `test -f UPSTREAM-CHANGES.md && echo "UPSTREAM-01 file-existence: PASS"`
    - `[ "$(grep -cE '^\| \`' UPSTREAM-CHANGES.md)" -ge 60 ] && echo "UPSTREAM-01 content count: PASS"`
    - `[ "$(grep -c 'D-21' UPSTREAM-CHANGES.md)" -ge 1 ] && echo "UPSTREAM-04 anchor: PASS"`
    - Record all three outputs in the SUMMARY §"Acceptance evidence".

    **Step 3 — Write the SUMMARY** at `.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md` with these sections (in order): `## Summary`, `## Commands run` (verbatim shell transcript of fetch + diff + grep, ≤ 40 lines), `## Files changed` (bullet list), `## Acceptance evidence` (3 lines of PASS), `## Alignment exceptions` (only if `|DIFF| > 10`), `## Issues encountered` (GnuTLS / network / awk errors if any), `## Next steps` (1-2 lines: "Plan 08-02 may now build SKILL.md against this manifest").

    **Step 4 — Commit** (per workflow security_enforcement: no npm install in this plan, so no slopcheck gate needed):
    - `git add UPSTREAM-CHANGES.md .planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md`
    - `git commit -m "docs(08): add UPSTREAM-CHANGES.md manifest (UPSTREAM-01, UPSTREAM-04)"`
    - The commit MUST NOT include `/tmp/upstream-divergence-*.txt` (those files are in `/tmp`, outside the repo).
    - Do NOT push; do NOT create a PR (this is a phase-internal commit, not a user-facing PR).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md && grep -c '^## Summary' .planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md && git log -1 --pretty=format:'%s' | grep -E 'docs\(08\): add UPSTREAM-CHANGES'</automated>
    <automated_returns>all three true</automated_returns>
  </verify>
  <acceptance_criteria>
    - `08-01-SUMMARY.md` exists with all required sections (`## Summary`, `## Commands run`, `## Files changed`, `## Acceptance evidence`)
    - `## Acceptance evidence` contains the 3 PASS lines for UPSTREAM-01 file-existence, UPSTREAM-01 content count, UPSTREAM-04 anchor
    - `git log -1 --pretty=format:'%s'` shows the commit message starting with `docs(08): add UPSTREAM-CHANGES`
    - `git status` is clean (no uncommitted changes) after the commit
    - Static alignment `|RAW - MANIFEST| ≤ 10` OR `## Alignment exceptions` section explains the gap
  </acceptance_criteria>
  <done>
    Static alignment is verified (manifest matches live divergence within tolerance), the 3 VALIDATION.md acceptance commands all PASS, the SUMMARY captures every command output for `/gsd-verify-work 08`, and a single conventional `docs(08):` commit lands the manifest at the repo root. The repo is ready for Plan 08-02 to author SKILL.md against this manifest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted external: `upstream` git remote | Pulled tags + commits; the initial-fill command fetches untrusted upstream history. The executor MUST NOT execute any code from upstream in this plan (read-only `git diff` is safe; `git checkout upstream/main -- <file>` is deferred to Plan 08-02 dry-run worktrees). |
| Trusted: this fork's working tree | `UPSTREAM-CHANGES.md` is created locally; no external write to it. |
| Untrusted: `/tmp` filesystem | `/tmp/upstream-divergence-*.txt` files are intermediate artefacts; the manifest does not reference them. |
| Trusted: `git history` of `main` branch | The final commit only touches the manifest + SUMMARY; no other tracked file changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-PI | Information Disclosure / Tampering | `git fetch upstream` | mitigate | Use GnuTLS workaround (`GIT_SSL_NO_VERIFY=1 git -c http.sslVerify=false fetch upstream`) when SSL handshake fails. Do NOT execute or `git checkout` any upstream blob in this plan (deferred to Plan 08-02 worktree). |
| T-08-01-RC | Repudiation | Manifest content vs raw diff | mitigate | Static alignment check (Task 3 Step 1) compares `grep -cE '^\| \`'` against `wc -l /tmp/upstream-divergence-raw.txt`; gap > 10 triggers an "Alignment exceptions" section naming each missing path. |
| T-08-01-IE | Information Exposure | Path enumeration in manifest | accept | Manifest only lists repo-relative paths already in the public repo. No secrets, no credentials, no `.env*` files. |
| T-08-01-DI | Denial of Integrity | Manifest silently drops files | mitigate | Task 1 writes raw diff to `/tmp/upstream-divergence-raw.txt` so the SUMMARY can independently re-verify count. Task 3 Step 1 reconciliation catches drift. |
| T-08-01-SC | Tampering | npm install / pip install | N/A | This plan does NOT install any package (no `npm install`, no `pip install`, no `cargo add`). Threat gate is moot. |
| T-08-01-CW | Content Wrongness (Pi-coupling re-introduction via inaccurate rationale) | `Rationale` column | mitigate | Executor must cite the actual D-XX / DECOUPLE-XX / Phase-X plan for every adapter / interface / source row. Cross-check: `grep -cE 'DECOUPLE-(01|02|06)' UPSTREAM-CHANGES.md` ≥ 3 in Task 2 verify. |

## OWASP A01-A10 Disposition

| OWASP ID | Category | Disposition | Rationale |
|----------|----------|-------------|-----------|
| A01 (Broken Access Control) | AuthZ | N/A | No endpoints, no auth, no user input. |
| A02 (Cryptographic Failures) | Crypto | N/A | No secrets, no TLS termination in this plan. (GnuTLS workaround disables verification of upstream GitHub TLS — acceptable here because we only read public diffs.) |
| A03 (Injection) | Input | N/A | Manifest content is static markdown. |
| A04 (Insecure Design) | Design | N/A | Manifest is a documentation artefact, not an executable component. |
| A05 (Security Misconfiguration) | Config | N/A | No config changes. |
| A06 (Vulnerable Components) | Deps | N/A | No package installs. |
| A07 (Identification & Auth Failures) | AuthN | N/A | No authentication surface. |
| A08 (Software & Data Integrity) | Integrity | mitigated | See T-08-01-RC + T-08-01-DI. Manifest vs raw diff cross-check guards against silent drops. |
| A09 (Logging & Monitoring) | Ops | N/A | No runtime; dry-run logging deferred to Plan 08-02. |
| A10 (SSRF) | Network | N/A | No outbound requests beyond `git fetch upstream` (already documented as GnuTLS workaround). |
</threat_model>

<verification>
## Phase checks (UPSTREAM-01 + UPSTREAM-04)

- `test -f UPSTREAM-CHANGES.md` exits 0
- `grep -cE '^\| \`' UPSTREAM-CHANGES.md` ≥ 60
- `grep -c 'D-21' UPSTREAM-CHANGES.md` ≥ 1
- `grep -cE 'DECOUPLE-(01|02|06)' UPSTREAM-CHANGES.md` ≥ 3
- Static alignment: `|wc -l /tmp/upstream-divergence-raw.txt - grep -cE '^\| \`' UPSTREAM-CHANGES.md| ≤ 10` OR documented exceptions
- `git log -1 --pretty=format:'%s'` shows `docs(08): add UPSTREAM-CHANGES.md manifest (UPSTREAM-01, UPSTREAM-04)`
- `git status` is clean post-commit
- `/gsd-verify-work 08` can read `08-01-SUMMARY.md` and re-run the 3 acceptance commands
</verification>

<success_criteria>
Plan 08-01 succeeds when:

1. `UPSTREAM-CHANGES.md` exists at the repo root, contains the 4 required sections (Decision anchors / Divergence manifest / Special-cases footnotes / Generation notes), has ≥ 60 5-column table rows, cites D-21 + at least 3 DECOUPLE-XX references, and explicitly tags the 3 known Pi-coupling residuals.
2. The static alignment check (raw diff vs manifest) passes within tolerance (≤ 10 row gap with documented exceptions).
3. The executor wrote `08-01-SUMMARY.md` with verbatim command transcripts and the 3 PASS evidence lines for `UPSTREAM-01` file-existence, `UPSTREAM-01` content count, and `UPSTREAM-04` anchor.
4. A single conventional `docs(08):` commit lands in the local repo (not pushed).
5. `/tmp/upstream-divergence-raw.txt` and `/tmp/upstream-divergence.tsv` exist as intermediate artefacts for Plan 08-02's cross-validation.

After Plan 08-01 lands, Plan 08-02 may begin authoring `skills/upstream-merge/SKILL.md` against the manifest data without re-running `git fetch upstream` (the ref is already local).
</success_criteria>

<output>
Create `.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md` when done.

The 08-01-SUMMARY.md is mandatory: it is the only artefact that records the fetch outcome, the raw divergence count, the alignment reconciliation, and the 3 PASS evidence lines. Plan 08-02 reads it before authoring SKILL.md, and `/gsd-verify-work 08` reads it to confirm UPSTREAM-01 + UPSTREAM-04.
</output>
