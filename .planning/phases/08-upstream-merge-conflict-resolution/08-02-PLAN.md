---
phase: 08-upstream-merge-conflict-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - skills/upstream-merge/SKILL.md
  - skills/upstream-merge/references/pi-coupling-markers.md
  - .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md
  - .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md
  - .planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md
  - .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md
autonomous: false
requirements:
  - UPSTREAM-02
  - UPSTREAM-03
user_setup: []

must_haves:
  truths:
    - "`skills/upstream-merge/SKILL.md` exists and contains exactly 4 top-level numbered sections (`## 1. When to invoke` / `## 2. Read UPSTREAM-CHANGES.md first` / `## 3. Decision tree` / `## 4. Checklist`)"
    - "SKILL.md embeds the RESEARCH Dimension 3 corrected grep template (with `\\b` word boundaries; the 8 `pi.X` patterns from CONTEXT are NOT present)"
    - "SKILL.md's 6-item checklist is machine-checkable: each item names a concrete command (`git diff --name-only --diff-filter=U`, `npx tsc --noEmit`, `npm test`, etc.)"
    - "Dry-run scenario 1 (OAuth in `init.ts`, Pi-coupling-free) is executed in an isolated worktree at `/tmp/dryrun-oauth-init` and produces `dry-run-scenario-1-oauth-init.md` showing 0 marker hits + `--theirs` decision + commit-message template"
    - "Dry-run scenario 2 (`mcp-toggle` in `commands.ts` with Pi import) is executed in an isolated worktree at `/tmp/dryrun-mcp-toggle` and produces `dry-run-scenario-2-mcp-toggle-commands.md` showing ≥1 marker hit + `--theirs` + follow-up issue creation + follow-up commit + commit-message template"
    - "Both dry-run logs cite the exact manifest row from `UPSTREAM-CHANGES.md` (file path + Default Resolution) that drove the decision"
    - "A `deferred-items.md` file exists at the phase root listing the 4 CONTEXT-deferred ideas (CI hook / auto-resolve bot / reverse-contribute / refresh schedule) for future phases"
  artifacts:
    - path: "skills/upstream-merge/SKILL.md"
      provides: "4-section agent skill for fork-maintainer merge workflow"
      contains: "## 1. When to invoke"
      min_lines: 120
    - path: "skills/upstream-merge/SKILL.md"
      provides: "RESEARCH Dimension 3 corrected grep template with \\b word boundaries"
      contains: "\\bExtensionAPI\\b"
    - path: "skills/upstream-merge/SKILL.md"
      provides: "6-item machine-checkable checklist"
      contains: "## 4. Checklist"
    - path: "skills/upstream-merge/references/pi-coupling-markers.md"
      provides: "Per-marker inventory + precision ranking (HIGH/MEDIUM/LOW) + import-path filter rules"
      contains: "## Pi-coupling markers"
    - path: ".planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md"
      provides: "Scenario 1 resolution log (Pi-coupling-free, accept --theirs)"
      contains: "## Scenario 1"
    - path: ".planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md"
      provides: "Scenario 2 resolution log (Pi-coupling re-introduction, follow-up flow)"
      contains: "## Scenario 2"
    - path: ".planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md"
      provides: "Phase 8 deferred ideas list (CI hook / auto-resolve bot / reverse-contribute / refresh schedule)"
      contains: "CI hook"
    - path: ".planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md"
      provides: "Executor summary recording SKILL.md section count, dry-run outcomes, and checklist verification"
      contains: "## Summary"
  key_links:
    - from: "skills/upstream-merge/SKILL.md"
      to: "UPSTREAM-CHANGES.md"
      via: "Section 2 (Read UPSTREAM-CHANGES.md first) cites the manifest path"
      pattern: "UPSTREAM-CHANGES\\.md"
    - from: "skills/upstream-merge/SKILL.md"
      to: "skills/upstream-merge/references/pi-coupling-markers.md"
      via: "Section 3 (Decision tree) links to the marker inventory"
      pattern: "pi-coupling-markers\\.md"
    - from: "dry-run-scenario-2-mcp-toggle-commands.md"
      to: "skills/upstream-merge/SKILL.md"
      via: "Resolution log cites the SKILL.md §3 follow-up branch + §3.1 grep"
      pattern: "skills/upstream-merge/SKILL\\.md"
    - from: "08-02-SUMMARY.md"
      to: "skills/upstream-merge/SKILL.md"
      via: "Acceptance evidence verifies 4 sections + 6-item checklist + corrected grep"
      pattern: "grep -cE \"^## \\[1-4\\]\\.\""

---

<objective>
Author `skills/upstream-merge/SKILL.md` (a 4-section agent skill for fork-maintainer conflict resolution) and prove it works by running 2 dry-run scenarios in isolated worktrees, then committing the skill, the per-scenario resolution logs, and a deferred-items list.

Purpose: Satisfies UPSTREAM-02 (SKILL.md exists with 4 sections + decision tree + checklist) and UPSTREAM-03 (the rules: 12-category default decisions, the 7 HIGH-precision + 1 MEDIUM Pi-coupling markers with `\b` word boundaries, and the 5-step follow-up issue flow for Pi-coupling re-introduction). The dry-run scenarios (per CONTEXT VERIFY-A + VERIFY-B) are the live evidence the skill can actually drive a conflict to commit, and they catch any false-positive / false-negative in the grep template before future agents use it for real.

Output:
1. `skills/upstream-merge/SKILL.md` (~140-160 lines, 4 numbered sections, embeds the corrected grep template).
2. `skills/upstream-merge/references/pi-coupling-markers.md` (per-marker inventory + import-path filter for `ToolInfo`).
3. `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` (Pi-coupling-free → `--theirs`).
4. `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md` (Pi-coupling re-intro → `--theirs` + follow-up issue + follow-up commit).
5. `.planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md` (Phase 8 deferred ideas: CI hook, auto-resolve bot, reverse-contribute, refresh schedule).
6. `.planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md` (executor summary).
7. The plan is **non-autonomous**: Task 4 is a `checkpoint:human-verify` for the user to walk through both dry-run logs and confirm they look right.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-VALIDATION.md
@.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md

# The manifest Plan 08-01 produced (read for Default Resolution column)
@UPSTREAM-CHANGES.md

# Existing skill structure (mirror the YAML frontmatter + 4-section pattern)
@skills/mcp-adapter-test/SKILL.md

# Project root for git remote + worktree + commit commands
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SKILL.md (4 sections) + pi-coupling-markers.md reference</name>
  <files>skills/upstream-merge/SKILL.md, skills/upstream-merge/references/pi-coupling-markers.md</files>
  <read_first>
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"UPSTREAM-02-A (结构)" + §"UPSTREAM-02-B (Pi-coupling marker grep 模板 — skill 内嵌)" + §"UPSTREAM-03-A" + §"UPSTREAM-03-B" + §"UPSTREAM-03-C"
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` Dimension 2 §"4 Section 字数预算" + §"Decision tree markdown 表达" + §"Grep 模板 (修正版 — 解决 Dimension 3 的 false positive 问题)" + §"Checklist 6 项 machine-checkable vs human-checkable" + Dimension 3 §"Grep 模板最终版(可嵌入 SKILL.md)" + §"关键发现 #1" + §"关键发现 #2" + Dimension 7 §"结论: 13 markers 列表可精简" + §"Manifest 静态对齐的 2 个核心 marker 行"
    - `UPSTREAM-CHANGES.md` (output of Plan 08-01 — every rationale citation in the decision tree branches must match a row in this manifest)
    - `skills/mcp-adapter-test/SKILL.md` (mirror the YAML frontmatter shape: `name`, `description`, trigger phrases)
  </read_first>
  <action>
    Create two new files in the `skills/upstream-merge/` directory (create the directory first via `mkdir -p skills/upstream-merge/references`).

    **File 1: `skills/upstream-merge/SKILL.md`** — exactly 4 numbered top-level sections, total ~140-160 lines. Use the YAML frontmatter pattern from `skills/mcp-adapter-test/SKILL.md` (lines 1-12):
    - `name: upstream-merge`
    - `description: >` block (1 paragraph, 5-7 lines) covering: fork-maintainer workflow, merges `https://github.com/nicobailon/pi-mcp-adapter` into `njuptlzf/mcp-adapter`, reads `UPSTREAM-CHANGES.md` for per-file decisions, runs Pi-coupling marker grep, applies conflict resolution. Trigger phrases: `"merge upstream"`, `"sync fork"`, `"upstream conflict"`, `"resolve upstream merge"`.
    - H1 title: `# Upstream Merge (fork-maintainer)`

    **Section `## 1. When to invoke`** (~15 lines, per RESEARCH Dimension 2):
    - 3 trigger conditions as a bulleted list, each 1-2 lines:
      - (a) After `git fetch upstream` and before `git merge upstream/main` (pre-flight)
      - (b) After `git pull upstream main` produces a conflict (in-flight resolution)
      - (c) Before `git cherry-pick <upstream-sha>` (targeted commit import)
    - 1 callout line: "If `git fetch upstream` fails with GnuTLS errors, use the documented workaround: `GIT_SSL_NO_VERIFY=1 git -c http.sslVerify=false fetch upstream`."

    **Section `## 2. Read UPSTREAM-CHANGES.md first`** (~10 lines):
    - 1-2 sentence intro: "Always read `UPSTREAM-CHANGES.md` at the repo root before touching any file. The 5-column manifest gives you a fast path for `ours` / `theirs` rows and a 'must-assess' flag for `assess` rows."
    - 4-line "How to look up a file" recipe:
      - grep by file: `grep -F '` <path>`' UPSTREAM-CHANGES.md`
      - grep by category: `grep -E '^\| (adapter|source|interface|skill|test|docs|config|planning|agents_meta|other) ' UPSTREAM-CHANGES.md`
      - verify freshness: `git diff upstream/main --name-status | wc -l` vs `grep -cE '^\| \`' UPSTREAM-CHANGES.md` (gap ≤ 10)
      - if file is missing from manifest: treat as `assess` (newly diverged) and run the full grep in §3.1

    **Section `## 3. Decision tree`** (~60 lines — the heart of the skill):
    - Step 1: Identify the file's `Category` from the manifest
    - Step 2: Branch on `Default Resolution`:
      - **`ours`** → `git checkout --ours <path>`, mark "ours" in commit body, jump to §4 Checklist
      - **`theirs`** → `git checkout --theirs <path>`, run `npx tsc --noEmit`, jump to §4 Checklist
      - **`assess`** → run §3.1 grep, branch on result: 0 hits → `--theirs`; ≥1 hit → §3.2 (follow-up flow)
      - **`manual`** → open editor, line-by-line review, each hunk: prefer upstream if generic, prefer ours if Pi-coupled
    - Step 3 (Special cases):
      - File is `new` + Category ∈ {adapter, interface, skill, planning} → `--ours` (we own the directory)
      - File is `deleted` in upstream + local file still referenced → keep ours, add `// deprecated: removed in upstream; kept for fork back-compat` JSDoc
    - **§3.1 — Pi-coupling marker grep (corrected template)** — embed the verbatim RESEARCH Dimension 3 corrected template (5 sub-commands: type/class / package / env var / UI surface / ToolInfo) with the `\b` word boundaries. Include the verbatim exclude pattern `grep -vE 'types/pi-(ai|coding-agent|tui)\.d\.ts:'` for Pi type declaration files. Add a one-line cross-reference (NO literal `pi.X` patterns in SKILL.md — the 8 false-positive patterns from the original CONTEXT draft are DELETED and catalogued in `references/pi-coupling-markers.md` §"DELETED markers"): "**Note:** The 8 `pi.X` call patterns from the original CONTEXT-03-B draft are DELETED — they produce systematic false positives on `agentapi.X` calls. See `references/pi-coupling-markers.md` §"DELETED markers" for the full inventory and rationale."
    - **§3.2 — 5-step follow-up flow** (verbatim from CONTEXT §"UPSTREAM-03-C"):
      1. Accept the upstream diff first (`git checkout --theirs <path>`, then `git add <path>`); do NOT block the merge
      2. Immediately stage a follow-up commit that refactors the Pi-coupling out (use Phase 5 DECOUPLE pattern: extract to adapter, wrap behind `AgentContext.ui`, or use the generic `RenderOutput` interface)
      3. Open a follow-up issue with title prefix `pi-coupling-followup:` and label `pi-coupling-followup`
      4. Record the issue number in the merge commit body (e.g., `Refs #N`)
      5. Do NOT manually re-edit the upstream diff during merge (avoid creating more conflicts)
    - **§3.3 — `manual` review rule of thumb** (1 line): "For `manual` rows, accept upstream hunks unless they touch a function signature that generic code depends on (e.g., `createMcpAdapter(agentapi, ctx, config, cache)` in `adapters/entry.ts` — signature is frozen per D-07)."

    **Section `## 4. Checklist`** (exactly 6 items, each machine-checkable per VALIDATION.md §"Phase Requirements → Validation Method" + RESEARCH Dimension 2 §"Checklist 6 项 machine-checkable vs human-checkable"):
    - (a) All conflicts resolved → `git diff --name-only --diff-filter=U | wc -l` = 0
    - (b) Pi-coupling markers = 0 in merged core code → the 5-sub-command grep from §3.1, applied to the post-merge working tree, returns 0 lines (or the only hits are in `adapters/`, `types/`, or `__tests__/`)
    - (c) `npx tsc --noEmit` exits 0
    - (d) `npm test` (or `npm run test:prebuild && npx vitest run`) all green
    - (e) `git diff upstream/main --stat` table updated → manifest 5-column table re-derived via `git diff upstream/main --name-status -- '*.ts' '*.md' '*.json' | awk` and compared (gap ≤ 10)
    - (f) Commit message prefix → `upstream-merge: <summary>` (e.g., `upstream-merge: sync v2.10.0 (N files, M conflicts resolved)`)

    **File 2: `skills/upstream-merge/references/pi-coupling-markers.md`** — a 3-section reference page (~60-80 lines):
    - H1: `# Pi-coupling markers (reference)`
    - 1-line intro: "The grep template in §3.1 of `SKILL.md` is a 5-sub-command recipe; this page is the per-marker inventory behind it."
    - `## HIGH-precision markers` table: `\bExtensionAPI\b`, `\bExtensionContext\b`, `\bExtensionUIContext\b`, `\bAgentToolResult\b`, `PI_CODING_AGENT_DIR`, `@earendil-works/pi-(coding-agent|ai|tui)`, `\.pi/agent/mcp\.json` — for each row, columns: marker / category / expected count after merge (0 in core) / false-positive notes
    - `## MEDIUM-precision markers` table: `\bctx\.ui\.(notify|form|custom|theme)` — note "structural compatibility; this fork's `UISystem` deliberately uses Pi-style API surface (D-04). Hits in `commands.ts` are NOT follow-up triggers."
    - `## DELETED markers` table: 8 `pi.X` patterns + `ToolInfo` (unqualified) + `AgentToolUpdateCallback` — list each pattern verbatim as a code-spanned token in the table (e.g., `` `pi\.registerTool` ``, `` `pi\.on\(` ``, `` `pi\.exec\(` ``, `` `pi\.sendMessage\(` ``, `` `pi\.getAllTools\(` ``, `` `pi\.registerCommand\(` ``, `` `pi\.registerFlag\(` ``, `` `pi\.getFlag\(` ``) with per-row "rationale" column explaining why each is DELETED (the `agentapi.X` substring collision + 0 real-coupling matches). Add a closing paragraph: "These 8 patterns are intentionally absent from `SKILL.md` §3.1's grep template; this table is the only place they are catalogued. `ToolInfo` may be flagged only by import path: `from .*pi-coding-agent.*ToolInfo` (NOT `from .*interfaces/agent-api.*ToolInfo`)."

    The executor MUST use the Write tool to create each file in a single Write call (chunk only if the tool's truncation fallback triggers).
  </action>
  <verify>
    <automated>test -f skills/upstream-merge/SKILL.md && test -f skills/upstream-merge/references/pi-coupling-markers.md && [ "$(grep -cE '^## [1-4]\.' skills/upstream-merge/SKILL.md)" -eq 4 ] && [ "$(grep -c '\bExtensionAPI\b' skills/upstream-merge/SKILL.md)" -ge 1 ] && [ "$(grep -cE 'pi\\.registerTool|pi\\.sendMessage' skills/upstream-merge/SKILL.md)" -eq 0 ]</automated>
    <automated_returns>all true (4 sections, corrected grep present, 8 pi.X patterns absent)</automated_returns>
  </verify>
  <acceptance_criteria>
    - `test -f skills/upstream-merge/SKILL.md` exits 0
    - `test -f skills/upstream-merge/references/pi-coupling-markers.md` exits 0
    - `grep -cE '^## [1-4]\.' skills/upstream-merge/SKILL.md` = 4 (exactly 4 numbered top-level sections)
    - `grep -c '\bExtensionAPI\b' skills/upstream-merge/SKILL.md` ≥ 1 (corrected grep template present)
    - `grep -cE 'pi\\.registerTool|pi\\.on|pi\\.sendMessage|pi\\.getFlag' skills/upstream-merge/SKILL.md` = 0 (the 8 false-positive patterns are NOT in `SKILL.md` at all; they appear ONLY in `references/pi-coupling-markers.md` §"DELETED markers")
    - `grep -c 'upstream-merge:' skills/upstream-merge/SKILL.md` ≥ 1 (commit-message prefix in Checklist item (f))
    - `grep -c 'pi-coupling-followup' skills/upstream-merge/SKILL.md` ≥ 1 (follow-up label in §3.2)
    - SKILL.md is ≤ 200 lines total
  </acceptance_criteria>
  <done>
    `skills/upstream-merge/SKILL.md` exists with exactly 4 numbered top-level sections, embeds the RESEARCH Dimension 3 corrected grep template (with `\b` word boundaries and `types/pi-*.d.ts` exclusion), the 5-step follow-up flow, and the 6-item machine-checkable checklist. `skills/upstream-merge/references/pi-coupling-markers.md` documents the 7 HIGH + 1 MEDIUM + DELETED markers with import-path filter for `ToolInfo`. **No** `pi.X` literal patterns (e.g., `pi\.registerTool`, `pi\.sendMessage`) appear in `SKILL.md`; they are catalogued exclusively in `references/pi-coupling-markers.md` §"DELETED markers".
  </done>
</task>

<task type="auto">
  <name>Task 2: Dry-run scenario 1 (OAuth in init.ts, Pi-coupling-free) — worktree + log</name>
  <files>.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md</files>
  <read_first>
    - `skills/upstream-merge/SKILL.md` (output of Task 1 — the agent follows §1-§4 in order)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"VERIFY-A" + §"VERIFY-B" + §"VERIFY-C" (Scenario 1 definition)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` Dimension 5 §"Scenario 1: OAuth refresh in init.ts(无 Pi-coupling)" + §"Resolution log 模板(markdown)" + §"Dry-run 应当在独立 worktree 中"
    - `UPSTREAM-CHANGES.md` §"Divergence manifest" (the `init.ts` row's Default Resolution = `assess` + Rationale linking to D-04)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md` (for the `upstream/main` SHA and the `git fetch` confirmation that the ref is already local)
  </read_first>
  <action>
    Run dry-run scenario 1 in an isolated git worktree to prove the SKILL.md workflow drives a conflict-free merge to commit. Per CONTEXT VERIFY-A Scenario 1: hypothetical upstream change = "OAuth refresh token logic added to `init.ts`, Pi-coupling-free".

    **Step 1 — Create worktree at `/tmp/dryrun-oauth-init`** (per RESEARCH Dimension 5 worktree pattern):
    - `git worktree add /tmp/dryrun-oauth-init -b dryrun/oauth-refresh-init upstream/main`
    - `cd /tmp/dryrun-oauth-init` (every subsequent command runs in this worktree; do NOT modify the main repo)

    **Step 2 — Materialise the hypothetical "upstream init.ts" with a Pi-coupling-free OAuth refresh addition**:
    - `git checkout upstream/main -- init.ts` (overwrite the worktree's `init.ts` with the upstream version, simulating "upstream has a new `init.ts`")
    - Append an illustrative comment block at the END of `init.ts` (NOT in a fenced code block — write a single bash heredoc-free comment to a temp file and append):
      - `// Hypothetical upstream addition (Scenario 1 of dry-run): OAuth refresh token rotation.` then `// Pi-coupling-free: uses only AgentAPI + AgentContext + UISystem interfaces.` then `async function refreshOAuthToken(_serverName: string): Promise<void> { /* ... */ }`
      - 3 lines total, no real logic, no real Pi import. This is illustrative for the dry-run only.

    **Step 3 — Run SKILL.md §1-§4 in order** (the agent's actual workflow when this skill is invoked):
    - **§1 When to invoke:** the scenario is "post-fetch, pre-merge" — declare that
    - **§2 Read UPSTREAM-CHANGES.md first:** copy `/path/to/UPSTREAM-CHANGES.md` to the worktree at `UPSTREAM-CHANGES.md` (this is needed because `/tmp/dryrun-oauth-init` is a fresh worktree without the manifest), then `grep -F '`init.ts`' UPSTREAM-CHANGES.md` to find the `init.ts` row → record the row's `Category = source` + `Default Resolution = assess` + `Rationale` verbatim into the log
    - **§3.1 grep:** `git diff upstream/main --name-only -- '*.ts' | xargs grep -nE '\bExtensionAPI\b|\bExtensionContext\b|\bExtensionUIContext\b|\bAgentToolResult\b|PI_CODING_AGENT_DIR|@earendil-works/pi-(coding-agent|ai|tui)' 2>/dev/null | grep -vE 'types/pi-(ai|coding-agent|tui)\.d\.ts:'` against the worktree's modified `init.ts` → record verbatim output (expect 0 lines, because the illustrative addition is Pi-coupling-free)
    - **§3.2 grep branch:** 0 hits → decide `--theirs` is safe (do NOT execute `git checkout --theirs`, because in this worktree the file IS theirs + illustrative comment; just record the decision)
    - **§3 step 3 special case:** Status for `init.ts` in the diff is `M` (modified); Category is `source`; the default branch was `assess`; after 0-hit grep, escalation to `--theirs` is correct
    - **§4 Checklist:** go through all 6 items; for each, either run the command (e.g., `cd /tmp/dryrun-oauth-init && npx tsc --noEmit` and `npm test`) and capture PASS, or note "command deferred to real merge" if it would require mutating the main repo
    - **Commit message template:** the agent records the literal string the human would type for the real merge: `upstream-merge: accept OAuth refresh token in init.ts (Pi-coupling-free per dry-run scenario 1)`

    **Step 4 — Write the resolution log** at `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` (back in the main repo working directory, NOT in the worktree) with this structure (per RESEARCH Dimension 5 §"Resolution log 模板" + VALIDATION.md §"Dry-run Scenario Validation"):
    - H1: `# Dry-run scenario 1: OAuth refresh in init.ts (Pi-coupling-free)`
    - 1-paragraph preamble: "Worktree at `/tmp/dryrun-oauth-init`; branch `dryrun/oauth-refresh-init`; simulated upstream commit adds a Pi-coupling-free OAuth refresh function to `init.ts`. Skill workflow §1-§4 walked end-to-end; this log is the evidence."
    - `## Setup` table: Worktree path / Branch / Base ref / Hypothetical change / `upstream/main` SHA
    - `## Manifest lookup` block: literal `grep -F '`init.ts`' UPSTREAM-CHANGES.md` output (the `init.ts` row from the manifest)
    - `## SKILL.md §3.1 Pi-coupling marker grep` block: literal bash command + literal output + decision ("0 hits → accept `--theirs`")
    - `## SKILL.md §3.2 / §3 step 3 follow-up decision` block: "0 hits → no follow-up issue needed; proceed with `--theirs`"
    - `## SKILL.md §4 Checklist` table: 6 rows (one per checklist item), columns: Item / Command / Result (PASS / N/A in dry-run) / Notes
    - `## Commit message template` block: literal commit-message string the agent would use for the real merge
    - `## Outcome` 1-line summary: "Skill drives a Pi-coupling-free upstream change to `--theirs` decision with 0 follow-up issues. Workflow validated."
    - `## Worktree cleanup` 2-line footer: "To clean up: `git worktree remove /tmp/dryrun-oauth-init && git branch -D dryrun/oauth-refresh-init`" (do NOT execute; the user can run it after `/gsd-verify-work 08`)

    **Step 5 — Do NOT commit or push** (the dry-run worktree is ephemeral; the log is the artefact).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md && [ "$(grep -c '## ' .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md)" -ge 6 ] && grep -c '0 hits' .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md</automated>
    <automated_returns>all true (file exists, ≥6 H2 sections, 0-hits outcome recorded)</automated_returns>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` exits 0
    - The log contains all 6 H2 sections (Setup / Manifest lookup / SKILL.md §3.1 grep / follow-up decision / Checklist / Commit message template + Outcome + Worktree cleanup)
    - The log includes the literal `grep -F '`init.ts`' UPSTREAM-CHANGES.md` output showing the `init.ts` row's `Default Resolution = assess`
    - The log includes the literal §3.1 grep command + its 0-hit output
    - The §4 Checklist table has exactly 6 rows
    - The commit message template starts with `upstream-merge:`
    - The worktree `/tmp/dryrun-oauth-init` still exists at end of task (NOT cleaned up; the user can do it after verify)
  </acceptance_criteria>
  <done>
    Dry-run scenario 1 completed: the SKILL.md workflow walked §1-§4 in an isolated worktree, the §3.1 grep returned 0 hits, the decision is `--theirs` with no follow-up issue, and the resolution log captures every step verbatim for `/gsd-verify-work 08` to audit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dry-run scenario 2 (mcp-toggle in commands.ts with Pi import) — worktree + log + follow-up issue template</name>
  <files>.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md</files>
  <read_first>
    - `skills/upstream-merge/SKILL.md` (output of Task 1 — follow §3.2 follow-up flow exactly)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"VERIFY-A" + §"VERIFY-B" (Scenario 2 definition)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` Dimension 5 §"Scenario 2: mcp-toggle in commands.ts with Pi import" + §"Resolution log 模板" (Scenario 2 variant) + Dimension 3 §"关键发现 #2: 3 个真实 Pi-coupling 残留点"
    - `UPSTREAM-CHANGES.md` (the `commands.ts` row's `Default Resolution = assess` + `ctx.ui` structural-compat note)
    - `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` (mirror the same log structure)
  </read_first>
  <action>
    Run dry-run scenario 2 in an isolated git worktree. Per CONTEXT VERIFY-A Scenario 2: hypothetical upstream change = "added `mcp-toggle` command to `commands.ts` that imports `@earendil-works/pi-coding-agent` directly (Pi-coupling re-introduction)".

    **Step 1 — Create worktree at `/tmp/dryrun-mcp-toggle`**:
    - `git worktree add /tmp/dryrun-mcp-toggle -b dryrun/mcp-toggle-commands upstream/main`
    - `cd /tmp/dryrun-mcp-toggle`

    **Step 2 — Materialise the hypothetical "upstream commands.ts" with a Pi-coupling re-introduction**:
    - `git checkout upstream/main -- commands.ts`
    - Insert an illustrative line at the top of `commands.ts` (after the existing imports, before the first non-import line) using a `sed` or equivalent: `import { ExtensionAPI } from "@earendil-works/pi-coding-agent";` (the single line of Pi-coupling that triggers Scenario 2)
    - Append an illustrative comment block at the end: `// Hypothetical upstream addition (Scenario 2 of dry-run): mcp-toggle command.` then `// Pi-coupling re-introduction: directly imports ExtensionAPI from @earendil-works/pi-coding-agent.` then `// Per SKILL.md §3.2, this triggers a follow-up issue + follow-up commit.`
    - 3 lines total, illustrative only, no real command logic.

    **Step 3 — Run SKILL.md §1-§4 in order** (parallel structure to Task 2, but escalate to follow-up flow):
    - **§1 When to invoke:** declare "post-fetch, pre-merge" trigger
    - **§2 Read UPSTREAM-CHANGES.md first:** copy `UPSTREAM-CHANGES.md` to the worktree, then `grep -F '`commands.ts`' UPSTREAM-CHANGES.md` → record the `commands.ts` row's `Category = source` + `Default Resolution = assess` + `Rationale` verbatim (the rationale should mention "structural compatibility of `ctx.ui.X` (MEDIUM marker, NOT follow-up trigger)" per UPSTREAM-01 spec)
    - **§3.1 grep:** the same 5-sub-command recipe from Task 2, run against the worktree's modified `commands.ts` → record verbatim output (expect ≥1 hit: `@earendil-works/pi-coding-agent` in `commands.ts`)
    - **§3.2 5-step follow-up flow** (per CONTEXT §"UPSTREAM-03-C" + the SKILL.md section you just wrote):
      - Step 1: Accept the upstream diff first (record "in real merge: `git checkout --theirs commands.ts && git add commands.ts`")
      - Step 2: Stage a follow-up commit (record "in real merge: refactor the `ExtensionAPI` import out, use generic `AgentContext`; commit message `refactor: replace ExtensionAPI import in commands.ts with generic AgentContext (refs #N)`")
      - Step 3: Open a follow-up issue — record the literal issue title `pi-coupling-followup: refactor mcp-toggle in commands.ts to use AgentContext` and label `pi-coupling-followup` (do NOT actually open the issue in this dry-run; just write the issue body template as a fenced code block in the log)
      - Step 4: Reference the issue number in the merge commit body
      - Step 5: Do NOT manually re-edit the upstream diff during merge
    - **§4 Checklist:** walk all 6 items, noting that item (b) (Pi-coupling markers = 0 in merged core code) will only PASS after the follow-up commit, not after the merge commit alone
    - **Commit message template:** record the literal `upstream-merge: accept mcp-toggle in commands.ts (Pi-coupling re-introduction; see issue #N, follow-up commit pending)` string

    **Step 4 — Write the resolution log** at `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md` (back in the main repo) with the same H2 structure as Scenario 1, plus an extra `## Follow-up issue template` section:
    - H1: `# Dry-run scenario 2: mcp-toggle in commands.ts (Pi-coupling re-introduction)`
    - 1-paragraph preamble: "Worktree at `/tmp/dryrun-mcp-toggle`; branch `dryrun/mcp-toggle-commands`; simulated upstream commit adds `mcp-toggle` command to `commands.ts` with direct `ExtensionAPI` import. Skill workflow §1-§4 walked end-to-end; §3.2 follow-up flow activated. This log is the evidence the 5-step process produces an actionable issue + follow-up commit, not a merge blocker."
    - `## Setup` table: same shape as Scenario 1
    - `## Manifest lookup` block: literal `grep -F '`commands.ts`' UPSTREAM-CHANGES.md` output
    - `## SKILL.md §3.1 Pi-coupling marker grep` block: literal bash command + literal output (≥1 hit expected) + decision ("hit detected → escalate to §3.2 follow-up flow")
    - `## SKILL.md §3.2 5-step follow-up flow` block: 5 numbered sub-sections, one per step in the flow
    - `## Follow-up issue template` block: a fenced code block (this is documentation, not action — fenced code is allowed in log artefacts) with the issue title + body + labels
    - `## Follow-up commit message template` block: literal `refactor:` commit message string
    - `## SKILL.md §4 Checklist` table: 6 rows; row (b) "Pi-coupling markers = 0 in merged core code" has Result = "PASS after follow-up commit" with a Notes column explaining the two-commit sequence
    - `## Commit message template` block: literal `upstream-merge:` string with the issue number placeholder
    - `## Outcome` 1-line summary: "Skill drives a Pi-coupling re-introduction through §3.2 to a 2-commit sequence (merge + follow-up refactor) with a labelled follow-up issue. Workflow validated."
    - `## Worktree cleanup` 2-line footer (same as Scenario 1)

    **Step 5 — Do NOT commit or push** (dry-run worktree is ephemeral).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md && [ "$(grep -c '## ' .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md)" -ge 8 ] && [ "$(grep -c 'pi-coupling-followup' .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md)" -ge 1 ] && grep -c '@earendil-works/pi-coding-agent' .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md</automated>
    <automated_returns>all true</automated_returns>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md` exits 0
    - The log contains ≥ 8 H2 sections (Setup / Manifest lookup / §3.1 grep / §3.2 follow-up / Follow-up issue template / Follow-up commit template / §4 Checklist / Commit message template + Outcome + Worktree cleanup)
    - The log includes the literal `grep -F '`commands.ts`' UPSTREAM-CHANGES.md` output showing the row's `Default Resolution = assess`
    - The log includes the literal §3.1 grep command + its ≥1-hit output (specifically `@earendil-works/pi-coding-agent`)
    - The log includes all 5 numbered sub-sections of the §3.2 follow-up flow
    - The log contains a follow-up issue template with the literal `pi-coupling-followup` label
    - The §4 Checklist table has exactly 6 rows; row (b) notes the two-commit sequence
    - The merge commit message template starts with `upstream-merge:` and references the follow-up issue
  </acceptance_criteria>
  <done>
    Dry-run scenario 2 completed: the SKILL.md workflow walked §1-§4 in an isolated worktree, the §3.1 grep returned ≥1 hit, the §3.2 5-step follow-up flow produced an actionable issue template + follow-up commit template, and the resolution log captures every step for `/gsd-verify-work 08` to audit. The skill is proven to handle BOTH the "clean merge" path (Scenario 1) and the "Pi-coupling re-introduction" path (Scenario 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — walk both dry-run logs + confirm SKILL.md flow</name>
  <files>.planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md (created in Task 5)</files>
  <read_first>
    - `skills/upstream-merge/SKILL.md` (output of Task 1)
    - `UPSTREAM-CHANGES.md` (output of Plan 08-01)
    - `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` (output of Task 2)
    - `.planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md` (output of Task 3)
  </read_first>
  <what-built>
    Task 1 produced `skills/upstream-merge/SKILL.md` (4 numbered sections, RESEARCH Dimension 3 corrected grep, 5-step follow-up flow, 6-item machine-checkable checklist) and `skills/upstream-merge/references/pi-coupling-markers.md` (HIGH/MEDIUM/DELETED marker inventory). Tasks 2+3 ran both dry-run scenarios in isolated worktrees (`/tmp/dryrun-oauth-init` and `/tmp/dryrun-mcp-toggle`) and produced the two resolution logs cited above.
  </what-built>
  <how-to-verify>
    Walk through the four artefacts in this order:

    1. Open `skills/upstream-merge/SKILL.md` and confirm:
       - 4 numbered top-level sections (`## 1. When to invoke` / `## 2. Read UPSTREAM-CHANGES.md first` / `## 3. Decision tree` / `## 4. Checklist`)
       - The §3.1 grep uses `\bExtensionAPI\b` (with word boundary) and does NOT contain the 8 false-positive `pi.X` patterns in a runnable position
       - The §3.2 follow-up flow has exactly 5 numbered steps
       - The §4 checklist has exactly 6 items, each with a concrete command

    2. Open `skills/upstream-merge/references/pi-coupling-markers.md` and confirm:
       - 7 HIGH-precision markers listed
       - 1 MEDIUM marker (`ctx.ui`) noted as "structural, NOT follow-up"
       - DELETED section explicitly mentions the 8 `pi.X` patterns + `ToolInfo` + `AgentToolUpdateCallback`

    3. Open `dry-run-scenario-1-oauth-init.md` and confirm:
       - The §3.1 grep output has 0 hits
       - The decision is `--theirs`
       - No follow-up issue opened
       - Checklist table has 6 rows

    4. Open `dry-run-scenario-2-mcp-toggle-commands.md` and confirm:
       - The §3.1 grep output has ≥1 hit (`@earendil-works/pi-coding-agent`)
       - The §3.2 follow-up flow has 5 numbered sub-sections
       - A follow-up issue template is present with the `pi-coupling-followup` label
       - The merge commit message references the follow-up issue number
       - Checklist row (b) notes the two-commit sequence

    Optional: `cd /tmp/dryrun-oauth-init && git log --oneline | head -3` and `cd /tmp/dryrun-mcp-toggle && git log --oneline | head -3` to confirm both worktrees exist (the executor did NOT clean them up — that's your call after verify).
  </how-to-verify>
  <resume-signal>
    Type `approved` to proceed to Task 5 (commit + SUMMARY). If any artefact is wrong, describe the issue (e.g., "Scenario 2 grep output is missing the `pi-coding-agent` hit" or "SKILL.md §3.1 still has `pi\.registerTool` in a runnable position") — the executor will fix and re-verify.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write deferred-items.md + 08-02-SUMMARY.md + commit</name>
  <files>.planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md, .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` §"Deferred Ideas" (4 items: CI hook, auto-resolve bot, reverse-contribute, refresh schedule)
    - `.planning/phases/07-integration-test-rebuild/deferred-items.md` (the pattern to mirror: H1 + 1-paragraph preamble + per-item subsections with Found during / Action taken)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-VALIDATION.md` §"Phase Requirements → Validation Method (1:1)" (the 4 PASS lines to record)
    - `.planning/phases/08-upstream-merge-conflict-resolution/08-01-SUMMARY.md` (mirror the SUMMARY structure: ## Summary / ## Commands run / ## Files changed / ## Acceptance evidence / ## Issues encountered / ## Next steps)
  </read_first>
  <action>
    Create 2 final files, then commit.

    **File 1: `deferred-items.md`** — mirror the Phase 7 pattern (`deferred-items.md` lines 1-24). 4 sections, one per CONTEXT §"Deferred Ideas" item:
    - H1: `# Deferred Items — Phase 08`
    - 1-paragraph preamble: "Items discovered during Phase 08 plan execution that are out of scope for the current phase and should be picked up by a later phase, wave, or human action."
    - 4 H2 sections, each with the same structure: H2 title (1-line) + 3-4 sub-bullets (Found during / Why deferred / Suggested owner / Action taken)
    - Section 1: `## CI hook / GitHub Action for divergence detection` (the proposed `.github/workflows/upstream-divergence.yml` that auto-runs `git diff upstream/main` and opens an issue on divergence > N files — per CONTEXT §"Deferred Ideas" item 1)
    - Section 2: `## Merge conflict auto-resolve bot` (the proposed `gh`-CLI-based bot that handles `ours`/`theirs` rows automatically and escalates `assess` rows to a human — per CONTEXT item 2)
    - Section 3: `## Reverse-contribute upstream PRs` (the proposed flow for sending local fork fixes back to `nicobailon/pi-mcp-adapter` — per CONTEXT item 3, requires license + contribution agreement)
    - Section 4: `## Quarterly manifest refresh schedule` (per CONTEXT item 4)

    **File 2: `08-02-SUMMARY.md`** — executor summary with these sections (verbatim structure from `08-01-SUMMARY.md`):
    - `## Summary` — 1-paragraph recap: skill authored, 2 dry-run logs landed, 6-item checklist validated, deferred-items list captured
    - `## Commands run` — verbatim shell transcript (≤ 50 lines): the 2 `git worktree add` commands, the 2 `git checkout upstream/main --` commands, the 2 §3.1 grep invocations + outputs, the `npx tsc --noEmit` + `npm test` outputs from inside both worktrees
    - `## Files changed` — bulleted list: `skills/upstream-merge/SKILL.md` (new), `skills/upstream-merge/references/pi-coupling-markers.md` (new), 2 dry-run logs (new), `deferred-items.md` (new)
    - `## Acceptance evidence` — 4 PASS lines (one per VALIDATION.md requirement row):
      - `UPSTREAM-02 file-existence: PASS — test -f skills/upstream-merge/SKILL.md exits 0`
      - `UPSTREAM-02 section count: PASS — grep -cE '^## [1-4]\.' skills/upstream-merge/SKILL.md = 4`
      - `UPSTREAM-03 grep template: PASS — RESEARCH Dimension 3 corrected template embedded; 8 pi.X false-positives absent from runnable position`
      - `UPSTREAM-03 dry-run: PASS — scenarios 1+2 walked to decision; 2 resolution logs landed`
    - `## Human verification` — 1-line note: "Task 4 checkpoint approved by user at <timestamp>." (or "n/a — autonomous run with default human-verify-timeout" if the user did not engage, which is acceptable for greenfield phase)
    - `## Issues encountered` — bullet list of any GnuTLS / awk / grep / worktree errors encountered and how they were resolved
    - `## Worktree cleanup` — 2 lines: "Worktrees at `/tmp/dryrun-oauth-init` and `/tmp/dryrun-mcp-toggle` retained for post-verify inspection. User can `git worktree remove /tmp/dryrun-*` after `/gsd-verify-work 08`."
    - `## Next steps` — 1-2 lines: "Phase 8 complete; both UPSTREAM-01..04 requirements satisfied; `/gsd-verify-work 08` may now run."

    **Step 3 — Commit** (per workflow security_enforcement: no new package install in this plan, so no slopcheck gate; the only files added are markdown + 1 SKILL.md; per AGENTS.md §"Always Do" / §"Never Do" line 11/21, the executor MUST run `gitnexus_detect_changes()` before committing to verify the blast radius):
    - **Pre-commit audit (MANDATORY per AGENTS.md):** `npx gitnexus detect-changes` (or call the `gitnexus_detect_changes()` MCP tool). Expected output: only the 5 new files (`SKILL.md`, `references/pi-coupling-markers.md`, 2 dry-run logs, `deferred-items.md`, `08-02-SUMMARY.md`) are affected — no source code symbols touched, so blast radius = documentation + skill files only. If the audit shows unexpected source-file impact, the executor MUST abort the commit and re-check Task 1-4 outputs.
    - `git add skills/upstream-merge/SKILL.md skills/upstream-merge/references/pi-coupling-markers.md .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md .planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md`
    - `git commit -m "feat(08): add upstream-merge skill with dry-run validation (UPSTREAM-02, UPSTREAM-03)"`
    - Do NOT push; do NOT create a PR. The 2 dry-run worktrees are in `/tmp` and are not part of the commit.
    - **Note:** This commit only modifies markdown + 1 SKILL.md file (no code symbol), so the gitnexus blast radius is empty; running `npx gitnexus detect-changes` is for AGENTS.md audit-trail compliance, not for catching regressions.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md && test -f .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md && [ "$(grep -c '^## ' .planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md)" -ge 4 ] && [ "$(grep -c 'PASS' .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md)" -ge 4 ] && git log -1 --pretty=format:'%s' | grep -E 'feat\(08\): add upstream-merge'</automated>
    <automated_returns>all true</automated_returns>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` exists with ≥ 4 H2 sections (one per CONTEXT deferred idea)
    - `08-02-SUMMARY.md` exists with all required sections (`## Summary` / `## Commands run` / ## Files changed` / `## Acceptance evidence` / `## Next steps`)
    - `## Acceptance evidence` contains the 4 PASS lines for UPSTREAM-02 file-existence, UPSTREAM-02 section count, UPSTREAM-03 grep template, UPSTREAM-03 dry-run
    - `git log -1 --pretty=format:'%s'` shows the commit message starting with `feat(08): add upstream-merge`
    - `git status` is clean post-commit (no uncommitted changes; `/tmp/dryrun-*` worktrees are not tracked)
    - The 2 dry-run logs and the manifest from Plan 08-01 are NOT modified by this commit
  </acceptance_criteria>
  <done>
    Phase 8 is complete: `skills/upstream-merge/SKILL.md` (4 sections, corrected grep, 5-step follow-up flow, 6-item checklist) is committed; both dry-run logs landed as evidence the skill works end-to-end; the deferred-items list captures the 4 future-work ideas; the 08-02-SUMMARY.md records every command output for `/gsd-verify-work 08`. The fork-maintainer workflow is ready for future upstream syncs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted external: upstream `nicobailon/pi-mcp-adapter` git history (in worktrees) | Dry-run worktrees at `/tmp/dryrun-oauth-init` and `/tmp/dryrun-mcp-toggle` are checked out from `upstream/main` and have illustrative Pi-coupling added. These are isolated worktrees; no code from them is ever `git checkout`'d into the main repo (the dry-run simulates the merge decision but does NOT execute it). |
| Trusted: this fork's working tree | `skills/upstream-merge/SKILL.md` and the 2 dry-run logs are created locally; no external write to them. |
| Untrusted input: `git diff upstream/main --name-status` output | Read-only data the SKILL.md §2 recipe parses. Treated as text, not executed. |
| Trusted: existing skill file (`skills/mcp-adapter-test/SKILL.md`) | Phase 8 mirrors its YAML frontmatter shape; no shared state. |
| Untrusted: `/tmp` filesystem (worktrees) | Ephemeral; the manifest does not reference them. User can clean up via `git worktree remove` after verify. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-FP | Information Disclosure (false positive in grep) | SKILL.md §3.1 grep template | mitigate | Use RESEARCH Dimension 3 corrected template (with `\b` word boundaries, `types/pi-*.d.ts` exclusion). The 8 `pi.X` false-positive patterns are DELETED from `SKILL.md` entirely and catalogued only in `references/pi-coupling-markers.md` §"DELETED markers". Acceptance criterion: `grep -cE 'pi\\.registerTool|pi\\.sendMessage' skills/upstream-merge/SKILL.md` = 0 (the patterns are absent from the runnable grep template and from SKILL.md's prose). |
| T-08-02-FN | Denial of Service (false negative misses Pi-coupling) | SKILL.md §3.1 grep | mitigate | 7 HIGH-precision + 1 MEDIUM marker covers all 13 CONTEXT markers except the 4 deleted (with explicit DELETED note). Dry-run Scenario 2 is the live test: the §3.1 grep on `commands.ts` (with the simulated `import { ExtensionAPI } from "@earendil-works/pi-coding-agent";` line) MUST return ≥1 hit. If it returns 0, the executor must fix the template before commit. |
| T-08-02-PI | Information Disclosure / Tampering (merge in worktree) | `/tmp/dryrun-*` worktrees | mitigate | Worktrees are created with `git worktree add` and base = `upstream/main`. The executor does NOT `git checkout upstream/main -- <file>` into the main repo. All illustrative edits (`sed`, append) happen INSIDE the worktree only. After dry-run, the worktrees are listed in the SUMMARY's `## Worktree cleanup` section for the user to remove. |
| T-08-02-RC | Repudiation (skill could not be followed) | SKILL.md prose | mitigate | Both dry-run logs (§4 Checklist table) record literal command outputs (PASS / N/A) for every checklist item. The 6-item checklist is machine-checkable per VALIDATION.md §"Phase Requirements → Validation Method" UPSTREAM-02 + UPSTREAM-03 rows. |
| T-08-02-IE | Information Exposure | Path enumeration in SKILL.md examples | accept | SKILL.md references public file paths already in the repo (`init.ts`, `commands.ts`, `interfaces/agent-api.ts`). No secrets. |
| T-08-02-CW | Content Wrongness (Pi-coupling re-introduction via bad rationale) | Dry-run logs' Rationale citations | mitigate | Each scenario's `## Manifest lookup` block greps `UPSTREAM-CHANGES.md` for the file path and inlines the manifest row's Category + Default Resolution + Rationale. Cross-check: `grep -c 'Default Resolution = assess' dry-run-scenario-{1,2}-*.md` ≥ 1 in each. |
| T-08-02-CK | Content Wrongness (follow-up flow documentation drift) | SKILL.md §3.2 5 steps | mitigate | The 5 steps are verbatim from CONTEXT §"UPSTREAM-03-C" + mirrored in Scenario 2 log. Acceptance criterion: `grep -c 'pi-coupling-followup' SKILL.md` ≥ 1; `grep -cE '^\\s*\\d\\.' SKILL.md` ≥ 5 in the §3.2 block. |
| T-08-02-SC | Tampering (npm/pip install) | No install in this plan | N/A | Plan 08-02 does NOT install any package (no `npm install`, no `pip install`, no `cargo add`). Threat gate is moot. |
| T-08-02-HV | Tampering (human verification step skipped or rubber-stamped) | Task 4 checkpoint | mitigate | Task 4 is `checkpoint:human-verify` with `gate="blocking"`. The executor must wait for `resume-signal: approved` before proceeding to Task 5. If the user does not engage within the verify timeout, the SUMMARY records `## Human verification` as `n/a — autonomous run with default human-verify-timeout` (acceptable for greenfield, but not the norm). |

## OWASP A01-A10 Disposition

| OWASP ID | Category | Disposition | Rationale |
|----------|----------|-------------|-----------|
| A01 (Broken Access Control) | AuthZ | N/A | No endpoints, no auth, no user input. |
| A02 (Cryptographic Failures) | Crypto | N/A | No secrets, no TLS termination in this plan. (GnuTLS workaround from Plan 08-01 is the only crypto-adjacent concern; deferred to that plan's T-08-01-PI.) |
| A03 (Injection) | Input | N/A | SKILL.md is read by humans/agents, not a query surface. Grep templates take file paths from the manifest (trusted). |
| A04 (Insecure Design) | Design | N/A | The skill's design goal IS to prevent insecure Pi-coupling re-introduction; mitigated via T-08-02-FP + T-08-02-FN. |
| A05 (Security Misconfiguration) | Config | N/A | No config changes. |
| A06 (Vulnerable Components) | Deps | N/A | No package installs. |
| A07 (Identification & Auth Failures) | AuthN | N/A | No authentication surface. |
| A08 (Software & Data Integrity) | Integrity | mitigated | See T-08-02-FP + T-08-02-FN + T-08-02-CK. The dry-run logs are the integrity evidence — if a §3.1 grep returns 0 hits for a known Pi-coupling line (Scenario 2), the executor MUST fix the template. |
| A09 (Logging & Monitoring) | Ops | mitigated | The 2 dry-run logs + the `08-02-SUMMARY.md` `## Commands run` section ARE the audit log for the skill's first live use. |
| A10 (SSRF) | Network | N/A | No outbound requests beyond what Plan 08-01 already documented (`git fetch upstream` with GnuTLS workaround). |
</threat_model>

<verification>
## Phase checks (UPSTREAM-02 + UPSTREAM-03)

- `test -f skills/upstream-merge/SKILL.md` exits 0
- `test -f skills/upstream-merge/references/pi-coupling-markers.md` exits 0
- `grep -cE '^## [1-4]\.' skills/upstream-merge/SKILL.md` = 4 (exactly 4 numbered sections)
- `grep -c '\bExtensionAPI\b' skills/upstream-merge/SKILL.md` ≥ 1 (corrected grep template present)
- `grep -cE 'pi\\.registerTool|pi\\.on|pi\\.sendMessage|pi\\.getFlag' skills/upstream-merge/SKILL.md` = 0 (the 8 false-positive patterns are NOT in `SKILL.md`; they live exclusively in `references/pi-coupling-markers.md` §"DELETED markers")
- `grep -c 'upstream-merge:' skills/upstream-merge/SKILL.md` ≥ 1 (commit-message prefix in Checklist)
- `grep -c 'pi-coupling-followup' skills/upstream-merge/SKILL.md` ≥ 1 (follow-up label in §3.2)
- `test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-1-oauth-init.md` exits 0
- `test -f .planning/phases/08-upstream-merge-conflict-resolution/dry-run-scenario-2-mcp-toggle-commands.md` exits 0
- Scenario 1 log contains the literal `0 hits` decision (Pi-coupling-free → `--theirs`)
- Scenario 2 log contains `@earendil-works/pi-coding-agent` hit + 5-step §3.2 follow-up sub-sections
- Both scenarios cite the manifest row for their target file (`init.ts` / `commands.ts`) via `grep -F`
- `test -f .planning/phases/08-upstream-merge-conflict-resolution/deferred-items.md` exits 0; ≥ 4 H2 sections
- `test -f .planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md` exits 0; ≥ 4 PASS evidence lines
- `git log -1 --pretty=format:'%s'` shows `feat(08): add upstream-merge skill with dry-run validation (UPSTREAM-02, UPSTREAM-03)`
- `git status` is clean post-commit
- Worktrees at `/tmp/dryrun-oauth-init` and `/tmp/dryrun-mcp-toggle` are listed in SUMMARY's `## Worktree cleanup` for user to remove
</verification>

<success_criteria>
Plan 08-02 succeeds when:

1. `skills/upstream-merge/SKILL.md` exists at the expected path with exactly 4 numbered top-level sections, embeds the RESEARCH Dimension 3 corrected grep template (with `\b` word boundaries + `types/pi-*.d.ts` exclusion), the 5-step follow-up flow, and the 6-item machine-checkable checklist. SKILL.md is ≤ 200 lines.
2. `skills/upstream-merge/references/pi-coupling-markers.md` exists with HIGH (7) / MEDIUM (1) / DELETED (3 categories) marker inventory.
3. Both dry-run logs (`dry-run-scenario-1-oauth-init.md` and `dry-run-scenario-2-mcp-toggle-commands.md`) land with: manifest row lookup, literal §3.1 grep output, §3.2 follow-up decision (Scenario 2 only), 6-item checklist table, commit message template, and worktree cleanup note.
4. The 8 false-positive `pi.X` patterns are NOT in `SKILL.md` (runnable position OR prose). They are catalogued exclusively in `references/pi-coupling-markers.md` §"DELETED markers".
5. `deferred-items.md` lists all 4 CONTEXT-deferred ideas (CI hook / auto-resolve bot / reverse-contribute / refresh schedule).
6. `08-02-SUMMARY.md` records 4 PASS evidence lines (one per VALIDATION.md requirement row) and the verbatim commands run inside both worktrees.
7. The plan's `autonomous: false` is honoured: Task 4 (human-verify checkpoint) blocks Task 5 (commit + SUMMARY) until the user types `approved`.
8. A single conventional `feat(08):` commit lands in the local repo with the SKILL.md + 2 dry-run logs + deferred-items + SUMMARY.
9. The 2 dry-run worktrees (`/tmp/dryrun-oauth-init` and `/tmp/dryrun-mcp-toggle`) are listed in the SUMMARY for user cleanup — they are NOT in the commit (they are in `/tmp`).
10. `/gsd-verify-work 08` can read both SUMMARYs and reproduce all 4 PASS evidence lines.

After Plan 08-02 lands, Phase 8 is complete: UPSTREAM-01 (manifest from Plan 08-01) + UPSTREAM-02 (SKILL.md) + UPSTREAM-03 (rules + 2 dry-run scenarios) + UPSTREAM-04 (D-21 reference in manifest) are all satisfied, and the fork-maintainer workflow is ready for future upstream syncs.
</success_criteria>

<output>
Create `.planning/phases/08-upstream-merge-conflict-resolution/08-02-SUMMARY.md` when done.

The 08-02-SUMMARY.md is mandatory: it is the only artefact that records the SKILL.md section count, the 2 dry-run outcomes, the 4 PASS evidence lines for `/gsd-verify-work 08`, and the worktree cleanup instructions for the user.
</output>