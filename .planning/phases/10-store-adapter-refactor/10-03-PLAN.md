---
phase: 10-store-adapter-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - __tests__/adapter-contract.test.ts
  - skills/upstream-merge/references/special-cases.md
autonomous: true
requirements:
  - STORE-04
  - UP-01
  - UP-02
  - PATH-01
user_setup: []

must_haves:
  truths:
    - "Parametric adapter contract tests pass for all 3 agents (kilo, pi, qoder)"
    - "npm run upstream:check exits 0 (no stale registry entries)"
    - "special-cases.md contains store-adapter.ts (fork-only, ours)"
    - "special-cases.md has updated qoder-adapter.ts and kilo-adapter.ts statuses (decoupled-wrapper)"
  artifacts:
    - path: "skills/upstream-merge/references/special-cases.md"
      provides: "Registry with 3 new/updated entries for Phase 10 files"
      contains: "store-adapter.ts"
  key_links:
    - from: "__tests__/adapter-contract.test.ts"
      to: "interfaces/agent-api.ts"
      via: "AGENT_ADAPTERS factory()"
      pattern: "AGENT_ADAPTERS\\.map"
    - from: "scripts/upstream-divergence.ts"
      to: "skills/upstream-merge/references/special-cases.md"
      via: "npm run upstream:check"
---

<objective>
Update the test infrastructure, upstream-merge registry, and run full verification to
confirm Phase 10 changes are correct, backward compatible, and properly registered for
upstream merge conflict resolution.

Purpose: Ensure the StoreAgentAdapter refactoring (Plan 01) and self-reporting path changes
(Plan 02) are verified by the full test suite and properly tracked in the upstream-merge
registry so future upstream syncs handle new files correctly.

Output: Updated `__tests__/adapter-contract.test.ts`, updated `skills/upstream-merge/references/special-cases.md`,
verified test suite and compilation.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-store-adapter-refactor/CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@__tests__/adapter-contract.test.ts
@skills/upstream-merge/references/special-cases.md
@skills/upstream-merge/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update upstream-merge special-cases registry</name>
  <files>skills/upstream-merge/references/special-cases.md</files>
  <action>
    Per UP-01 and UP-02: update `skills/upstream-merge/references/special-cases.md` with entries for the Phase 10 changes.

    ### Entries to ADD (append to the table before the taxonomy section)

    **1. New file: `adapters/store-adapter.ts` (UP-01)**

    Append after line 28 (after the last `bin/qoder-mcp-bridge.ts` row):

    ```markdown
    | `adapters/store-adapter.ts` | `fork-only` | Shared StoreAgentAdapter base class extracted from qoder/kilo adapters; zero Pi imports — zero conflict risk with upstream | `ours` |
    ```

    **Status:** `fork-only` per UP-01 — this file has no upstream equivalent and never will.
    **Decision:** `ours` — always keep fork version.

    **2. Updated status: `adapters/qoder-adapter.ts` (UP-02)**

    Find the existing entry for `interfaces/agent-api.ts` (line 13). The qoder-adapter.ts and kilo-adapter.ts currently have NO entry in the registry (they're resolved by the 12-category matrix default rules). Per UP-02, we need to reflect their new role as thin wrappers.

    Add NEW entries (before `interfaces/agent-api.ts` line 13, to keep adapters together):

    ```markdown
    | `adapters/qoder-adapter.ts` | `decoupled-wrapper` | Phase 10: refactored to thin wrapper extending StoreAgentAdapter; Qoder-specific Query.streamInput routing preserved | `ours` |
    | `adapters/kilo-adapter.ts` | `decoupled-wrapper` | Phase 10: refactored to thin wrapper extending StoreAgentAdapter; Kilo-specific sendMessageFn callback preserved | `ours` |
    ```

    **Status:** `decoupled-wrapper` per UP-02 — they now wrap the shared StoreAgentAdapter class with agent-specific routing.
    **Decision:** `ours` — fork-authored, always keep.

    ### Verify no other entries need updating

    The following existing entries are NOT affected by Phase 10:
    - `interfaces/agent-api.ts` (line 13) — only a new optional field added; still `decoupled-wrapper`
    - `interfaces/agent-paths.ts` (line 14) — minor internal refactor only; still `decoupled-wrapper`
    - `bin/kilo-mcp-server.ts` (line 27) — no changes needed (imports still work)
    - `bin/qoder-mcp-bridge.ts` (line 28) — no changes needed (imports still work)
    - All other entries unchanged

    ### Update the "last curated" footer

    Line 42: update date and description:
    ```markdown
    2026-06-26 (Phase 10 StoreAdapter refactor + agent self-reporting paths; 23 anchored entries — added `adapters/store-adapter.ts` + `adapters/qoder-adapter.ts` + `adapters/kilo-adapter.ts`). Update this footer on Phase 10+ amendments.
    ```

    ### After editing, run verification

    ```bash
    npm run upstream:check
    ```

    Expected output: exits 0 (clean). The 3 new entries appear in `✓ registered` section. No `⚠ diverged-but-not-registered` warnings for the new files.
  </action>
  <verify>
    <automated>npm run upstream:check</automated>
  </verify>
  <done>
    special-cases.md has 23 entries (was 20). store-adapter.ts registered as fork-only/ours.
    qoder-adapter.ts and kilo-adapter.ts registered as decoupled-wrapper/ours. npm run upstream:check
    exits 0 with all 3 new entries recognized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify adapter contract test + full test suite</name>
  <files>__tests__/adapter-contract.test.ts</files>
  <action>
    ### Part A: Verify parametric contract test still works

    The `__tests__/adapter-contract.test.ts` (124 lines) uses `describe.each(AGENT_ADAPTERS.map(...))` to run 6 tests against all registered adapters. After Plan 01's refactoring:

    - QoderAdapter and KiloAdapter still exist as classes
    - Their `factory: () => new QoderAdapter()` and `factory: () => new KiloAdapter()` entries in AGENT_ADAPTERS are unchanged
    - Both extend StoreAgentAdapter which implements AgentAPI
    - The 6 contract tests (method existence, register→readback round trips, getFlag, sendMessage/exec signatures) all pass without modification

    **Run the contract test in isolation:**
    ```bash
    npx vitest run __tests__/adapter-contract.test.ts
    ```

    Expected: ALL 10 tests pass (6 parametric tests × 3 adapters = 18 actual test cases, plus 4 MockAgentAPI tests = 22 total passing).

    ### Part B: Check if contract test needs enhancement

    Per the CONTEXT.md verification requirements: "describe.each(AGENT_ADAPTERS) adapter contract test covers StoreAdapter".

    Since StoreAgentAdapter is NOT directly in AGENT_ADAPTERS (it's a base class, not a standalone adapter), the contract test covers it indirectly through QoderAdapter and KiloAdapter. This is correct and sufficient.

    **Optional enhancement (only if context budget permits):** Add a comment to the test file noting:
    ```ts
    // StoreAgentAdapter (adapters/store-adapter.ts) is tested indirectly through
    // QoderAdapter and KiloAdapter, both of which extend it. The AgentAPI contract
    // methods exercised here are implemented in StoreAgentAdapter.
    ```

    ### Part C: Run full test suite

    ```bash
    npm test
    ```

    Expected: all test suites pass, including:
    - `__tests__/qoder-adapter.test.ts` — 25+ tests
    - `__tests__/qoder-adapter-integration.test.ts` — 6+ tests
    - `__tests__/adapter-contract.test.ts` — 22+ tests
    - `__tests__/config.test.ts` — config discovery tests
    - All other existing test files

    ### Part D: Verify TypeScript compilation

    ```bash
    npx tsc --noEmit
    ```

    Expected: zero errors. Confirms all imports, type references, and class hierarchies are correct.

    ### Part E: Verify upstream check

    ```bash
    npm run upstream:check
    ```

    Expected: exits 0 (clean).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run __tests__/adapter-contract.test.ts __tests__/qoder-adapter.test.ts</automated>
  </verify>
  <done>
    All 3 agents pass parametric contract tests. Full test suite passes (npm test).
    TypeScript compilation passes (npx tsc --noEmit). upstream:check exits 0.
    StoreAgentAdapter is covered indirectly through QoderAdapter and KiloAdapter contract tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full verification suite and confirm rollback readiness</name>
  <files>None (verification only)</files>
  <action>
    ### Full verification checklist

    Run each command and confirm passing:

    1. **TypeScript compilation:**
       ```bash
       npx tsc --noEmit
       ```
       Expected: zero errors.

    2. **Full test suite:**
       ```bash
       npm test
       ```
       Expected: all test suites pass.

    3. **Upstream divergence check:**
       ```bash
       npm run upstream:check
       ```
       Expected: exits 0. No `⚠ diverged-but-not-registered` warnings for Phase 10 files.

    4. **QoderAdapter behavior parity:** Run specifically:
       ```bash
       npx vitest run __tests__/qoder-adapter.test.ts __tests__/qoder-adapter-integration.test.ts
       ```
       Verify all tests pass — confirms `Query.streamInput` routing, `attachQuery`/`detachQuery`, `getQueryRef()`, `adaptQoderContext`, `adaptQoderUI` all work identically to pre-refactor.

    5. **KiloAdapter behavior parity:** Run:
       ```bash
       npx vitest run __tests__/adapter-contract.test.ts
       ```
       The contract test covers KiloAdapter's AgentAPI surface. The `attachSendMessage`/`detachSendMessage` methods are tested indirectly via bin entry points compilation.

    6. **Parametric contract test:**
       ```bash
       npx vitest run __tests__/adapter-contract.test.ts --reporter=verbose
       ```
       Verify output shows all 3 agents (kilo, pi, qoder) passing all 6 contract tests.

    ### Rollback readiness check

    Confirm that git state allows rollback:
    ```bash
    git status
    ```
    All changes are in:
    - `adapters/store-adapter.ts` (NEW)
    - `adapters/qoder-adapter.ts` (modified)
    - `adapters/kilo-adapter.ts` (modified)
    - `interfaces/agent-api.ts` (modified)
    - `config.ts` (modified)
    - `interfaces/agent-paths.ts` (optionally modified)
    - `skills/upstream-merge/references/special-cases.md` (modified)

    **Rollback procedure (documented only — do NOT execute):**
    ```bash
    git checkout -- adapters/qoder-adapter.ts adapters/kilo-adapter.ts interfaces/agent-api.ts config.ts interfaces/agent-paths.ts skills/upstream-merge/references/special-cases.md
    rm adapters/store-adapter.ts
    ```

    No database migrations, no data files, no environment changes — pure code refactoring with instant git rollback.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run upstream:check</automated>
  </verify>
  <done>
    All verification steps pass:
    - tsc --noEmit: 0 errors
    - npm test: all suites pass
    - upstream:check: exits 0
    - QoderAdapter/KiloAdapter behavior parity confirmed
    - Rollback is single git checkout command
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry → upstream-divergence.ts | special-cases.md entries parsed by script; malformed rows logged as WARN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Information Disclosure | Test output | accept | Test output may contain file paths — all are workspace-local, not secrets |
| T-10-07 | Denial of Service | upstream:check | accept | Script runs `git diff upstream/main` — network-dependent but manual-only trigger per D-33 |
| T-10-SC | Tampering | npm install | mitigate | No new npm packages added in this plan |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npm test` — full test suite passes
3. `npm run upstream:check` — exits 0
4. `npx vitest run __tests__/adapter-contract.test.ts` — all 3 agents pass
5. `npx vitest run __tests__/qoder-adapter.test.ts __tests__/qoder-adapter-integration.test.ts` — Qoder parity confirmed
</verification>

<success_criteria>
- [ ] special-cases.md has 3 new/updated entries (store-adapter, qoder-adapter, kilo-adapter)
- [ ] Parametric adapter contract test passes for all 3 agents
- [ ] Full test suite (npm test) passes
- [ ] TypeScript compilation (tsc --noEmit) passes
- [ ] npm run upstream:check exits 0
- [ ] QoderAdapter behavior parity verified (Query.streamInput)
- [ ] KiloAdapter behavior parity verified (sendMessageFn)
- [ ] Rollback path documented and validated
</success_criteria>

<output>
Create `.planning/phases/10-store-adapter-refactor/10-03-SUMMARY.md` when done
</output>
