---
phase: 11-skill-unification-and-fixes
plan: 01
type: execute
wave: 1
depends_on:
  - "Phase 10 (StoreAdapter base class + PATH-01 self-reporting paths)"
files_created:
  - skills/mcp-adapter/references/deploy-examples.md
files_modified:
  - skills/mcp-adapter/references/deploy.md
  - skills/upstream-merge/references/special-cases.md
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
  - DEC-04
  - DEC-05
user_setup: []

must_haves:
  truths:
    - "skills/mcp-adapter/references/deploy-examples.md exists and contains deployment code templates (402 lines migrated from legacy skill)"
    - "All anchor links in deploy.md resolve to actual headings in deploy-examples.md — zero broken references"
    - "deploy-examples.md is registered in skills/upstream-merge/references/special-cases.md as fork-only/ours"
    - "npm run upstream:check exits 0 (zero stale entries after commit)"
    - "npx tsc --noEmit exits 0; npm test passes 590+ tests; npm run verify:deploy -- --agent kilo passes"
    - "All Phase 11 changes (prior work + gap fixes) are committed to git"
  artifacts:
    - path: "skills/mcp-adapter/references/deploy-examples.md"
      provides: "Deployment code templates — Branch A/B/C examples for Pi, Qoder, Kilo, and custom agents"
      min_lines: 400
    - path: "skills/mcp-adapter/references/deploy.md"
      provides: "Phase 2 deployment workflow with correct anchor links to deploy-examples.md"
      contains: "#branch-c-custom-agent"
    - path: "skills/upstream-merge/references/special-cases.md"
      provides: "Fork-only file registry including deploy-examples.md"
      contains: "deploy-examples.md"
  key_links:
    - from: "skills/mcp-adapter/references/deploy.md"
      to: "skills/mcp-adapter/references/deploy-examples.md"
      via: "markdown anchor links"
      pattern: "deploy-examples\\.md#branch-c-custom-agent"
    - from: "skills/upstream-merge/references/special-cases.md"
      to: "skills/mcp-adapter/references/deploy-examples.md"
      via: "fork-only registry row"
      pattern: "deploy-examples\\.md.*fork-only"
---

<objective>
Close the remaining gaps in Phase 11 (Skill Unification & Post-Phase-10 Fixes) after the majority
of the work was already executed in the working tree but never committed.

The researcher (RESEARCH.md) confirmed that DEC-01 through DEC-04 are already implemented in the
uncommitted working tree: the unified `skills/mcp-adapter/SKILL.md` with 4 reference files, the
`bin/kilo-mcp-server.ts` resolver fix, deprecation banners on 3 old skills, and 6 special-cases.md
registry entries. TypeScript compilation passes, all 590 tests pass, and Kilo deployment verification
passes.

**This plan addresses the 3 critical gaps discovered by the researcher:**

1. **Missing file**: `skills/mcp-adapter/references/deploy-examples.md` is referenced by SKILL.md
   (line 206) and deploy.md (lines 49, 105, 113) but does not exist. Source is at
   `skills/deploy-mcp-adapter/references/deploy-examples.md` (402 lines).

2. **Broken anchors**: deploy.md lines 105 and 113 reference `#custom-agent-integration` but the
   source file's heading is `## Branch C: Custom Agent`, which generates GitHub anchor
   `#branch-c-custom-agent`.

3. **Missing registry entry**: `deploy-examples.md` is not in `special-cases.md` (grep returns 0
   matches). This causes `npm run upstream:check` to report it as an unregistered fork-only file.

4. **Uncommitted state**: All Phase 11 changes (prior work + these gap fixes) must be committed for
   `npm run upstream:check` to recognize the new fork-only files and exit 0.

Purpose: Complete Phase 11 to a shippable state with zero broken references, full upstream-merge
registry coverage, and all changes committed.

Output: `skills/mcp-adapter/references/deploy-examples.md` (new, migrated), fixed `deploy.md`
(anchor corrections), updated `special-cases.md` (+1 entry), and a git commit containing all
Phase 11 changes.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skill-unification-and-fixes/CONTEXT.md
@.planning/phases/11-skill-unification-and-fixes/RESEARCH.md
@.planning/ROADMAP.md
@skills/mcp-adapter/SKILL.md
@skills/mcp-adapter/references/deploy.md
@skills/mcp-adapter/references/resolver.md
@skills/deploy-mcp-adapter/references/deploy-examples.md
@skills/upstream-merge/references/special-cases.md
@bin/kilo-mcp-server.ts
@interfaces/agent-api.ts
@interfaces/agent-paths.ts
</context>

<tasks>

<!-- ================================================================== -->
<!-- TRACEABILITY: Verification of prior work (already complete)         -->
<!-- These tasks confirm the working tree matches RESEARCH.md findings.  -->
<!-- They are READ-ONLY — no file modifications.                          -->
<!-- ================================================================== -->

<task type="auto">
  <name>Task 1: [VERIFICATION] Confirm prior Phase 11 work — DEC-01 through DEC-05 (ALREADY COMPLETE)</name>
  <files>[]</files>
  <action>
    **This task is verification-only. All work listed below is already present in the uncommitted
    working tree. Do NOT modify any files. Run the verification commands and confirm each check
    passes. If any check fails, STOP and report the discrepancy — do not attempt to fix it here
    (the gap-fixing tasks below handle remaining work).**

    Verify the following already-completed work (per RESEARCH.md §Summary and §Code Examples):

    **DEC-04 — kilo-mcp-server.ts resolver fix (ALREADY DONE):**
    - Confirm `bin/kilo-mcp-server.ts` imports `createKiloResolver` from `../interfaces/agent-paths.ts`
      (line 33) and passes `kiloResolver.globalConfigPath()` as third argument to `loadMcpConfig()`
      (lines 115-117).
    - Run: `npm run verify:deploy -- --agent kilo` → must output "✅ Kilo verification passed".

    **DEC-01 — Unified mcp-adapter skill (ALREADY DONE):**
    - Confirm `skills/mcp-adapter/SKILL.md` exists (207 lines) with Phase 0-3 structure.
    - Confirm 4 reference files exist in `skills/mcp-adapter/references/`: `generate.md`,
      `deploy.md`, `verify.md`, `resolver.md`.
    - Run: `ls skills/mcp-adapter/SKILL.md skills/mcp-adapter/references/generate.md
      skills/mcp-adapter/references/deploy.md skills/mcp-adapter/references/verify.md
      skills/mcp-adapter/references/resolver.md` → all 5 files must exist.

    **DEC-02 — Dynamic resolver (ALREADY DONE):**
    - Confirm `skills/mcp-adapter/references/resolver.md` uses `AGENT_ADAPTERS[i].resolverFactory()`
      and does NOT contain active hardcoded `agent-paths/<id>.md` references.
    - Run: `grep -r "agent-paths/<id>" skills/mcp-adapter/ | grep -v "replaces the legacy" | grep -v "Phase 11 DEC-02" | wc -l` → must return 0.
      (Note: resolver.md line 6 contains a deprecation description referencing the old pattern —
      this is correct documentation, not active hardcoding. The grep excludes these prose lines.)

    **DEC-03 — Capability-gate (ALREADY DONE):**
    - Confirm `skills/mcp-adapter/SKILL.md` contains capability-gate logic in Phase 0.
    - Run: `grep -ci "capability" skills/mcp-adapter/SKILL.md` → must return > 0.

    **DEC-05 (partial) — special-cases.md registry (ALREADY DONE for 6 entries, GAP for deploy-examples.md):**
    - Confirm 6 Phase 11 entries exist in `skills/upstream-merge/references/special-cases.md`
      (lines 32-37): SKILL.md, generate.md, deploy.md, verify.md, resolver.md, agent-paths/README.md.
    - Run: `grep -c "Phase 11" skills/upstream-merge/references/special-cases.md` → must return ≥ 6.

    **Deprecation banners (ALREADY DONE):**
    - Confirm all 3 old skills have deprecation banners at top (after frontmatter).
    - Run: `grep -l "DEPRECATED" skills/deploy-mcp-adapter/SKILL.md
      skills/generate-mcp-config/SKILL.md skills/mcp-adapter-test/SKILL.md` → 3 files listed.

    **README.md (ALREADY DONE):**
    - Confirm README.md contains Kilo/Qoder capability explanation.
    - Run: `grep -ci "kilo" README.md` → must return > 0.

    **TypeScript + tests (ALREADY PASSING):**
    - Run: `npx tsc --noEmit` → exit 0, zero errors.
    - Run: `npm test` → 590 passed, 10 skipped.

    **Document results in the execution log. If all checks pass, proceed to Task 2.**
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3 && npm run verify:deploy -- --agent kilo 2>&1 | tail -3</automated>
  </verify>
  <done>
    All 8 verification checks pass. Working tree confirmed to contain all DEC-01 through DEC-04
    work and 6/7 DEC-05 registry entries. The only remaining gaps are: missing deploy-examples.md,
    broken anchors in deploy.md, and missing deploy-examples.md registry entry — all addressed in
    Tasks 2-4 below.
  </done>
</task>

<!-- ================================================================== -->
<!-- GAP CLOSURE: Remaining work                                        -->
<!-- ================================================================== -->

<task type="auto">
  <name>Task 2: Migrate deploy-examples.md to unified skill (DEC-01 gap fix)</name>
  <files>skills/mcp-adapter/references/deploy-examples.md</files>
  <action>
    **Copy the file verbatim — do not rewrite or adapt content.**

    Copy `skills/deploy-mcp-adapter/references/deploy-examples.md` (402 lines) to
    `skills/mcp-adapter/references/deploy-examples.md`.

    The source file contains:
    - Branch A: Pi native install (line 20)
    - Branch B: SDK bridge + SessionStart hook (line 33), with sub-sections:
      `### Qoder Integration Entry Point` (line 38), Qoder Config Path Resolution (line 100),
      Qoder UI Capabilities (line 110), Qoder SessionStart Hook Registration (line 122)
    - Branch C / Strategy A: MCP stdio server (line 147), with Kilo sub-section (line 154)
    - Branch C: Custom Agent (line 238), with Registration Flow Summary (line 345)
    - npm Package Reference (line 363)
    - AgentChannel pattern (line 378)

    **Why verbatim copy:** The file is user-facing documentation showing npm package usage with
    `pi-mcp-adapter` as the published package name. Import paths (e.g.,
    `import { createQoderResolver } from "pi-mcp-adapter/interfaces/agent-paths.ts"`) reference the
    published package, not repo-internal paths. No changes needed. (RESEARCH.md §Open Questions Q1
    confirms this recommendation.)

    **GitNexus note:** This is a new `.md` file — no symbol impact analysis needed per AGENTS.md
    (GitNexus applies to `.ts` functions/classes/methods, not documentation files).

    After copying, verify the file exists and has the expected headings:
    `grep -c "^##" skills/mcp-adapter/references/deploy-examples.md` → should return ≥ 5.
  </action>
  <verify>
    <automated>test -f skills/mcp-adapter/references/deploy-examples.md && wc -l skills/mcp-adapter/references/deploy-examples.md | grep -q "40[0-9]" && grep -q "## Branch C: Custom Agent" skills/mcp-adapter/references/deploy-examples.md && grep -q "### Qoder Integration Entry Point" skills/mcp-adapter/references/deploy-examples.md && echo "OK"</automated>
  </verify>
  <done>
    `skills/mcp-adapter/references/deploy-examples.md` exists with ~402 lines. The heading
    `## Branch C: Custom Agent` is present (generates GitHub anchor `#branch-c-custom-agent`).
    The `### Qoder Integration Entry Point` heading is present (generates anchor
    `#qoder-integration-entry-point`). SKILL.md line 206 reference and deploy.md line 49 reference
    now resolve to actual content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix broken anchor links in deploy.md (lines 105, 113)</name>
  <files>skills/mcp-adapter/references/deploy.md</files>
  <action>
    **Fix 2 broken anchor references in `skills/mcp-adapter/references/deploy.md`.**

    The file references `deploy-examples.md#custom-agent-integration` on lines 105 and 113, but the
    source heading is `## Branch C: Custom Agent`, which generates GitHub anchor
    `#branch-c-custom-agent`. The anchor `#custom-agent-integration` does not match any heading.

    **Line 105** (current):
    ```
    8 required methods. Full guide: [deploy-examples.md](deploy-examples.md#custom-agent-integration)
    ```
    **Change to:**
    ```
    8 required methods. Full guide: [deploy-examples.md](deploy-examples.md#branch-c-custom-agent)
    ```

    **Line 113** (current):
    ```
    Same universal flow. See [deploy-examples.md](deploy-examples.md#custom-agent-integration)
    ```
    **Change to:**
    ```
    Same universal flow. See [deploy-examples.md](deploy-examples.md#branch-c-custom-agent)
    ```

    **Do NOT change line 49** — it references `deploy-examples.md#qoder-integration-entry-point`,
    which correctly matches the `### Qoder Integration Entry Point` heading (GitHub slugifies to
    `qoder-integration-entry-point`). This anchor is correct.

    After editing, verify zero broken anchors remain:
    `grep -n "custom-agent-integration" skills/mcp-adapter/references/deploy.md` → must return 0 matches.
    `grep -n "branch-c-custom-agent" skills/mcp-adapter/references/deploy.md` → must return 2 matches.

    **GitNexus note:** This is a `.md` file edit — no symbol impact analysis needed per AGENTS.md.
  </action>
  <verify>
    <automated>! grep -q "custom-agent-integration" skills/mcp-adapter/references/deploy.md && [ "$(grep -c "branch-c-custom-agent" skills/mcp-adapter/references/deploy.md)" = "2" ] && echo "OK"</automated>
  </verify>
  <done>
    deploy.md contains zero references to `#custom-agent-integration`. Both lines 105 and 113 now
    reference `#branch-c-custom-agent`, which resolves to the `## Branch C: Custom Agent` heading
    in the migrated deploy-examples.md. Line 49's `#qoder-integration-entry-point` anchor remains
    unchanged (already correct).
  </done>
</task>

<task type="auto">
  <name>Task 4: Add deploy-examples.md to upstream-merge special-cases.md registry (DEC-05 gap fix)</name>
  <files>skills/upstream-merge/references/special-cases.md</files>
  <action>
    **Add 1 new registry row and update the footer count.**

    In `skills/upstream-merge/references/special-cases.md`, insert a new row after line 36
    (after the `resolver.md` entry, before the `agent-paths/README.md` entry):

    ```markdown
    | `skills/mcp-adapter/references/deploy-examples.md` | `fork-only` | Phase 11: deployment code templates migrated from deploy-mcp-adapter; upstream has no skills/ dir | `ours` |
    ```

    This brings the total from 28 to 29 anchored entries.

    **Update the footer** (line 51) from:
    ```
    2026-06-26 (Phase 11 skill unification + post-Phase-10 fixes; 28 anchored entries — added `skills/mcp-adapter/` + 4 references files). Update this footer on Phase 11+ amendments.
    ```
    to:
    ```
    2026-06-26 (Phase 11 skill unification + post-Phase-10 fixes; 29 anchored entries — added `skills/mcp-adapter/` + 5 references files including deploy-examples.md). Update this footer on Phase 11+ amendments.
    ```

    After editing, verify the entry is parseable:
    `grep -c "deploy-examples.md" skills/upstream-merge/references/special-cases.md` → must return 1.

    **Note on upstream:check:** At this point `npm run upstream:check` will STILL exit 1 because
    the new files are untracked (not yet committed). The stale-entries report will show 7 entries
    (the original 6 + deploy-examples.md). This is expected — Task 6 (commit) resolves it. Do not
    attempt to "fix" the upstream:check failure before committing.

    **GitNexus note:** This is a `.md` file edit — no symbol impact analysis needed per AGENTS.md.
  </action>
  <verify>
    <automated>[ "$(grep -c "deploy-examples.md" skills/upstream-merge/references/special-cases.md)" = "1" ] && grep -q "29 anchored entries" skills/upstream-merge/references/special-cases.md && echo "OK"</automated>
  </verify>
  <done>
    `special-cases.md` contains exactly 1 row for `deploy-examples.md` with status `fork-only` and
    decision `ours`. Footer updated to "29 anchored entries". The registry now covers all 7 Phase 11
    fork-only files (SKILL.md + 5 reference files + agent-paths/README.md).
  </done>
</task>

<task type="auto">
  <name>Task 5: Full verification suite — tsc, test, verify:deploy, upstream:check</name>
  <files>[]</files>
  <action>
    **Run the complete Phase 11 verification suite. All 4 checks must pass.**

    Execute in order:

    1. **TypeScript compilation:**
       `npx tsc --noEmit`
       Expected: exit 0, zero errors.

    2. **Test suite:**
       `npm test`
       Expected: 590 passed, 10 skipped (55 test files). Exit 0.

    3. **Kilo deployment verification (DEC-04 confirmation):**
       `npm run verify:deploy -- --agent kilo`
       Expected: "✅ Kilo verification passed". Exit 0.

    4. **Anchor link integrity (manual grep verification):**
       - `grep -c "custom-agent-integration" skills/mcp-adapter/references/deploy.md` → 0
       - `grep -c "branch-c-custom-agent" skills/mcp-adapter/references/deploy.md` → 2
       - `grep -c "qoder-integration-entry-point" skills/mcp-adapter/references/deploy.md` → 1
       - `test -f skills/mcp-adapter/references/deploy-examples.md` → exists

    5. **Registry completeness:**
       - `grep -c "deploy-examples.md" skills/upstream-merge/references/special-cases.md` → 1
       - `grep -c "Phase 11" skills/upstream-merge/references/special-cases.md` → ≥ 7

    6. **upstream:check — NOTE: will still exit 1 until Task 6 commits the files.**
       Run `npm run upstream:check` and record the output. It should report 7 stale entries
       (the 6 original untracked files + deploy-examples.md). This is EXPECTED and will be
       resolved by Task 6 (commit). Do NOT treat this as a failure at this stage — the stale
       entries are untracked files that the script cannot see in git HEAD.

    If checks 1-5 all pass and check 6 shows the expected 7 stale entries, proceed to Task 6.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3 && npm run verify:deploy -- --agent kilo 2>&1 | tail -3 && [ "$(grep -c 'custom-agent-integration' skills/mcp-adapter/references/deploy.md)" = "0" ] && echo "VERIFY OK"</automated>
  </verify>
  <done>
    tsc exits 0. Tests: 590 passed, 10 skipped. verify:deploy kilo passes. Zero broken anchors in
    deploy.md. deploy-examples.md exists with correct headings. special-cases.md has 1 entry for
    deploy-examples.md. upstream:check shows 7 stale entries (expected pre-commit state).
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit all Phase 11 changes</name>
  <files>[]</files>
  <action>
    **Commit all uncommitted Phase 11 work in a single commit.**

    This commit includes:
    - **Prior work (already in working tree):** bin/kilo-mcp-server.ts (DEC-04),
      skills/mcp-adapter/SKILL.md + 4 reference files (DEC-01/02/03), 3 deprecation banners,
      6 special-cases.md entries (DEC-05 partial), README.md, agent-paths/README.md.
    - **Gap fixes (Tasks 2-4):** skills/mcp-adapter/references/deploy-examples.md (new),
      skills/mcp-adapter/references/deploy.md (anchor fixes),
      skills/upstream-merge/references/special-cases.md (+1 entry, footer update).

    **Note:** `git status` will show modified AGENTS.md, CLAUDE.md, and .planning/ROADMAP.md —
    these are NOT Phase 11 changes and are intentionally excluded from this commit.

    Steps:

    1. **Stage all Phase 11 files:**
       ```bash
       git add bin/kilo-mcp-server.ts \
         skills/mcp-adapter/ \
         skills/deploy-mcp-adapter/SKILL.md \
         skills/generate-mcp-config/SKILL.md \
         skills/mcp-adapter-test/SKILL.md \
         skills/mcp-adapter-test/references/agent-paths/README.md \
         skills/upstream-merge/references/special-cases.md \
         README.md
       ```

    2. **GitNexus pre-commit check (per AGENTS.md):**
       Run `gitnexus_detect_changes()` to verify the changes only affect expected symbols and
       execution flows. Since the only `.ts` change is `bin/kilo-mcp-server.ts` (1-line resolver
       fix already verified), and all other changes are `.md` files, the blast radius should be
       minimal. If GitNexus is unavailable (not indexed), skip this step and note it in the commit
       message body.

    3. **Commit:**
       ```bash
       git commit -m "feat(11): unify skills, fix kilo resolver, close deploy-examples gap

    Phase 11: Skill Unification & Post-Phase-10 Fixes

    DEC-01: Unified mcp-adapter skill (SKILL.md + 5 reference files)
    DEC-02: Dynamic resolver via AGENT_ADAPTERS[i].resolverFactory()
    DEC-03: Capability-gate at Phase 0 entry
    DEC-04: kilo-mcp-server.ts uses createKiloResolver() (was Pi default)
    DEC-05: 7 fork-only entries in special-cases.md registry

    Gap fixes (this commit):
    - Migrated deploy-examples.md (402 lines) from deploy-mcp-adapter/references/
    - Fixed broken anchor #custom-agent-integration → #branch-c-custom-agent (deploy.md:105,113)
    - Added deploy-examples.md to special-cases.md registry (entry 29/29)

    Verification: tsc 0 errors, 590 tests pass, verify:deploy kilo passes, upstream:check exit 0"
       ```

    4. **Post-commit upstream:check:**
       `npm run upstream:check`
       Expected: exit 0, zero stale entries. All 7 fork-only files are now tracked in git HEAD.

    If `upstream:check` still exits non-zero after commit, investigate:
    - Check if any Phase 11 file was missed in `git add` (run `git status --short`)
    - Check if the special-cases.md row format is parseable (see RESEARCH.md Pitfall 4)
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "feat(11)" && npm run upstream:check 2>&1 | tail -3 && echo "COMMIT OK"</automated>
  </verify>
  <done>
    All Phase 11 changes committed in a single `feat(11):` commit. `npm run upstream:check` exits 0
    with zero stale entries. `git status --short` shows clean working tree (no untracked Phase 11
    files). The unified mcp-adapter skill is fully functional with zero broken references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill docs → AI agent consumer | Markdown files are read by AI agents at runtime; broken references cause user-facing 404s |
| Fork → upstream merge | special-cases.md registry must accurately track all fork-only files to prevent merge conflicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | deploy-examples.md content | accept | File contains only public npm package usage examples and local filesystem paths; no secrets, tokens, or PII |
| T-11-02 | Tampering | special-cases.md registry | mitigate | Registry row uses exact 4-column schema (`| path | status | why | decision |`); `npm run upstream:check` validates parseability post-commit |
| T-11-03 | Spoofing | deploy.md anchor links | mitigate | Anchor links now match actual GitHub-generated slugs from heading text; verified via grep that zero `#custom-agent-integration` references remain |
| T-11-04 | Repudiation | git commit | mitigate | Single `feat(11):` commit with structured body listing all DEC-01 through DEC-05 coverage for audit traceability |
| T-11-SC | Tampering | npm install | accept | Zero new npm packages installed in Phase 11; all changes are `.ts` (1-line fix) and `.md` files — no supply-chain risk |
</threat_model>

<verification>
## Phase 11 Final Verification Checklist

1. **TypeScript:** `npx tsc --noEmit` → exit 0, zero errors
2. **Tests:** `npm test` → 590 passed, 10 skipped, exit 0
3. **Kilo deploy:** `npm run verify:deploy -- --agent kilo` → "✅ Kilo verification passed"
4. **upstream:check:** `npm run upstream:check` → exit 0, zero stale entries (post-commit)
5. **File existence:** `ls skills/mcp-adapter/references/deploy-examples.md` → exists (402 lines)
6. **Anchor integrity:** `grep -c "custom-agent-integration" skills/mcp-adapter/references/deploy.md` → 0
7. **Registry completeness:** `grep -c "deploy-examples.md" skills/upstream-merge/references/special-cases.md` → 1
8. **Git clean:** `git status --short` → no untracked Phase 11 files
9. **Commit present:** `git log --oneline -1` → shows `feat(11):` commit
</verification>

<rollback>
## Rollback Strategy

**Scenario:** Phase 11 commit needs to be reverted.

**Single-commit rollback (preferred):**
```bash
git revert HEAD --no-edit
```
This reverts the entire `feat(11):` commit, restoring all files to their pre-Phase-11 state.

**Selective rollback (if only gap fixes need reverting):**
1. Delete the migrated file: `rm skills/mcp-adapter/references/deploy-examples.md`
2. Revert deploy.md anchors: change `#branch-c-custom-agent` back to `#custom-agent-integration` on lines 105, 113
3. Remove deploy-examples.md row from special-cases.md (revert footer to "28 anchored entries")
4. Amend the commit or create a new fixup commit

**Note:** The prior Phase 11 work (DEC-01 through DEC-04, deprecation banners, README) was already
in the working tree before this plan. If the entire Phase 11 needs to be undone, use
`git revert HEAD` (reverts the commit) followed by `git checkout -- <files>` for any files that
were modified but not yet committed (should be none after Task 6).

**Risk assessment:** Low. All changes are additive (new `.md` files, deprecation banners) or
1-line fixes (kilo-mcp-server.ts, anchor links). No database migrations, no config breaking
changes, no API contract modifications.
</rollback>

<success_criteria>
- [ ] `skills/mcp-adapter/references/deploy-examples.md` exists with ~402 lines (migrated verbatim)
- [ ] `deploy.md` lines 105 and 113 reference `#branch-c-custom-agent` (not `#custom-agent-integration`)
- [ ] `deploy.md` line 49 still references `#qoder-integration-entry-point` (unchanged, correct)
- [ ] `special-cases.md` contains 1 row for `deploy-examples.md` with `fork-only` / `ours`
- [ ] `special-cases.md` footer updated to "29 anchored entries"
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm test` passes (590 passed, 10 skipped)
- [ ] `npm run verify:deploy -- --agent kilo` passes
- [ ] `npm run upstream:check` exits 0 (post-commit)
- [ ] All Phase 11 changes committed in a single `feat(11):` commit
- [ ] `git status --short` shows no untracked Phase 11 files
</success_criteria>

<output>
Create `.planning/phases/11-skill-unification-and-fixes/11-01-SUMMARY.md` when done
</output>
