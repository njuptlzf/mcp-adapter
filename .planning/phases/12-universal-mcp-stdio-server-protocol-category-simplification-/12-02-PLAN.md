---
phase: 12-universal-mcp-stdio-server-protocol-category-simplification-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - interfaces/agent-api.ts
  - interfaces/agent-paths.ts
autonomous: true
requirements: [D-01, D-02]

must_haves:
  truths:
    - "AGENT_ADAPTERS contains exactly 2 entries: universal-mcp and pi"
    - "universal-mcp entry's factory returns a valid AgentAPI with all 8 required methods (registerTool, registerCommand, registerFlag, on, getAllTools, getFlag, sendMessage, exec)"
    - "createUniversalResolver returns ~/.config/mcp/mcp.json as global config path and .mcp.json as project config name"
    - "No imports of KiloAdapter, QoderAdapter, StoreAgentAdapter, adaptKiloContext, adaptQoderContext in agent-api.ts"
    - "createKiloResolver and createQoderResolver still exist (removed in Plan 03 when consumers are deleted)"
  artifacts:
    - path: "interfaces/agent-api.ts"
      provides: "Simplified AGENT_ADAPTERS registry with universal-mcp + pi entries"
      contains: "universal-mcp"
      min_lines: 200
    - path: "interfaces/agent-paths.ts"
      provides: "createUniversalResolver function for universal config path discovery"
      contains: "createUniversalResolver"
      min_lines: 80
  key_links:
    - from: "interfaces/agent-api.ts"
      to: "interfaces/agent-paths.ts"
      via: "imports createUniversalResolver"
      pattern: "createUniversalResolver"
    - from: "AGENT_ADAPTERS[universal-mcp].factory"
      to: "AgentAPI interface"
      via: "returns object implementing all 8 AgentAPI methods"
      pattern: "registerTool.*registerCommand.*registerFlag"
    - from: "AGENT_ADAPTERS[universal-mcp].resolverFactory"
      to: "createUniversalResolver"
      via: "factory function"
      pattern: "resolverFactory.*createUniversalResolver"
---

<objective>
Simplify the AGENT_ADAPTERS registry to two entries (universal-mcp + pi) and add createUniversalResolver for agent-agnostic config path discovery.

Purpose: This is the registry-level foundation for Phase 12's protocol-category simplification. The universal-mcp entry replaces the deleted Kilo and Qoder entries (D-01). The universal resolver eliminates agent-specific config paths (D-02). The universal-mcp entry's factory provides an inline AgentAPI implementation per D-04 — no shared base class, no StoreAgentAdapter import.

Output: Two modified interface files. The codebase still compiles after this plan because old adapter files still exist (deleted in Plan 03) and old resolvers still exist (removed in Plan 03 when their consumers are deleted).
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md
@interfaces/agent-api.ts
@interfaces/agent-paths.ts
@adapters/store-adapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createUniversalResolver to interfaces/agent-paths.ts</name>
  <files>interfaces/agent-paths.ts</files>
  <read_first>
    - interfaces/agent-paths.ts (full file — current resolvers: createPiResolver, createQoderResolver, createKiloResolver, resolveQoderGlobalConfigPath, resolveEnvAgentDir, DEFAULT_AGENT_RESOLVER, resolveAgentGlobalConfigPath)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 713-729 (createUniversalResolver code example)
    - config.ts (to understand how globalConfigPath is consumed by loadMcpConfig)
  </read_first>
  <action>
    Add `createUniversalResolver` function to interfaces/agent-paths.ts per D-02. Do NOT remove existing createKiloResolver, createQoderResolver, or resolveQoderGlobalConfigPath yet — they are still imported by bin/kilo-mcp-server.ts and bin/qoder-mcp-bridge.ts (deleted in Plan 03).

    Add after the existing createKiloResolver function (before DEFAULT_AGENT_RESOLVER):

    Export function `createUniversalResolver()` returning `AgentPathResolver` with:
    - `agentId: "universal-mcp"`
    - `globalConfigPath: () => join(homedir(), ".config", "mcp", "mcp.json")` — per D-02: shared global path, no agent-specific directory
    - `projectConfigName: () => ".mcp.json"` — per D-02: .mcp.json in cwd

    Use existing imports `join` from `node:path` and `homedir` from `node:os` (already at top of file). Do NOT use resolveEnvAgentDir — the universal resolver has no env var override per D-02 (MCP_CONFIG_PATH is handled by the caller, not the resolver).

    Add JSDoc comment: "Universal resolver for the mcp-adapter universal MCP stdio server. Per D-02: config path discovery is --config > MCP_CONFIG_PATH > .mcp.json in cwd > ~/.config/mcp/mcp.json. No agent-specific global paths."

    Do NOT modify any existing function — only ADD createUniversalResolver.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "createUniversalResolver" interfaces/agent-paths.ts | grep -v '^#' | head -1 | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - `createUniversalResolver` function exists in interfaces/agent-paths.ts and is exported
    - `createUniversalResolver().agentId` equals "universal-mcp"
    - `createUniversalResolver().globalConfigPath()` returns path ending with ".config/mcp/mcp.json"
    - `createUniversalResolver().projectConfigName()` returns ".mcp.json"
    - Existing functions (createPiResolver, createKiloResolver, createQoderResolver) are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>createUniversalResolver is exported from interfaces/agent-paths.ts with correct paths per D-02. tsc passes. No existing functions modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simplify AGENT_ADAPTERS registry in interfaces/agent-api.ts</name>
  <files>interfaces/agent-api.ts</files>
  <read_first>
    - interfaces/agent-api.ts (full file — current AGENT_ADAPTERS array with kilo, pi, qoder entries; imports of KiloAdapter, QoderAdapter, adaptKiloContext, adaptQoderContext, createKiloResolver, createQoderResolver)
    - interfaces/agent-paths.ts (after Task 1 — now has createUniversalResolver)
    - adapters/store-adapter.ts lines 78-284 (StoreAgentAdapter implementation — reference for inline AgentAPI pattern: tools/commands/flags Maps, on/getAllTools/getFlag/sendMessage/exec methods, fire method, attachChannel, error logging pattern T-10-01)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 670-711 (AGENT_ADAPTERS simplified code example)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md D-04 (delete ALL per-agent adapter code including base class — each entry point has its own inline implementation)
    - __tests__/adapter-contract.test.ts lines 1-40 (understand what the parametric contract test expects from each AGENT_ADAPTERS entry)
  </read_first>
  <behavior>
    - AGENT_ADAPTERS array has exactly 2 entries: universal-mcp (first) and pi (second)
    - universal-mcp entry factory returns an object implementing all 8 AgentAPI methods
    - universal-mcp entry factory does NOT import or extend StoreAgentAdapter (D-04: inline implementation)
    - universal-mcp entry has resolverFactory: createUniversalResolver
    - universal-mcp entry has envHints: [{ envVar: "MCP_CONFIG_PATH" }]
    - universal-mcp entry has capabilities: { ui: false, sampling: false, renderer: false } (static; actual capabilities are runtime-discovered)
    - universal-mcp entry has createVerificationContext that returns { cwd: input.cwd, hasUI: input.hasUI }
    - pi entry is unchanged (same factory, resolverFactory, envHints, capabilities)
    - No imports of KiloAdapter, QoderAdapter, adaptKiloContext, adaptQoderContext anywhere in the file
    - createKiloResolver and createQoderResolver are NOT imported (only createPiResolver and createUniversalResolver)
  </behavior>
  <action>
    Modify interfaces/agent-api.ts per D-01, D-02, D-04:

    1. **Update imports** (line 20): Replace `import { createKiloResolver, createPiResolver, createQoderResolver } from "./agent-paths.ts"` with `import { createPiResolver, createUniversalResolver } from "./agent-paths.ts"`.

    2. **Remove adapter imports** (lines 22-25): Delete the 4 lines importing `QoderAdapter`, `adaptQoderContext`, `KiloAdapter`, `adaptKiloContext` from their respective adapter files. Keep the `PiAdapter` import (line 21).

    3. **Replace AGENT_ADAPTERS array** (lines 197-248): Remove the `kilo` and `qoder` entries entirely. Replace with a new `universal-mcp` entry as the FIRST element, followed by the existing `pi` entry (unchanged).

    The universal-mcp entry (per D-04: inline AgentAPI, no shared base class):
    - id: "universal-mcp"
    - displayName: "Universal MCP"
    - factory: An inline arrow function that creates and returns an object implementing AgentAPI. This inline implementation provides:
      - `tools = new Map<string, ToolRegistration>()` (public readonly)
      - `commands = new Map<string, CommandConfig>()` (public readonly)
      - `flags = new Map<string, FlagConfig & { value?: string }>()` (public readonly)
      - `handlers = new Map<string, Set<(...args: unknown[]) => unknown>>()` (public readonly)
      - `registerTool(tool)`: tools.set(tool.name, tool)
      - `registerCommand(name, config)`: commands.set(name, config)
      - `registerFlag(name, config)`: flags.set(name, { ...config })
      - `on(event, handler)`: create Set if not exists, add handler
      - `getAllTools()`: return [...tools.values()].map(t => ({ name: t.name }))
      - `getFlag(name)`: return flags.get(name)?.value
      - `sendMessage(message, options)`: no-op (messages go to stderr in bin/mcp-server.ts, not here — this factory is for testing)
      - `exec(command, args)`: return Promise.resolve({ code: 0, stdout: "", stderr: "" }) (mock for testing)
      - Also include `attachChannel(channel)` and `fireSessionStart(ctx)` and `fireSessionShutdown()` as no-op stubs (for contract test compatibility — createMcpAdapter calls adapter.on("session_start", ...) and adapter.fireSessionStart is not AgentAPI but may be called by tests)
      - T-10-01 pattern: error logging uses prefix only, never args
    - resolverFactory: createUniversalResolver
    - envHints: [{ envVar: "MCP_CONFIG_PATH" }]
    - capabilities: { ui: false, sampling: false, renderer: false }
    - createVerificationContext: `(input, _adapter) => ({ cwd: input.cwd, hasUI: input.hasUI })`

    Keep the existing `pi` entry unchanged (lines 208-237 in current file).

    4. **Update AgentId type** (optional): In interfaces/agent-paths.ts, the AgentId type can stay as-is since `(string & {})` already accepts "universal-mcp". No change needed unless you want to document it.

    IMPORTANT: Do NOT delete any adapter files in this task. The old files (kilo-adapter.ts, qoder-adapter.ts, store-adapter.ts) still exist and still compile — they're just no longer imported by agent-api.ts. They will be deleted in Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run __tests__/adapter-contract.test.ts -x && grep -c "KiloAdapter\|QoderAdapter\|adaptKiloContext\|adaptQoderContext" interfaces/agent-api.ts | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - AGENT_ADAPTERS array has exactly 2 entries: first is "universal-mcp", second is "pi"
    - No imports of KiloAdapter, QoderAdapter, adaptKiloContext, adaptQoderContext in interfaces/agent-api.ts (grep returns 0)
    - createKiloResolver and createQoderResolver are NOT imported in interfaces/agent-api.ts
    - createUniversalResolver IS imported in interfaces/agent-api.ts
    - universal-mcp entry's factory function returns an object with all 8 AgentAPI methods
    - universal-mcp entry's factory does NOT import or reference StoreAgentAdapter
    - `npx tsc --noEmit` exits 0
    - `npx vitest run __tests__/adapter-contract.test.ts -x` passes (parametric test auto-adapts to new registry)
    - `npx vitest run __tests__/capability-gate.test.ts -x` passes (parametric test auto-adapts)
  </acceptance_criteria>
  <done>AGENT_ADAPTERS has universal-mcp + pi only. universal-mcp factory provides inline AgentAPI. createUniversalResolver wired. tsc + parametric tests pass. No per-agent adapter imports remain in agent-api.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → AGENT_ADAPTERS factory | Parametric tests instantiate adapters from registry; inline factory must produce safe objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | Inline AgentAPI factory | mitigate | Factory returns fresh object per call (no shared state between test instances). Maps are instance-level, not module-level |
| T-12-06 | Information Disclosure | exec() mock in factory | accept | Mock exec returns { code: 0, stdout: "", stderr: "" } — no real process spawned in test context. T-10-02 pattern preserved |
| T-12-SC | Tampering | npm installs | accept | No new packages — all dependencies existing and verified |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run __tests__/adapter-contract.test.ts -x` passes
- `npx vitest run __tests__/capability-gate.test.ts -x` passes
- `grep -c "KiloAdapter\|QoderAdapter\|StoreAgentAdapter\|adaptKiloContext\|adaptQoderContext" interfaces/agent-api.ts` returns 0
- `grep -c "createUniversalResolver" interfaces/agent-api.ts` returns at least 1
- `grep "universal-mcp" interfaces/agent-api.ts` returns at least 2 (id + createVerificationContext or factory reference)
- `grep "id:" interfaces/agent-api.ts | grep -v '^#' | wc -l` returns 2 (only universal-mcp and pi)
</verification>

<success_criteria>
- AGENT_ADAPTERS simplified to universal-mcp + pi per D-01
- createUniversalResolver added per D-02
- Inline AgentAPI factory for universal-mcp entry per D-04
- All parametric tests pass with the new registry
- tsc clean
- No per-agent adapter imports remain in agent-api.ts
</success_criteria>

<output>
Create `.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New Symbols

| File | Symbol | Type | Description |
|------|--------|------|-------------|
| interfaces/agent-paths.ts | `createUniversalResolver` | function | Returns AgentPathResolver with agentId "universal-mcp", globalConfigPath ~/.config/mcp/mcp.json, projectConfigName .mcp.json (D-02) |

### Modified Symbols

| File | Symbol | Change |
|------|--------|--------|
| interfaces/agent-api.ts | `AGENT_ADAPTERS` | Removed kilo and qoder entries; added universal-mcp entry with inline AgentAPI factory (D-01, D-04) |
| interfaces/agent-api.ts | imports | Removed imports of KiloAdapter, QoderAdapter, adaptKiloContext, adaptQoderContext, createKiloResolver, createQoderResolver; added createUniversalResolver import |
