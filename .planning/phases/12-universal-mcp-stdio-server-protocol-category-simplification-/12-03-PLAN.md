---
phase: 12-universal-mcp-stdio-server-protocol-category-simplification-
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - bin/mcp-server.ts
  - package.json
  - vitest.config.ts
  - interfaces/agent-paths.ts
  - __tests__/package-manifest.test.ts
files_deleted:
  - bin/kilo-mcp-server.ts
  - bin/qoder-mcp-bridge.ts
  - adapters/kilo-adapter.ts
  - adapters/qoder-adapter.ts
  - adapters/store-adapter.ts
  - adapters/qoder-sampling-provider.ts
  - adapters/qoder-renderer.ts
  - scripts/kilo-mcp-entry.ts
  - scripts/qoder-smoke.ts
  - __tests__/qoder-adapter.test.ts
  - __tests__/qoder-adapter-integration.test.ts
  - __tests__/qoder-sampling-provider.test.ts
  - __tests__/store-adapter.test.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-09, D-10, D-11]

must_haves:
  truths:
    - "bin/mcp-server.ts exists and is agent-agnostic (does not import any per-agent adapter)"
    - "bin/kilo-mcp-server.ts no longer exists"
    - "MCP Server is created and connected BEFORE fireSessionStart() (Pitfall 1: flow reordering)"
    - "Client capabilities checked via server.getClientCapabilities() NOT server.getCapabilities() (Pitfall 5)"
    - "ProtocolSamplingForwarder injected into ctx.samplingProvider when client declares sampling capability"
    - "ProtocolElicitationForwarder injected into ctx.ui.form when client declares elicitation.form capability"
    - "ctx.hasUI set to true when any forwarder is injected (satisfies init.ts conditions per Pitfall 4)"
    - "All per-agent adapter files deleted: kilo-adapter.ts, qoder-adapter.ts, store-adapter.ts, qoder-sampling-provider.ts, qoder-renderer.ts, qoder-mcp-bridge.ts, kilo-mcp-entry.ts, qoder-smoke.ts"
    - "All per-adapter test files deleted: qoder-adapter.test.ts, qoder-adapter-integration.test.ts, qoder-sampling-provider.test.ts, store-adapter.test.ts"
    - "package.json bin has only mcp-server entry (no kilo-mcp-server, no qoder-mcp-bridge)"
    - "vitest.config.ts has no coverage thresholds for deleted files"
    - "createKiloResolver and createQoderResolver removed from interfaces/agent-paths.ts"
  artifacts:
    - path: "bin/mcp-server.ts"
      provides: "Universal MCP stdio server with reordered flow, inline AgentAPI, capability discovery, forwarder injection"
      min_lines: 120
    - path: "package.json"
      provides: "Single bin entry: mcp-server"
      contains: "mcp-server"
    - path: "vitest.config.ts"
      provides: "Coverage thresholds without deleted file entries"
  key_links:
    - from: "bin/mcp-server.ts"
      to: "adapters/protocol-sampling-forwarder.ts"
      via: "imports ProtocolSamplingForwarder"
      pattern: "ProtocolSamplingForwarder"
    - from: "bin/mcp-server.ts"
      to: "adapters/protocol-elicitation-forwarder.ts"
      via: "imports ProtocolElicitationForwarder"
      pattern: "ProtocolElicitationForwarder"
    - from: "bin/mcp-server.ts"
      to: "interfaces/agent-paths.ts"
      via: "imports createUniversalResolver"
      pattern: "createUniversalResolver"
    - from: "bin/mcp-server.ts"
      to: "adapters/entry.ts"
      via: "imports createMcpAdapter"
      pattern: "createMcpAdapter"
    - from: "bin/mcp-server.ts"
      to: "@modelcontextprotocol/sdk/server/index.js"
      via: "server.getClientCapabilities()"
      pattern: "getClientCapabilities"
---

<objective>
Create the universal MCP stdio server (bin/mcp-server.ts) with the reordered flow, inline AgentAPI implementation, runtime capability discovery, and protocol forwarder injection. Then delete all per-agent adapter files, per-adapter test files, and legacy scripts. Update package.json bin and vitest.config.ts.

Purpose: This is the central integration plan of Phase 12. It creates the agent-agnostic server that replaces kilo-mcp-server.ts (D-05), implements the critical flow reordering (Pitfall 1), uses getClientCapabilities() for capability discovery (Pitfall 5), and injects forwarders when the Agent Client declares sampling/elicitation (D-06, D-07, D-11). All per-agent code is deleted per D-04.

Output: One new server file, two modified config files, one modified interface file, and ~13 file deletions.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-01-SUMMARY.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-02-SUMMARY.md
@bin/kilo-mcp-server.ts
@adapters/store-adapter.ts
@init.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/mcp-server.ts with reordered flow + inline AgentAPI + capability discovery + forwarder injection</name>
  <files>bin/mcp-server.ts</files>
  <read_first>
    - bin/kilo-mcp-server.ts (full file — current flow to be reversed: parseArgs, config, adapter, createMcpAdapter, attachChannel, fireSessionStart, collect tools, create Server, setRequestHandler, connect transport)
    - adapters/protocol-sampling-forwarder.ts (from Plan 01 — ProtocolSamplingForwarder class)
    - adapters/protocol-elicitation-forwarder.ts (from Plan 01 — ProtocolElicitationForwarder class)
    - adapters/store-adapter.ts lines 78-284 (reference for inline AgentAPI: tools/commands/flags Maps, on/getAllTools/getFlag/sendMessage/exec, fire method, attachChannel, T-10-01 error logging pattern)
    - interfaces/agent-paths.ts (after Plan 02 — has createUniversalResolver)
    - init.ts lines 36-54 (sampling/elicitation enablement conditions — understand what ctx fields must be set for forwarders to work)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 355-483 (bin/mcp-server.ts code example with reordered flow)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 304-318 (Pitfall 1: flow ordering — connect BEFORE fireSessionStart)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 339-345 (Pitfall 5: getClientCapabilities vs getCapabilities)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 331-338 (Pitfall 4: init.ts conditions — set ctx.hasUI=true and provide ctx.ui.form)
  </read_first>
  <action>
    Create bin/mcp-server.ts per D-04, D-05, D-06, D-07, D-11. This file REPLACES bin/kilo-mcp-server.ts (which is deleted in Task 3). Per D-04, this file inlines its OWN AgentAPI implementation — no import of StoreAgentAdapter.

    **Structure (reordered flow per Pitfall 1):**

    1. **Imports**: 
       - `createMcpAdapter` from `../adapters/entry.ts`
       - `ProtocolSamplingForwarder` from `../adapters/protocol-sampling-forwarder.ts`
       - `ProtocolElicitationForwarder` from `../adapters/protocol-elicitation-forwarder.ts`
       - `loadMcpConfig` from `../config.ts`
       - `loadMetadataCache` from `../metadata-cache.ts`
       - `createUniversalResolver` from `../interfaces/agent-paths.ts`
       - Types: `AgentContext`, `ToolRegistration`, `CommandConfig`, `FlagConfig` from `../interfaces/agent-api.ts`
       - Type: `AgentChannel` from `../interfaces/agent-channel.ts`
       - `Server` from `@modelcontextprotocol/sdk/server/index.js`
       - `StdioServerTransport` from `@modelcontextprotocol/sdk/server/stdio.js`
       - `CallToolRequestSchema`, `ListToolsRequestSchema` from `@modelcontextprotocol/sdk/types.js`

    2. **Constants**: SERVER_NAME = "mcp-adapter", SERVER_VERSION = "2.9.0", GENERIC_GLOBAL_CONFIG_PATH via createUniversalResolver().globalConfigPath()

    3. **Inline AgentAPI class** (per D-04 — NOT importing StoreAgentAdapter):
       Define a class `InlineMcpAdapter` implementing `AgentAPI` with:
       - `readonly tools = new Map<string, ToolRegistration>()`
       - `readonly commands = new Map<string, CommandConfig>()`
       - `readonly flags = new Map<string, FlagConfig & { value?: string }>()`
       - `readonly handlers = new Map<string, Set<(...args: unknown[]) => unknown>>()`
       - `private channel: AgentChannel | undefined`
       - `registerTool(tool)`: this.tools.set(tool.name, tool)
       - `registerCommand(name, config)`: this.commands.set(name, config)
       - `registerFlag(name, config)`: this.flags.set(name, { ...config })
       - `on(event, handler)`: create Set if not exists, add handler
       - `getAllTools()`: return [...this.tools.values()].map(t => ({ name: t.name }))
       - `getFlag(name)`: return this.flags.get(name)?.value
       - `sendMessage(message)`: if channel, channel.send(message); else no-op (stderr routing is via channel)
       - `exec(command, args)`: dynamic import node:child_process.spawn, return { code, stdout, stderr } (same pattern as StoreAgentAdapter T-10-02)
       - `attachChannel(channel)`: this.channel = channel
       - `fireSessionStart(ctx)`: invoke handlers for "session_start" with ctx (same fire pattern as StoreAgentAdapter, T-10-01: catch and log errors with prefix only, never args)
       - `fireSessionShutdown()`: invoke handlers for "session_shutdown"
       - Error logging: use prefix "[mcp-adapter]" + event name + handler count only — NEVER log args (T-10-01 pattern from StoreAgentAdapter)

    4. **parseArgs + showHelp** (same as kilo-mcp-server.ts but with updated help text): 
       - Help text: "Usage: mcp-server [options]" (not "kilo-mcp-server")
       - Config discovery: "1. --config flag, 2. MCP_CONFIG_PATH env var, 3. .mcp.json in cwd, 4. ~/.config/mcp/mcp.json (shared global)" — per D-02, NO agent-specific path

    5. **main() function** (REORDERED flow per Pitfall 1):
       Step 1: Parse args, handle help/version
       Step 2: Load config using createUniversalResolver().globalConfigPath() as the fallback global path
       Step 3: Create InlineMcpAdapter instance
       Step 4: Create initial AgentContext: { cwd: process.cwd(), hasUI: false }
       Step 5: Load metadata cache
       Step 6: Call createMcpAdapter(adapter, ctx, config, cache) — registers proxy tool, commands, flags, session lifecycle handlers
       Step 7: Attach AgentChannel (routes sendMessage to stderr — same pattern as kilo-mcp-server.ts lines 137-143)
       Step 8: Create MCP Server with { name: SERVER_NAME, version: SERVER_VERSION }, { capabilities: { tools: {} } }
       Step 9: Set request handlers (ListToolsRequestSchema, CallToolRequestSchema — same as kilo-mcp-server.ts lines 169-197)
       Step 10: **CRITICAL** — `await server.connect(transport)` BEFORE fireSessionStart (Pitfall 1). Create StdioServerTransport, connect.
       Step 11: **CRITICAL** — Check `server.getClientCapabilities()` (NOT getCapabilities() — Pitfall 5). Store result as `clientCaps`.
       Step 12: If `clientCaps?.sampling` — create `new ProtocolSamplingForwarder(server)`, set `ctx.samplingProvider = samplingForwarder`, set `ctx.hasUI = true` (Pitfall 4: satisfies init.ts sampling condition)
       Step 13: If `clientCaps?.elicitation?.form` — create `new ProtocolElicitationForwarder(server)`, set `ctx.ui = { notify: (msg, level) => console.error/log, form: (config) => elicitationForwarder.form(config) }`, set `ctx.hasUI = true` (Pitfall 4: satisfies init.ts elicitation condition)
       Step 14: `await adapter.fireSessionStart(ctx)` — triggers initializeMcp which reads ctx with forwarders injected. Wrap in try/catch, log to stderr.
       Step 15: Log "[mcp-server] MCP server ready via stdio" to stderr

    **Error handling**: All errors go to stderr (console.error), never stdout (MCP protocol uses stdout). Same pattern as kilo-mcp-server.ts.

    **D-11 compliance**: Do NOT check config.settings.sampling or config.settings.elicitation anywhere in this file. The only gate is the client capability check via getClientCapabilities().
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "getClientCapabilities" bin/mcp-server.ts | xargs test 1 -le && grep -c "getCapabilities\b" bin/mcp-server.ts | xargs test 0 -eq && grep -c "StoreAgentAdapter\|KiloAdapter\|QoderAdapter" bin/mcp-server.ts | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - bin/mcp-server.ts exists and is a valid TypeScript file
    - `npx tsc --noEmit` exits 0
    - File contains `server.connect(transport)` BEFORE `adapter.fireSessionStart(ctx)` (flow reordering per Pitfall 1)
    - File contains `server.getClientCapabilities()` (NOT `server.getCapabilities()` — Pitfall 5)
    - File imports ProtocolSamplingForwarder from `../adapters/protocol-sampling-forwarder.ts`
    - File imports ProtocolElicitationForwarder from `../adapters/protocol-elicitation-forwarder.ts`
    - File imports createUniversalResolver from `../interfaces/agent-paths.ts`
    - File imports createMcpAdapter from `../adapters/entry.ts`
    - File does NOT import StoreAgentAdapter, KiloAdapter, QoderAdapter, or any per-agent adapter
    - File does NOT contain `config.settings?.sampling` or `config.settings?.elicitation` (D-11: no config check)
    - File contains `ctx.hasUI = true` when forwarders are injected (Pitfall 4)
    - Inline AgentAPI class has all 8 AgentAPI methods: registerTool, registerCommand, registerFlag, on, getAllTools, getFlag, sendMessage, exec
    - Inline AgentAPI class has attachChannel and fireSessionStart methods (needed by main flow)
  </acceptance_criteria>
  <done>bin/mcp-server.ts created with reordered flow, inline AgentAPI, getClientCapabilities() check, and forwarder injection. tsc passes. No per-agent adapter imports. No config.settings checks.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json bin + vitest.config.ts + remove old resolvers from agent-paths.ts</name>
  <files>package.json, vitest.config.ts, interfaces/agent-paths.ts, __tests__/package-manifest.test.ts</files>
  <read_first>
    - package.json lines 8-12 (current bin entries: pi-mcp-adapter, kilo-mcp-server, qoder-mcp-bridge)
    - vitest.config.ts lines 64-93 (coverage thresholds for qoder-adapter.ts, qoder-sampling-provider.ts, qoder-renderer.ts, scripts/qoder-smoke.ts)
    - interfaces/agent-paths.ts (after Plan 02 — has createUniversalResolver; still has createKiloResolver, createQoderResolver, resolveQoderGlobalConfigPath)
    - __tests__/package-manifest.test.ts (understand what assertions it makes about package.json bin and files array)
  </read_first>
  <action>
    **package.json** (per D-10):
    - Change bin section from:
      `"pi-mcp-adapter": "cli.js", "kilo-mcp-server": "bin/kilo-mcp-server.ts", "qoder-mcp-bridge": "bin/qoder-mcp-bridge.ts"`
      to:
      `"pi-mcp-adapter": "cli.js", "mcp-server": "bin/mcp-server.ts"`
    - Remove any reference to kilo-mcp-server or qoder-mcp-bridge
    - In the `"files"` array: keep `"bin"`, `"adapters"`, `"interfaces"` (unchanged). These directories still exist with updated content.

    **vitest.config.ts** (per Pitfall 6):
    - Remove the coverage threshold entries for:
      - `"adapters/qoder-adapter.ts"` (lines 64-70)
      - `"adapters/qoder-sampling-provider.ts"` (lines 71-77)
      - `"adapters/qoder-renderer.ts"` (lines 79-84)
      - `"scripts/qoder-smoke.ts"` (lines 86-93)
    - Keep all other threshold entries (pi-adapter.ts, pi-sampling-provider.ts, pi-renderer.ts, entry.ts, agent-paths.ts, sampling.ts)
    - Do NOT add new thresholds for protocol-sampling-forwarder.ts or protocol-elicitation-forwarder.ts (can be added later if needed)

    **interfaces/agent-paths.ts**:
    - Remove `createKiloResolver` function (lines 68-74)
    - Remove `createQoderResolver` function (lines 55-61)
    - Remove `resolveQoderGlobalConfigPath` function (lines 35-37) — no longer used by any code
    - Keep: `createPiResolver`, `createUniversalResolver`, `DEFAULT_AGENT_RESOLVER`, `resolveAgentGlobalConfigPath`, `resolveEnvAgentDir` (still used by createPiResolver)
    - Update `AgentId` type: change `"qoder"` to `"universal-mcp"` (optional — the `(string & {})` already accepts it, but updating the literal union is cleaner)

    **__tests__/package-manifest.test.ts**:
    - Update any assertions that check for `kilo-mcp-server` or `qoder-mcp-bridge` in package.json bin to instead check for `mcp-server`
    - If the test checks the number of bin entries, update the expected count
    - If the test checks that bin entries point to existing files, verify `bin/mcp-server.ts` exists
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "kilo-mcp-server\|qoder-mcp-bridge" package.json | xargs test 0 -eq && grep -c "mcp-server" package.json | xargs test 1 -le && grep -c "qoder-adapter\|qoder-sampling\|qoder-renderer\|qoder-smoke" vitest.config.ts | xargs test 0 -eq && grep -c "createKiloResolver\|createQoderResolver\|resolveQoderGlobalConfigPath" interfaces/agent-paths.ts | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - package.json bin has exactly 2 entries: "pi-mcp-adapter" → "cli.js" and "mcp-server" → "bin/mcp-server.ts"
    - package.json does NOT contain "kilo-mcp-server" or "qoder-mcp-bridge" anywhere
    - vitest.config.ts does NOT have coverage thresholds for qoder-adapter.ts, qoder-sampling-provider.ts, qoder-renderer.ts, or scripts/qoder-smoke.ts
    - interfaces/agent-paths.ts does NOT export createKiloResolver, createQoderResolver, or resolveQoderGlobalConfigPath
    - interfaces/agent-paths.ts DOES export createPiResolver and createUniversalResolver
    - `npx tsc --noEmit` exits 0
    - `npx vitest run __tests__/package-manifest.test.ts -x` passes
  </acceptance_criteria>
  <done>package.json has single mcp-server bin entry. vitest.config.ts cleaned of deleted file thresholds. agent-paths.ts cleaned of Kilo/Qoder resolvers. tsc + package-manifest test pass.</done>
</task>

<task type="auto">
  <name>Task 3: Delete all per-agent adapter files, per-adapter tests, and legacy scripts</name>
  <files>bin/kilo-mcp-server.ts, bin/qoder-mcp-bridge.ts, adapters/kilo-adapter.ts, adapters/qoder-adapter.ts, adapters/store-adapter.ts, adapters/qoder-sampling-provider.ts, adapters/qoder-renderer.ts, scripts/kilo-mcp-entry.ts, scripts/qoder-smoke.ts, __tests__/qoder-adapter.test.ts, __tests__/qoder-adapter-integration.test.ts, __tests__/qoder-sampling-provider.test.ts, __tests__/store-adapter.test.ts</files>
  <read_first>
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 284-301 (Files to Delete table + Files to Retain note)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md D-04 (delete ALL per-agent adapter code including base class)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md D-09 (delete per-adapter tests)
  </read_first>
  <action>
    Delete the following files using the DeleteFile tool (NOT shell rm):

    **Per-agent adapter files (D-04):**
    - adapters/kilo-adapter.ts — per-agent Kilo adapter (replaced by inline AgentAPI in bin/mcp-server.ts)
    - adapters/qoder-adapter.ts — per-agent Qoder adapter (replaced)
    - adapters/store-adapter.ts — base class (D-04: delete ALL including base class; each entry point has its own inline implementation)
    - adapters/qoder-sampling-provider.ts — per-agent sampling provider (replaced by ProtocolSamplingForwarder)
    - adapters/qoder-renderer.ts — per-agent renderer (no longer needed; Branch C uses MCP content blocks)

    **Bin files (D-04, D-05, D-10):**
    - bin/kilo-mcp-server.ts — replaced by bin/mcp-server.ts (created in Task 1)
    - bin/qoder-mcp-bridge.ts — per-agent bridge eliminated (D-04)

    **Legacy scripts:**
    - scripts/kilo-mcp-entry.ts — legacy entry superseded by bin/mcp-server.ts
    - scripts/qoder-smoke.ts — smoke test for deleted Qoder adapter

    **Per-adapter test files (D-09):**
    - __tests__/qoder-adapter.test.ts — tests deleted QoderAdapter
    - __tests__/qoder-adapter-integration.test.ts — tests deleted QoderAdapter
    - __tests__/qoder-sampling-provider.test.ts — tests deleted QoderSamplingProvider
    - __tests__/store-adapter.test.ts — tests deleted StoreAgentAdapter

    After all deletions, verify no remaining imports of deleted files exist:
    - `grep -r "kilo-adapter\|qoder-adapter\|store-adapter\|qoder-sampling-provider\|qoder-renderer\|qoder-mcp-bridge\|kilo-mcp-entry\|qoder-smoke" --include="*.ts" .` should return 0 results in source files (excluding .planning/ and node_modules/)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run -x --reporter=default 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 13 files listed above are deleted (do not exist on disk)
    - `npx tsc --noEmit` exits 0 (no broken imports)
    - `npx vitest run` passes (no test failures from missing imports)
    - `grep -r "store-adapter\|kilo-adapter\|qoder-adapter\|qoder-sampling-provider\|qoder-renderer\|qoder-mcp-bridge\|kilo-mcp-entry\|qoder-smoke" --include="*.ts" adapters/ bin/ scripts/ interfaces/ __tests__/` returns 0 results (excluding test files that were themselves deleted)
  </acceptance_criteria>
  <done>All per-agent adapter files, per-adapter tests, and legacy scripts deleted. tsc passes. vitest passes. No dangling imports remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent (MCP Client) → bin/mcp-server.ts | Untrusted input via stdio: tool call arguments, capability declarations |
| bin/mcp-server.ts → Downstream MCP Servers | Trusted code path: mcp-adapter connects to downstream servers per .mcp.json config |
| Downstream → bin/mcp-server.ts → Agent | Server→Client reverse calls: sampling/elicitation forwarded through forwarders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | bin/mcp-server.ts stderr routing | mitigate | All diagnostic output goes to stderr (console.error); MCP protocol uses stdout exclusively. Same pattern as kilo-mcp-server.ts lines 137-142 |
| T-12-08 | Tampering | CallToolRequestSchema handler | accept | MCP SDK validates tool name and args via Zod schema; adapter's tool.execute() handles argument validation |
| T-12-09 | Spoofing | Client capability declaration | accept | Agent Client declares capabilities during MCP initialization handshake; server cannot verify implementation — trust-based per MCP spec |
| T-12-10 | Information Disclosure | InlineMcpAdapter.exec() | mitigate | Dynamic import of node:child_process; only called from trusted host code (auth-flow, setup, lifecycle). T-10-02 pattern preserved: no path from MCP tool result to exec |
| T-12-11 | Denial of Service | fireSessionStart error handling | mitigate | Wrap fireSessionStart in try/catch; non-fatal — servers connect lazily on first tool call. Same pattern as kilo-mcp-server.ts lines 146-152 |
| T-12-SC | Tampering | npm installs | accept | No new packages installed — all dependencies existing and verified in RESEARCH.md |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` passes (all remaining tests, no broken imports)
- `ls bin/mcp-server.ts` succeeds, `ls bin/kilo-mcp-server.ts` fails (file not found)
- `ls bin/qoder-mcp-bridge.ts` fails (file not found)
- `ls adapters/store-adapter.ts` fails (file not found)
- `grep -c "mcp-server" package.json` returns at least 1 (bin entry)
- `grep -c "kilo-mcp-server\|qoder-mcp-bridge" package.json` returns 0
- `grep -c "getClientCapabilities" bin/mcp-server.ts` returns at least 1
- `grep -c "getCapabilities[^)]" bin/mcp-server.ts` returns 0 (no bare getCapabilities call)
- `grep -c "fireSessionStart" bin/mcp-server.ts` returns at least 1
- `grep -c "server.connect" bin/mcp-server.ts` returns at least 1
</verification>

<success_criteria>
- bin/mcp-server.ts is the universal MCP stdio server with reordered flow (D-05)
- Inline AgentAPI implementation (D-04: no StoreAgentAdapter import)
- getClientCapabilities() used for capability discovery (Pitfall 5)
- Forwarders injected conditionally based on client capabilities (D-06, D-07, D-11)
- All per-agent adapter files and tests deleted (D-04, D-09)
- package.json has single mcp-server bin entry (D-10)
- vitest.config.ts cleaned of deleted file thresholds (Pitfall 6)
- createKiloResolver/createQoderResolver removed (D-02)
- tsc + vitest pass
</success_criteria>

<output>
Create `.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New Symbols

| File | Symbol | Type | Description |
|------|--------|------|-------------|
| bin/mcp-server.ts | `InlineMcpAdapter` | class | Inline AgentAPI implementation for the universal MCP stdio server (D-04: no shared base class). Provides tools/commands/flags Maps, event handlers, exec, attachChannel, fireSessionStart/fireSessionShutdown |
| bin/mcp-server.ts | `main` | function | Entry point: reordered flow — config → adapter → createMcpAdapter → MCP Server → connect → getClientCapabilities → inject forwarders → fireSessionStart |
| bin/mcp-server.ts | `parseArgs` | function | CLI argument parser (--config, --help, --version) |
| bin/mcp-server.ts | `showHelp` | function | Help text display |

### Deleted Symbols

| File | Symbol | Reason |
|------|--------|--------|
| adapters/kilo-adapter.ts | `KiloAdapter`, `adaptKiloContext` | D-04: per-agent adapter eliminated |
| adapters/qoder-adapter.ts | `QoderAdapter`, `adaptQoderContext` | D-04: per-agent adapter eliminated |
| adapters/store-adapter.ts | `StoreAgentAdapter`, `AgentProfile` | D-04: base class deleted; each entry point has inline AgentAPI |
| adapters/qoder-sampling-provider.ts | `QoderSamplingProvider` | D-04: replaced by ProtocolSamplingForwarder |
| adapters/qoder-renderer.ts | (renderer) | D-04: Branch C uses MCP content blocks |
| bin/kilo-mcp-server.ts | (entire file) | D-05: renamed to bin/mcp-server.ts |
| bin/qoder-mcp-bridge.ts | (entire file) | D-04: per-agent bridge eliminated |
| scripts/kilo-mcp-entry.ts | (entire file) | Legacy, superseded by bin/mcp-server.ts |
| scripts/qoder-smoke.ts | (entire file) | Smoke test for deleted Qoder adapter |
| interfaces/agent-paths.ts | `createKiloResolver` | D-02: replaced by createUniversalResolver |
| interfaces/agent-paths.ts | `createQoderResolver` | D-02: replaced by createUniversalResolver |
| interfaces/agent-paths.ts | `resolveQoderGlobalConfigPath` | D-02: no agent-specific paths |

### Modified Symbols

| File | Symbol | Change |
|------|--------|--------|
| package.json | `bin` | Changed from 3 entries to 2: pi-mcp-adapter + mcp-server (D-10) |
| vitest.config.ts | `coverage.thresholds` | Removed entries for deleted files: qoder-adapter, qoder-sampling-provider, qoder-renderer, qoder-smoke (Pitfall 6) |
