---
phase: 12-universal-mcp-stdio-server-protocol-category-simplification-
plan: 05
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - skills/mcp-adapter/SKILL.md
  - skills/mcp-adapter/references/resolver.md
  - skills/mcp-adapter/references/deploy.md
  - skills/mcp-adapter/references/verify.md
  - skills/upstream-merge/references/special-cases.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [D-03, D-08, D-10, D-12]

must_haves:
  truths:
    - "SKILL.md Phase 0 asks a single question: 'Pi or other MCP-compatible agent?' (D-12)"
    - "Branch B is removed from SKILL.md entirely (D-12)"
    - "Branch A (Pi native extension) and Branch C (universal MCP stdio) are the only two branches documented"
    - "SKILL.md does NOT mention Kilo or Qoder adapters by name (D-08: universal)"
    - "README.md documents the universal MCP server (mcp-server bin entry) with no per-agent adapter mentions"
    - "README.md lists verified agents (any MCP-compatible agent works, not a hardcoded list)"
    - "special-cases.md updated: removed entries for deleted files, added entries for new files (protocol-sampling-forwarder.ts, protocol-elicitation-forwarder.ts, bin/mcp-server.ts)"
    - "CHANGELOG.md documents the migration: kilo-mcp-server → mcp-server, qoder-mcp-bridge deleted, per-agent adapters removed"
  artifacts:
    - path: "skills/mcp-adapter/SKILL.md"
      provides: "Simplified skill with Branch A + Branch C only"
      contains: "universal"
      min_lines: 100
    - path: "skills/upstream-merge/references/special-cases.md"
      provides: "Updated fork-only file registry"
      contains: "protocol-sampling-forwarder"
    - path: "README.md"
      provides: "Updated README with universal MCP server documentation"
      contains: "mcp-server"
    - path: "CHANGELOG.md"
      provides: "Migration documentation"
      contains: "universal"
  key_links:
    - from: "skills/mcp-adapter/SKILL.md"
      to: "bin/mcp-server.ts"
      via: "Branch C references mcp-server bin entry"
      pattern: "mcp-server"
    - from: "skills/upstream-merge/references/special-cases.md"
      to: "adapters/protocol-sampling-forwarder.ts"
      via: "fork-only file entry"
      pattern: "protocol-sampling-forwarder"
---

<objective>
Update all documentation to reflect the universal MCP server architecture: simplify SKILL.md to Branch A + Branch C, rewrite README for the single universal server, update the upstream-merge special-cases registry, and document the migration in CHANGELOG.

Purpose: D-03 (Pi Branch A only), D-08 (Branch C is complete, not "best-effort"), D-10 (single bin entry documented), D-12 (SKILL.md Phase 0 simplification). This plan ensures users and fork maintainers see the simplified architecture without legacy per-agent references.

Output: 7 modified documentation files.
</objective>

<execution_context>
@$HOME/.qoder/gsd-core/workflows/execute-plan.md
@$HOME/.qoder/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md
@.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-03-SUMMARY.md
@skills/mcp-adapter/SKILL.md
@skills/upstream-merge/references/special-cases.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify SKILL.md to Branch A + Branch C + update references</name>
  <files>skills/mcp-adapter/SKILL.md, skills/mcp-adapter/references/resolver.md, skills/mcp-adapter/references/deploy.md, skills/mcp-adapter/references/verify.md</files>
  <read_first>
    - skills/mcp-adapter/SKILL.md (full file — understand current Phase 0 structure, branch descriptions, capability matrix)
    - skills/mcp-adapter/references/resolver.md (capability matrix — currently lists Kilo, Qoder)
    - skills/mcp-adapter/references/deploy.md (deployment branches — currently has Branch A, B, C)
    - skills/mcp-adapter/references/verify.md (test matrix — currently references per-adapter tests)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md D-03, D-08, D-12 (decisions to implement)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 57-68 (D-12: SKILL.md Phase 0 simplified to single question)
  </read_first>
  <action>
    **skills/mcp-adapter/SKILL.md** (per D-03, D-08, D-12):
    - **Phase 0 simplification** (D-12): Replace the current Phase 0 (which reads AGENT_ADAPTERS registry, displays static capability matrix) with a single question: "Are you using Pi or another MCP-compatible agent?"
      - If Pi → Branch A (native extension install via `pi-mcp-adapter` package)
      - If other MCP-compatible agent → Branch C (register `mcp-server` in agent's MCP config)
      - Remove all references to reading the registry, static capability matrix display, and agent-specific path discovery
      - Branch B is REMOVED ENTIRELY — no SDK bridge, no per-agent adapter mentions
    - **Branch descriptions** (D-03, D-08):
      - Branch A (Pi): Native extension. Install `pi-mcp-adapter` as a Pi extension. Full TUI panel, custom renderers, in-process sampling via PiSamplingProvider.
      - Branch C (Universal MCP): Register `mcp-server` in the agent's MCP config file (`.mcp.json` or agent-specific config). The server discovers client capabilities at runtime via MCP protocol. Sampling and elicitation are forwarded via MCP Server→Client reverse calls when the agent declares those capabilities. Branch C is a COMPLETE implementation within the MCP protocol's scope — NOT "best-effort" or "lesser" (D-08). Tool actions (executeStatus) and content blocks provide equivalent functionality. What Pi Branch A provides extra is richer UI (TUI rendering), which is a presentation enhancement, not a capability difference.
    - **Remove all mentions** of: KiloAdapter, QoderAdapter, StoreAgentAdapter, kilo-mcp-server, qoder-mcp-bridge, createKiloResolver, createQoderResolver, ~/.kilo/mcp.json, ~/.qoder/agent/mcp.json
    - **Config path** (D-02): Document the universal discovery chain: `--config` flag > `MCP_CONFIG_PATH` env > `.mcp.json` in cwd > `~/.config/mcp/mcp.json` (shared global)
    - **Capability discovery** (D-12): State that "Capabilities are discovered at runtime when the Agent connects as MCP Client. No static capability matrix is needed."

    **skills/mcp-adapter/references/resolver.md**:
    - Simplify the capability matrix to 2 rows: Pi (Branch A) and Universal MCP (Branch C)
    - Remove Kilo and Qoder rows
    - For Universal MCP: capabilities are "runtime-discovered" (not static)

    **skills/mcp-adapter/references/deploy.md**:
    - Remove Branch B section entirely
    - Branch A: Pi native extension (unchanged content)
    - Branch C: Universal MCP stdio server. Registration example:
      ```json
      {
        "mcpServers": {
          "mcp-adapter": {
            "command": "mcp-server"
          }
        }
      }
      ```
    - Remove any per-agent deployment instructions (Kilo-specific, Qoder-specific)

    **skills/mcp-adapter/references/verify.md**:
    - Update test matrix: `verify:deploy --agent universal-mcp` (replaces `--agent kilo` and `--agent qoder`)
    - Remove references to per-adapter tests (qoder-adapter.test.ts, qoder-sampling-provider.test.ts, store-adapter.test.ts)
    - Add reference to new tests: protocol-sampling-forwarder.test.ts, protocol-elicitation-forwarder.test.ts, mcp-server-e2e.test.ts
  </action>
  <verify>
    <automated>grep -c "Branch B" skills/mcp-adapter/SKILL.md skills/mcp-adapter/references/deploy.md | xargs test 0 -eq && grep -c "kilo-mcp-server\|qoder-mcp-bridge\|KiloAdapter\|QoderAdapter\|StoreAgentAdapter" skills/mcp-adapter/SKILL.md | xargs test 0 -eq && grep -c "mcp-server" skills/mcp-adapter/SKILL.md | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Phase 0 contains a single question about Pi vs other MCP-compatible agents (D-12)
    - SKILL.md does NOT contain "Branch B" anywhere (D-12)
    - SKILL.md does NOT mention KiloAdapter, QoderAdapter, StoreAgentAdapter, kilo-mcp-server, or qoder-mcp-bridge (D-08)
    - SKILL.md mentions "mcp-server" as the bin entry for Branch C (D-10)
    - SKILL.md states Branch C is complete (not "best-effort") per D-08
    - deploy.md has Branch A + Branch C only (no Branch B)
    - deploy.md Branch C shows `mcp-server` command (not `kilo-mcp-server`)
    - resolver.md has 2 rows: Pi + Universal MCP
    - verify.md references `--agent universal-mcp` (not `--agent kilo` or `--agent qoder`)
  </acceptance_criteria>
  <done>SKILL.md simplified to Branch A + Branch C. All references to per-agent adapters removed. Branch B eliminated. Config path universalized.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md + CHANGELOG.md + special-cases.md</name>
  <files>README.md, CHANGELOG.md, skills/upstream-merge/references/special-cases.md</files>
  <read_first>
    - README.md (full file — understand current structure, agent mentions, installation instructions)
    - CHANGELOG.md (recent entries — understand format, last version entry)
    - skills/upstream-merge/references/special-cases.md (full file — current fork-only file registry, ~37 entries)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-CONTEXT.md D-08, D-10 (decisions)
    - .planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-RESEARCH.md lines 207-234 (recommended project structure — new files, deleted files)
  </read_first>
  <action>
    **README.md** (per D-08, D-10):
    - Update installation/usage section: replace `kilo-mcp-server` and `qoder-mcp-bridge` references with single `mcp-server` command
    - Update "Verified Agents" section: state that ANY MCP-compatible agent works with Branch C (universal MCP stdio server). Do NOT list specific agents as "verified" — the universal server is agent-agnostic. Mention that capabilities (sampling, elicitation) are discovered at runtime.
    - Remove any per-agent adapter mentions (KiloAdapter, QoderAdapter)
    - Update the compatibility section: Pi → Branch A (native extension), any other MCP-compatible agent → Branch C (universal MCP stdio server via `mcp-server` bin)
    - Remove Branch B mentions
    - If README has a capability matrix, simplify to 2 columns: Pi (Branch A) and Universal MCP (Branch C)

    **CHANGELOG.md** (per D-10):
    - Add new entry at the top documenting the Phase 12 changes:
      - Title: `[Unreleased] - Universal MCP Stdio Server`
      - Breaking changes:
        - `kilo-mcp-server` renamed to `mcp-server` (no alias kept — D-10)
        - `qoder-mcp-bridge` deleted entirely (D-04)
        - Per-agent adapters (`KiloAdapter`, `QoderAdapter`, `StoreAgentAdapter`, `QoderSamplingProvider`) deleted (D-04)
        - `createKiloResolver`, `createQoderResolver` removed from `interfaces/agent-paths.ts` (D-02)
      - New features:
        - Universal MCP stdio server (`bin/mcp-server.ts`) — agent-agnostic, speaks MCP protocol (D-05)
        - `ProtocolSamplingForwarder` — forwards sampling via MCP Server→Client `sampling/createMessage` (D-06)
        - `ProtocolElicitationForwarder` — forwards elicitation via MCP Server→Client `elicitation/create` (D-07)
        - Runtime capability discovery via `server.getClientCapabilities()` (D-11)
      - Migration steps: Replace `kilo-mcp-server` with `mcp-server` in agent MCP config. Remove any Qoder-specific bridge setup. Config path is now universal: `--config` > `MCP_CONFIG_PATH` > `.mcp.json` > `~/.config/mcp/mcp.json`

    **skills/upstream-merge/references/special-cases.md**:
    - **Remove entries** for deleted files:
      - `adapters/kilo-adapter.ts`
      - `adapters/qoder-adapter.ts`
      - `adapters/store-adapter.ts`
      - `adapters/qoder-sampling-provider.ts`
      - `adapters/qoder-renderer.ts`
      - `bin/kilo-mcp-server.ts` (renamed to mcp-server.ts)
      - `bin/qoder-mcp-bridge.ts`
      - `scripts/kilo-mcp-entry.ts`
      - `scripts/qoder-smoke.ts`
    - **Add entries** for new files:
      - `adapters/protocol-sampling-forwarder.ts` — Fork-only: implements SamplingProvider via MCP Server→Client reverse call; upstream has no equivalent
      - `adapters/protocol-elicitation-forwarder.ts` — Fork-only: implements UISystem.form via MCP Server→Client reverse call; upstream has no equivalent
      - `bin/mcp-server.ts` — Fork-only: universal MCP stdio server; renamed from kilo-mcp-server.ts; upstream has no bin/ directory
    - **Update entries** for modified files:
      - `interfaces/agent-api.ts` — AGENT_ADAPTERS simplified to universal-mcp + pi (removed kilo, qoder entries)
      - `interfaces/agent-paths.ts` — Removed createKiloResolver, createQoderResolver; added createUniversalResolver
      - `package.json` — bin section: removed kilo-mcp-server, qoder-mcp-bridge; added mcp-server
      - `vitest.config.ts` — Removed coverage thresholds for deleted files
    - Update the footer count (e.g., "N entries" → new count)
    - Run `npm run upstream:check` after updating to verify the registry is in sync
  </action>
  <verify>
    <automated>grep -c "kilo-mcp-server\|qoder-mcp-bridge" README.md | xargs test 0 -eq && grep -c "mcp-server" README.md | xargs test 1 -le && grep -c "protocol-sampling-forwarder" skills/upstream-merge/references/special-cases.md | xargs test 1 -le && npm run upstream:check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - README.md does NOT mention kilo-mcp-server or qoder-mcp-bridge (D-10)
    - README.md mentions mcp-server as the universal bin entry
    - README.md states any MCP-compatible agent works (D-08: universal, not per-agent)
    - CHANGELOG.md has a Phase 12 entry with breaking changes and migration steps
    - CHANGELOG.md documents kilo-mcp-server → mcp-server rename and qoder-mcp-bridge deletion
    - special-cases.md has entries for protocol-sampling-forwarder.ts and protocol-elicitation-forwarder.ts
    - special-cases.md does NOT have entries for deleted files (kilo-adapter.ts, qoder-adapter.ts, etc.)
    - special-cases.md has an entry for bin/mcp-server.ts (new fork-only file)
    - `npm run upstream:check` exits 0 (or documents any remaining stale entries)
  </acceptance_criteria>
  <done>README updated with universal mcp-server. CHANGELOG documents migration. special-cases.md updated with new/deleted files. upstream:check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this plan modifies documentation only. All trust boundaries are inherited from Plans 01-04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Information Disclosure | README/CHANGELOG | accept | Documentation is public; no secrets or internal paths disclosed. Migration steps are user-facing instructions |
| T-12-SC | Tampering | npm installs | accept | No new packages — documentation-only plan |
</threat_model>

<verification>
- `grep -c "Branch B" skills/mcp-adapter/SKILL.md skills/mcp-adapter/references/deploy.md` returns 0 for both
- `grep -c "kilo-mcp-server\|qoder-mcp-bridge\|KiloAdapter\|QoderAdapter" skills/mcp-adapter/SKILL.md README.md` returns 0 for both
- `grep -c "mcp-server" skills/mcp-adapter/SKILL.md README.md` returns at least 1 for both
- `grep -c "protocol-sampling-forwarder\|protocol-elicitation-forwarder" skills/upstream-merge/references/special-cases.md` returns at least 1
- `npm run upstream:check` exits 0
</verification>

<success_criteria>
- SKILL.md simplified to Branch A + Branch C (D-03, D-08, D-12)
- Branch B removed entirely (D-12)
- README documents universal mcp-server, no per-agent mentions (D-08, D-10)
- CHANGELOG documents migration steps (D-10)
- special-cases.md updated with new fork-only files and removed deleted entries
- upstream:check passes
</success_criteria>

<output>
Create `.planning/phases/12-universal-mcp-stdio-server-protocol-category-simplification-/12-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Modified Artifacts

| File | Change |
|------|--------|
| skills/mcp-adapter/SKILL.md | Phase 0 simplified to single question; Branch B removed; Branch C documented as complete (D-03, D-08, D-12) |
| skills/mcp-adapter/references/resolver.md | Capability matrix simplified to Pi + Universal MCP (2 rows) |
| skills/mcp-adapter/references/deploy.md | Branch B removed; Branch C shows mcp-server command |
| skills/mcp-adapter/references/verify.md | Test matrix updated: --agent universal-mcp; references new test files |
| README.md | Universal mcp-server documentation; no per-agent adapter mentions (D-08, D-10) |
| CHANGELOG.md | Phase 12 migration entry: breaking changes + new features + migration steps (D-10) |
| skills/upstream-merge/references/special-cases.md | Removed deleted file entries; added protocol-sampling-forwarder.ts, protocol-elicitation-forwarder.ts, bin/mcp-server.ts; updated modified file entries |
