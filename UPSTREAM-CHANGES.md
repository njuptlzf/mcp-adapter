# Upstream Changes Manifest

## Tracks every file diverged from upstream (nicobailon/pi-mcp-adapter) to njuptlzf/mcp-adapter. Read this file first when running `skills/upstream-merge/SKILL.md`.

Generated at: 2026-06-18 for upstream/main @ a764c25
Source of truth: `git diff upstream/main --name-status -- '*.ts' '*.md' '*.json'`
Refresh policy: regenerate via PR when upstream ships a major release (per UPSTREAM-01-D, no CI hook)

## Decision anchors

- **D-07** — `AGENT_ADAPTERS` static registry in `interfaces/agent-api.ts` (Phase 1) — source of truth for which adapters we own; adding a new adapter = one descriptor.
- **D-21** — Phase 7 `skills/mcp-adapter-test/SKILL.md` parametric + per-agent references (`references/agent-paths/_template.md` → `<id>.md`); main file untouched on adapter additions = **UPSTREAM-04 merge-friendly**.
- **D-04 / ENTRY-02 / 05-05** — `index.ts` keeps `ExtensionAPI` import as Pi backward-compat wrapper around `createMcpAdapter`; any upstream diff that re-shapes the public `mcpAdapter(pi: ExtensionAPI)` signature requires manual review.
- **DECOUPLE-01** (Phase 5-01) — `proxy-modes.ts` + `direct-tools.ts` already use generic `McpToolResult` (D-04).
- **DECOUPLE-02** (Phase 5-04) — `sampling-handler.ts` + `elicitation-handler.ts` already use generic `UISystem` (D-04).
- **DECOUPLE-06** (Phase 5-04) — `tool-result-renderer.ts` already uses generic `RenderOutput` (D-04). **DECOUPLE-06 does NOT cover `mcp-panel.ts` / `mcp-setup-panel.ts`** — those still import `@earendil-works/pi-tui` and are flagged as follow-up needed in the manifest body.
- **DECOUPLE-07** (Phase 5-02) — `agent-dir.ts` already uses `AgentPathResolver` (D-07) with `MCP_AGENT_DIR` env primary + `PI_CODING_AGENT_DIR` backward-compat fallback.
- **D-01..D-03** — Capability Gate reads `interfaces/agent-api.ts` `envHints` (includes `PI_CODING_AGENT_DIR`) — legal Pi reference for detection, not for new logic.
- **D-17** (Phase 7-04) — `tests/reporters/matrix-reporter.ts` Matrix Reporter is fork-only artefact; never sync from upstream.

## Divergence manifest

| Path | Status | Category | Default Resolution | Rationale |
|------|--------|----------|---------------------|-----------|
| `adapters/entry.ts` | new | adapter | ours | Universal agent-agnostic entry point (D-07 ENTRY-01..03 / D-04). Upstream has no equivalent — keep ours. |
| `adapters/pi-adapter.ts` | new | adapter | ours | Pi-specific adapter (D-07 / Phase 6 ADAPTER-01..03). Upstream has no equivalent — keep ours. |
| `adapters/pi-renderer.ts` | new | adapter | ours | Pi-specific adapter (D-07 / Phase 6 ADAPTER-01..03). Upstream has no equivalent — keep ours. |
| `adapters/pi-sampling-provider.ts` | new | adapter | ours | Pi-specific adapter (D-07 / Phase 6 ADAPTER-01..03). Upstream has no equivalent — keep ours. |
| `adapters/qoder-adapter.ts` | new | adapter | ours | Qoder-specific adapter (D-07 / Phase 6 ADAPTER-04..06). Upstream has no equivalent — keep ours. |
| `adapters/qoder-renderer.ts` | new | adapter | ours | Qoder-specific adapter (D-07 / Phase 6 ADAPTER-04..06). Upstream has no equivalent — keep ours. |
| `adapters/qoder-sampling-provider.ts` | new | adapter | ours | Qoder-specific adapter (D-07 / Phase 6 ADAPTER-04..06). Upstream has no equivalent — keep ours. |
| `.claude/skills/gitnexus/gitnexus-cli/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.claude/skills/gitnexus/gitnexus-debugging/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.claude/skills/gitnexus/gitnexus-exploring/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.claude/skills/gitnexus/gitnexus-guide/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.claude/skills/gitnexus/gitnexus-refactoring/SKILL.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `AGENTS.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `CLAUDE.md` | new | agents_meta | ours | **Fork-only** — `AGENTS.md` / `CLAUDE.md` / `.claude/` directory are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `package.json` | modified | config | manual | **Manual merge** — local peer/optional dependency structure (D-04) + upstream new deps. Accept upstream additions; preserve local `peerDependencies` + `optionalDependencies` shape. |
| `tsconfig.json` | modified | config | manual | **Line-by-line** — local strict + DOM lib overrides; accept upstream `compilerOptions` additions. |
| `vitest.config.ts` | modified | config | manual | **Line-by-line** — local `MatrixReporter` (D-17) + `test:prebuild` chain (D-14/D-15). Accept upstream excludes; preserve local reporters + prebuild. |
| `CHANGELOG.md` | modified | docs | manual | **Intent alignment** — fork changelog, references both upstream and fork versions. Prefer ours. |
| `MAPPING.md` | new | docs | manual | **Intent alignment** — adapter-to-Pi/Qoder mapping table. Fork-only documentation. |
| `OAUTH.md` | modified | docs | manual | **Intent alignment** — fork OAuth flow doc. Preserve local examples. |
| `README.md` | modified | docs | manual | **Intent alignment** — local hero is "Universal MCP Adapter" (D-18/D-19/D-20). Always ours if upstream reverts to Pi-only framing. |
| `interfaces/agent-api.ts` | new | interface | manual | **Keep ours + manual merge** — local generic interface (D-07). Upstream is Pi-specific; manual line-by-line to preserve `AgentAPI` / `AgentContext` / `UISystem` surface. |
| `interfaces/agent-paths.ts` | new | interface | manual | **Keep ours + manual merge** — local generic interface (D-07). Upstream is Pi-specific; manual line-by-line to preserve `AgentAPI` / `AgentContext` / `UISystem` surface. |
| `interfaces/sampling.ts` | new | interface | manual | **Keep ours + manual merge** — local generic interface (D-07). Upstream is Pi-specific; manual line-by-line to preserve `AgentAPI` / `AgentContext` / `UISystem` surface. |
| `docs/architecture-comparison.md` | new | other | ours | **Fork-only artefact** — `docs/` (additional) + `scripts/` are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `docs/mcp-adapter-test-plan.md` | new | other | ours | **Fork-only artefact** — `docs/` (additional) + `scripts/` are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `docs/mcp-adapter-token-savings.md` | new | other | ours | **Fork-only artefact** — `docs/` (additional) + `scripts/` are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `scripts/qoder-smoke.ts` | new | other | ours | **Fork-only artefact** — `docs/` (additional) + `scripts/` are njuptlzf/mcp-adapter-specific. Upstream has no equivalent. |
| `.planning/PROJECT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/REQUIREMENTS.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/ROADMAP.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/STATE.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/config.json` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/01-universal-adapter/01-01-CONTEXT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/01-universal-adapter/01-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/01-universal-adapter/01-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/01-universal-adapter/01-VERIFICATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/02-dependency-restructuring/02-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/02-dependency-restructuring/02-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/02-dependency-restructuring/02-VERIFICATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-02-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-03-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-03-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/03-core-logic-abstraction/03-VERIFICATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-02-COVERAGE.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-02-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/04-testing-verification/04-VERIFICATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-00-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-00-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-02-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-03-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-03-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-04-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-04-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-05-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-05-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-CONTEXT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-DISCUSSION-LOG.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-PATTERNS.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-RESEARCH.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-UAT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/05-VALIDATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/05-type-decoupling-entry-point-refactor/deferred-items.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-02-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-03-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-03-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-04-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-04-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-05-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-05-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-CONTEXT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-LEARNINGS.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-RESEARCH.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-UAT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/06-second-agent-adapter/06-VALIDATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-01-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-02-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-03-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-03-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-04-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-04-SUMMARY.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-CONTEXT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-DISCUSSION-LOG.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-LEARNINGS.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-RESEARCH.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-VALIDATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/07-VERIFICATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/07-integration-test-rebuild/deferred-items.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-01-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-02-PLAN.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-CONTEXT.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-DISCUSSION-LOG.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-RESEARCH.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `.planning/phases/08-upstream-merge-conflict-resolution/08-VALIDATION.md` | new | planning | ours | **Fork-only GSD workflow** — `.planning/` is this fork's phased development tracker. Upstream has no equivalent. |
| `skills/README.md` | new | skill | ours | Skills directory index. Fork-only. |
| `skills/mcp-adapter-test/SKILL.md` | new | skill | ours | **D-21** — per-agent references 复制 `_template.md`;主文件不动 = UPSTREAM-04 merge-friendly. Fork-only skill. |
| `skills/mcp-adapter-test/references/agent-paths/_template.md` | new | skill | ours | Per-agent parametric reference (D-10 / D-21). New adapter = new file,主文件 SKILL.md 不动. |
| `skills/mcp-adapter-test/references/agent-paths/pi.md` | new | skill | ours | Per-agent parametric reference (D-10 / D-21). New adapter = new file,主文件 SKILL.md 不动. |
| `skills/mcp-adapter-test/references/agent-paths/qoder.md` | new | skill | ours | Per-agent parametric reference (D-10 / D-21). New adapter = new file,主文件 SKILL.md 不动. |
| `skills/mcp-adapter-test/references/mcp-config.md` | new | skill | ours | mcp-adapter-test parametric reference (D-10). Fork-only. |
| `skills/mcp-adapter-test/references/smoke-calls.md` | new | skill | ours | mcp-adapter-test parametric reference (D-10). Fork-only. |
| `agent-dir.ts` | modified | source | assess | **assess** — **DECOUPLE-07** completed: `getAgentDir` now uses `AgentPathResolver` (D-07 / Phase 2). Accept upstream; if `PI_CODING_AGENT_DIR` env-fallback re-introduced, manual review. |
| `commands.ts` | modified | source | assess | **assess** — `ctx.ui.notify/form/custom/theme` (14 hits) is **generic `UISystem`** by D-04 / Phase 3, **not Pi-coupling** (RESEARCH Dimension 3 key finding #3). Accept upstream; do NOT flag as follow-up. |
| `config.ts` | modified | source | assess | **assess** — generic config layer. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `direct-tools.ts` | modified | source | assess | **assess** — **DECOUPLE-01/03** completed: imports generic `McpToolResult` + `AgentContext` (D-04). Run grep; if 0 hits, `--theirs`. |
| `elicitation-handler.ts` | modified | source | assess | **assess** — **DECOUPLE-02** completed: imports generic `UISystem`. Run grep; if 0 hits, `--theirs`. |
| `index.ts` | modified | source | manual | **manual** — **D-04 / ENTRY-02 / 05-05** backward-compat wrapper. Legal Pi-coupling (`ExtensionAPI` import). Accept upstream only if `mcpAdapter(pi: ExtensionAPI)` signature unchanged. Re-shaping the public Pi signature = manual review. |
| `init.ts` | modified | source | assess | **assess (HIGH RISK)** — core MCP init; 14 upstream commits. Run Pi-coupling marker grep; if 0 hits, `--theirs`; if hits, follow UPSTREAM-03-C. |
| `mcp-auth-flow.ts` | modified | source | assess | **assess (HIGH RISK)** — core MCP auth flow; continuous upstream bugfix area. Run Pi-coupling marker grep. |
| `mcp-oauth-provider.ts` | modified | source | assess | **assess (HIGH RISK)** — core MCP OAuth provider; continuous upstream bugfix area. Run Pi-coupling marker grep. |
| `mcp-panel.ts` | modified | source | assess | **assess + follow-up needed** — still imports `@earendil-works/pi-tui` (`matchesKey`, `truncateToWidth`, `visibleWidth`). **DECOUPLE-06 only covered `tool-result-renderer.ts`**, NOT this file. Future Phase 9 should decouple. See Special-cases footnotes. |
| `mcp-setup-panel.ts` | modified | source | assess | **assess + follow-up needed** — still imports `@earendil-works/pi-tui` (`matchesKey`, `truncateToWidth`, `visibleWidth`). **DECOUPLE-06 only covered `tool-result-renderer.ts`**, NOT this file. Future Phase 9 should decouple. See Special-cases footnotes. |
| `panel-keys.ts` | deleted | source | ours | **ours (deleted in fork)** — present in upstream; **fork removed it** (legacy; not referenced after D-04 Phase 3). Keep fork-side deletion; do NOT `git checkout --theirs panel-keys.ts` (would re-add an unused file). |
| `proxy-modes.ts` | modified | source | assess | **assess** — **DECOUPLE-01/04** completed: imports generic `McpToolResult` + generic `ToolInfo` (D-04). Run grep; if 0 hits, `--theirs`. |
| `sampling-handler.ts` | modified | source | assess | **assess** — **DECOUPLE-02/05** completed: imports generic `UISystem` + agent-agnostic sampling abstractions. Run grep; if 0 hits, `--theirs`. |
| `server-manager.ts` | modified | source | assess | **assess** — core MCP server management. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `state.ts` | modified | source | assess | **assess** — core MCP state. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `tool-metadata.ts` | modified | source | assess | **assess** — core MCP tool metadata. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `tool-result-renderer.ts` | modified | source | assess | **assess** — **DECOUPLE-06** completed: uses generic `RenderOutput` (D-04). Run grep; if 0 hits, `--theirs`. |
| `types.ts` | modified | source | assess | **assess** — generic types layer. Run Pi-coupling marker grep; `AgentToolResult` comment-only mention is legal. |
| `types/pi-ai.d.ts` | new | source | ours | **ours** — Pi type declaration (D-04 / Phase 5). Legal Pi-coupling; keep ours. |
| `types/pi-coding-agent.d.ts` | new | source | ours | **ours** — Pi type declaration (D-04 / Phase 5). Legal Pi-coupling; keep ours. |
| `types/pi-tui.d.ts` | new | source | ours | **ours** — Pi type declaration (D-04 / Phase 5). Legal Pi-coupling; keep ours. |
| `ui-resource-handler.ts` | modified | source | assess | **assess** — core MCP UI resource handler. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `ui-session.ts` | modified | source | assess | **assess** — core MCP UI session. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `utils.ts` | modified | source | assess | **assess** — generic utilities layer. Run Pi-coupling marker grep; if 0 hits, `--theirs`. |
| `__tests__/adapter-contract.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/agent-paths-integration.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/agent-paths.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/capability-gate.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/compatibility/legacy-pi-mock.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/direct-tools-auto-auth.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/elicitation-handler.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/elicitation-sdk-integration.test.ts` | deleted | test | ours | **Deleted in fork** — replaced by Phase 7 parametric framework (D-08 generic `MockAgentAPI` + D-09 `describe.each(AGENT_ADAPTERS)`). Do NOT `git checkout --theirs` (would re-add a Pi-coupled test). |
| `__tests__/entry.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/fixtures/mock-agent-api.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/index-lifecycle.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/init-elicitation.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/integration.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/mcp-auth-flow-client-credentials.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/mcp-oauth-provider.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/mcp-panel-keybindings.test.ts` | deleted | test | ours | **Deleted in fork** — replaced by Phase 7 parametric framework (D-08 generic `MockAgentAPI` + D-09 `describe.each(AGENT_ADAPTERS)`). Do NOT `git checkout --theirs` (would re-add a Pi-coupled test). |
| `__tests__/mock-adapter.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/pi-adapter.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/pi-renderer.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/pi-sampling-provider.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/proxy-modes-auto-auth.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/proxy-modes-manual-auth.test.ts` | deleted | test | ours | **Deleted in fork** — replaced by Phase 7 parametric framework (D-08 generic `MockAgentAPI` + D-09 `describe.each(AGENT_ADAPTERS)`). Do NOT `git checkout --theirs` (would re-add a Pi-coupled test). |
| `__tests__/qoder-adapter-integration.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/qoder-adapter.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/qoder-sampling-provider.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/sampling-handler.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/server-manager-sampling.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/tool-metadata.test.ts` | deleted | test | ours | **Deleted in fork** — replaced by Phase 7 parametric framework (D-08 generic `MockAgentAPI` + D-09 `describe.each(AGENT_ADAPTERS)`). Do NOT `git checkout --theirs` (would re-add a Pi-coupled test). |
| `__tests__/tool-result-renderer.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `__tests__/ui-resource-handler.test.ts` | modified | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/TESTING.md` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/agent-scenarios/.mcp.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/agent-scenarios/AGENTS.md` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/agent-scenarios/e2e-runner.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/agent-scenarios/smoke-calculator.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/compatibility/non-pi-agent.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/compatibility/results/compatibility-report.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/01-calculator/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/01-calculator/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/02-string-utils/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/02-string-utils/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/03-datetime/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/03-datetime/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/04-unit-converter/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/04-unit-converter/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/05-json-tools/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/05-json-tools/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/06-markdown/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/06-markdown/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/07-file-stats/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/07-file-stats/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/08-http-mock/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/08-http-mock/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/09-kv-store/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/09-kv-store/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/10-text-analyzer/server-spec.json` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/demo-servers/10-text-analyzer/server.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/fixtures/mock-agent.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/global-setup.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/reporters/matrix-reporter.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/smoke/calculator-smoke.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/smoke/e2e-all-servers.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/smoke/e2e-direct-tools.test.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/token-benchmark/report.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/token-benchmark/run-adapter.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/token-benchmark/run-baseline.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/token-benchmark/run-conversation-sim.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |
| `tests/token-benchmark/token-counter.ts` | new | test | assess | **Assess via Pi-coupling marker grep** — local parametric framework (D-08/D-09/D-10) + generic fixture. Accept upstream changes that don't reintroduce `ExtensionContext` / `@earendil-works/pi-*` (Dimension 3 grep). |

## Special-cases footnotes

- `mcp-panel.ts` + `mcp-setup-panel.ts` (Status=modified, source, assess): **DECOUPLE-06 only covered `tool-result-renderer.ts`**; these panel files still import `@earendil-works/pi-tui` (`matchesKey`, `truncateToWidth`, `visibleWidth`). Future Phase 9 follow-up should decouple them behind the generic `RenderOutput` interface.
- `index.ts` (Status=modified, source, manual): **Legal Pi-coupling** — D-04/ENTRY-02/05-05 backward-compat wrapper. Any upstream diff that re-shapes the public `mcpAdapter(pi: ExtensionAPI)` signature requires manual review. The `ExtensionAPI` import is intentional, not a regression.
- `interfaces/agent-api.ts` (Status=new, interface, manual): **Legal Pi-coupling** — JSDoc and `envHints` mention `ExtensionAPI` / `PI_CODING_AGENT_DIR` for Capability Gate detection (D-01..D-03). Do NOT treat as follow-up.
- `panel-keys.ts` (Status=deleted, source, ours): **Divergence note** — file is present in upstream; fork has intentionally removed it (legacy; not referenced after D-04 Phase 3). Keep fork-side deletion. Do NOT run `git checkout --theirs panel-keys.ts` (would re-add an unused file). See UPSTREAM-03 deleted-file rule.

## Generation notes

- Generated: 2026-06-18 (one-shot baseline; future refresh via PR re-running `git diff upstream/main --name-status`).
- Total diverged files: 209 (raw `git diff` output, no vendor paths present in this snapshot); manifest rows: 209 (post-vendor-filter, see `/tmp/upstream-divergence.tsv`).
- Default Resolution distribution: 114 `ours` (fork-only), 84 `assess` (core MCP / test — marker grep required), 11 `manual` (interfaces / config / docs — human review required).
- D-21 cited in 4 rows (`skills/mcp-adapter-test/SKILL.md` + 3 `references/agent-paths/<id>.md`); DECOUPLE-XX cited in 8 rows (DECOUPLE-01/02/06/07).
